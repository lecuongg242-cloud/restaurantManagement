---
phase: 03-loi-order
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/orders/kds.ts
  - app/r/[slug]/kds/page.tsx
  - app/r/[slug]/kds/actions.ts
  - components/kds/KdsBoard.tsx
  - components/kds/KdsTicket.tsx
autonomous: false
requirements: [ORDER-04]
user_setup:
  - "Test realtime cần 2 cửa sổ cạnh nhau: POS (duyệt) + KDS (nhận vé). Đo ≤3s cần đồng hồ/timestamp trên màn."
must_haves:
  truths:
    - "Chỉ item thuộc order confirmed hiện trên KDS; vé mới xuất hiện ≤3s sau khi POS duyệt (không reload)"
    - "3 cột: Chờ làm · Đang làm · Sẵn sàng; bếp bấm 'Bắt đầu' (queued→preparing) / 'Xong' (preparing→ready) mức MÓN"
    - "Vé hiện bàn + đồng hồ đếm lên + món+SL+tùy chọn+ghi chú; quá ngưỡng phút → viền/nhãn status-late"
    - "Order tự lên ready khi mọi item ready; POS thấy để phục vụ"
  artifacts:
    - "KdsBoard.tsx subscribe postgres_changes filter tenant_id; hiển thị delta (now − confirmed_at) khi vé xuất hiện lần đầu (đo ORDER-04)"
    - "kds/actions.ts startItem/readyItem kiểm transition + roll-up order status"
    - "Chữ lớn đọc xa, màu status mạnh + NHÃN chữ (không chỉ màu — a11y mù màu)"
  key_links:
    - "Nguồn vé = orders.status in (confirmed, preparing, ready) + items chưa served/cancelled, gom vé theo order"
    - "confirmed_at (0008) là mốc đo ≤3s; roll-up: mọi item ready → order ready (server action, không trigger DB)"
---

<objective>
Màn hình bếp realtime: vé món hiện ≤3s sau duyệt, bếp đổi trạng thái làm/xong ở mức món, quá hạn cảnh báo. Khép kín chuỗi khách → POS → bếp; kèm phép đo ORDER-04 (10 lần).

Purpose: Bằng chứng ORDER-04 (KDS ≤3s — tiêu chí phát hành V1 #2).
Output: Trang KDS đầy đủ (thay placeholder P1), actions đổi trạng thái item, phép đo độ trễ.
</objective>

<context>
@docs/10-BanThietKe/03-LuongFrontend.md   (§E)
@docs/10-BanThietKe/02-FrontendChiTiet.md (§4.3 KDS, §2.2 status, §7 a11y)
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§6 realtime)
@docs/30-KeHoach/P3/00-TongQuan.md        (quyết định #7, #8)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Truy vấn KDS + actions trạng thái item</name>
  <files>lib/orders/kds.ts, app/r/[slug]/kds/actions.ts</files>
  <read_first>lib/orders/status.ts, app/r/[slug]/pos/actions.ts (mẫu guard)</read_first>
  <action>kds.ts (server, RLS): getKdsTickets(tenantId) → orders (confirmed/preparing/ready, kèm confirmed_at, bàn qua table_session→tables) + items (queued/preparing/ready) + modifiers snapshot — gom vé theo order, sort confirmed_at tăng. actions.ts (guard canAccess('kds')): startItem(itemId, staffId) queued→preparing (order confirmed→preparing nếu là item đầu); readyItem(itemId, staffId) preparing→ready + prepared_at=now; sau mỗi ready: nếu mọi item của order ∈ {ready, served, cancelled} → order.status='ready'. Kiểm canTransition + scope tenant.</action>
  <verify>readyItem trên item queued → lỗi; item cuối ready → order ready</verify>
  <acceptance_criteria>
    - Đổi trạng thái mức MÓN; roll-up order đúng (kể cả khi có item cancelled)
    - Guard vai trò KDS (station/kitchen/owner/manager)
  </acceptance_criteria>
  <done>Nghiệp vụ bếp mức món</done>
</task>

<task type="auto">
  <name>Task 2: Bảng KDS 3 cột realtime + vé</name>
  <files>app/r/[slug]/kds/page.tsx, components/kds/KdsBoard.tsx, components/kds/KdsTicket.tsx</files>
  <read_first>app/r/[slug]/kds/page.tsx (placeholder P1), components/pos/PosBoard.tsx (mẫu subscribe 03-02)</read_first>
  <action>kds/page.tsx (server): guard canAccess('kds') + getKdsTickets → KdsBoard (client, initial + tenantId). KdsBoard: subscribe postgres_changes orders + order_items (filter tenant_id) → merge state; order mới confirmed → vé vào cột "Chờ làm" + toast nhẹ + **badge delta giây (now − confirmed_at) lưu lại lần xuất hiện đầu** (phục vụ đo ORDER-04, hiện góc vé, xanh nếu ≤3s / đỏ nếu >3s). 3 cột "Chờ làm · Đang làm · Sẵn sàng" (item ở cột theo status; vé nhóm theo order, item trong vé đổi cột cùng vé — vé hiện ở cột của item "thấp" nhất). KdsTicket: tên bàn (Fraunces lớn) + đồng hồ đếm lên từ confirmed_at + dòng món SL×tên + tùy chọn (thụt lề) + ghi chú (nổi bật); nút LỚN ≥44px "Bắt đầu"/"Xong" từng món; >10 phút chưa ready → viền + nhãn "TRỄ" (status-late, kèm chữ). Chữ cỡ lớn (body ≥18px), tương phản mạnh, không trang trí.</action>
  <verify>POS duyệt ở cửa sổ kia → vé hiện + badge delta ≤3s; Bắt đầu/Xong di chuyển đúng cột; vé để >10' hiện TRỄ</verify>
  <acceptance_criteria>
    - Vé chỉ hiện khi order confirmed (pending_confirm KHÔNG hiện)
    - Badge delta giây hiển thị lúc vé xuất hiện (công cụ đo ORDER-04)
    - Trạng thái = màu + nhãn chữ; nút ≥44px; chữ đọc xa
  </acceptance_criteria>
  <done>KDS realtime hoạt động</done>
</task>

<task type="auto">
  <name>Task 3: Phép đo ≤3s × 10 lần</name>
  <files>docs/30-KeHoach/P3/03-03-SUMMARY.md (bảng đo)</files>
  <read_first>docs/30-KeHoach/P3/00-TongQuan.md (quyết định #8)</read_first>
  <action>Kịch bản đo: 2 cửa sổ (POS + KDS) cùng máy. Lặp 10 lần: khách/POS tạo order → POS duyệt → đọc badge delta trên vé KDS → ghi vào bảng (lần, giây, đạt/không). Nếu >3s: kiểm tra lại filter subscribe (đúng tenant_id, đúng bảng), số kênh (1 kênh chung, không tạo mới mỗi render), và initial fetch không chặn render. Ghi bảng 10 dòng + trung bình vào SUMMARY.</action>
  <verify>10/10 lần ≤3s (hoặc ghi rõ lần trượt + nguyên nhân + cách xử lý)</verify>
  <acceptance_criteria>
    - Bảng đo 10 lần có số liệu thật trong SUMMARY
    - Trung bình và max ≤3s
  </acceptance_criteria>
  <done>ORDER-04 có bằng chứng đo</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KDS realtime 3 cột. Dev localhost:3000, 2 cửa sổ POS + KDS.</what-built>
  <how-to-verify>
    1. Cửa sổ A: /r/pho-viet/pos (station + PIN). Cửa sổ B: /r/pho-viet/kds (cùng phiên station hoặc đăng nhập lại).
    2. Điện thoại gửi order → A duyệt → B: vé hiện cột "Chờ làm" KHÔNG reload, badge delta ≤3s, đồng hồ chạy.
    3. Vé đủ: bàn, món+SL, tùy chọn thụt lề, ghi chú nổi bật. Order pending_confirm chưa duyệt KHÔNG hiện trên B.
    4. B bấm "Bắt đầu" 1 món → cột "Đang làm"; "Xong" → "Sẵn sàng"; mọi món xong → A thấy item ready để bấm "Đã phục vụ".
    5. Để 1 vé quá 10 phút → nhãn "TRỄ" đỏ (kèm chữ, không chỉ màu).
    6. Chạy phép đo 10 lần (Task 3) → ghi bảng.
    7. KDS của Bún Bò không nhận vé Phở Việt (RLS + filter).
  </how-to-verify>
  <resume-signal>Gõ "approved, đo ≤3s x/10" hoặc mô tả lỗi (vé không hiện, delta >3s, cột sai, RLS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Vé chỉ hiện sau confirmed; realtime không reload
- [ ] Đo 10 lần ≤3s có bảng số liệu (ORDER-04)
- [ ] Đổi trạng thái mức món; roll-up order ready; POS phục vụ được
- [ ] Vé trễ cảnh báo màu + chữ; touch ≥44px; cách ly tenant
</verification>

<success_criteria>
- Checkpoint "approved" + bảng đo 10 lần đạt
- ORDER-04 (tiêu chí phát hành V1 #2) chứng minh bằng số liệu
</success_criteria>

<output>
Tạo docs/30-KeHoach/P3/03-03-SUMMARY.md (kèm bảng đo 10 lần: lần | giây | đạt).
</output>
