---
phase: 03-loi-order
plan: 01
type: execute
wave: 1
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - supabase/migrations/0008_orders_core.sql
  - lib/orders/types.ts
  - lib/orders/status.ts
  - lib/orders/customer-menu.ts
  - lib/orders/create-order.ts
  - app/r/[slug]/(customer)/menu/page.tsx
  - app/r/[slug]/api/order/route.ts
  - app/r/[slug]/api/order/[id]/route.ts
  - app/r/[slug]/(customer)/order/[id]/page.tsx
  - components/customer/MenuBrowser.tsx
  - components/customer/ModifierSheet.tsx
  - components/customer/CartSheet.tsx
  - components/customer/QtyStepper.tsx
  - components/customer/OrderStatusStepper.tsx
autonomous: false
requirements: [TABLE-02, ORDER-01, MENU-02]
user_setup:
  - "Cần 1 bàn có qr_token từ P2 (/r/pho-viet/admin/tables) và menu có món + nhóm tùy chọn."
must_haves:
  truths:
    - "Khách mở /r/[slug]/menu?t=[qr_token] thấy menu đúng nhà hàng + tên bàn; món hết mờ + nhãn 'Hết', không thêm vào giỏ được"
    - "Chọn món → sheet tùy chọn (validate min/max/required) + SL + ghi chú → giỏ → 'Gửi order' ≤6 chạm; không vỡ ở 360px, nút ≥44px"
    - "Gửi order tạo orders status=pending_confirm (snapshot tên/giá) + mở/ghép table_session; bàn sang occupied"
    - "/r/[slug]/order/[id] hiện stepper trạng thái, tự cập nhật (polling)"
  artifacts:
    - "0008_orders_core.sql: table_sessions (1 phiên open/bàn), orders, order_items, order_item_modifiers + RLS + realtime publication"
    - "app/r/[slug]/api/order/route.ts: POST tạo order qua service role, validate server (available + min/max/required), scope theo qr_token"
    - "ModifierSheet validate min/max/required ở client TRƯỚC khi thêm giỏ; server re-validate"
  key_links:
    - "create-order.ts resolve qr_token → tables → tenant_id (KHÔNG tin slug đơn thuần); mọi ghi anon qua service role (D15)"
    - "qr_order_auto_send=true (02-04) → order vào thẳng confirmed (bỏ duyệt D8)"
    - "Giỏ là state client (sessionStorage theo qr_token) — không ghi DB trước khi gửi"
---

<objective>
Khách quét QR gọi món trên điện thoại: menu theo bàn → tùy chọn/ghi chú → giỏ → gửi (pending_confirm) → theo dõi trạng thái. Lát cắt dọc đầu tiên của chuỗi order-đến-bếp; mở/ghép phiên bàn (TABLE-02 phần mở).

Purpose: Bằng chứng ORDER-01 + TABLE-02 (mở/ghép) + MENU-02 phần "khách không đặt được món hết"; đặt schema orders cho toàn P3/P4.
Output: Migration orders core, menu khách mobile-first, sheet tùy chọn + giỏ, API tạo order, trang theo dõi.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.3–3.4, §4 khách anon)
@docs/10-BanThietKe/02-FrontendChiTiet.md (§4.1 customer, §5.2 component mới)
@docs/10-BanThietKe/03-LuongFrontend.md   (§A)
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D3, D8, D15)
@docs/30-KeHoach/P3/00-TongQuan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration orders core</name>
  <files>supabase/migrations/0008_orders_core.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md (§3.3–3.4), supabase/migrations/0004_menu_core.sql (mẫu RLS)</read_first>
  <action>Tạo theo §3.3–3.4: table_sessions(id, tenant_id, table_id fk tables on delete cascade, status text check in ('open','closed') default 'open', opened_at default now(), closed_at, opened_by uuid null) + partial unique index (table_id) where status='open'. orders(id, tenant_id, table_session_id fk null, channel text check in ('dine_in','takeaway','delivery') default 'dine_in', source text check in ('qr','staff'), status text check in ('pending_confirm','confirmed','preparing','ready','served','completed','cancelled'), customer_contact jsonb null, note text, created_by uuid null, confirmed_by uuid null, confirmed_at timestamptz null, cancel_reason text null, created_at, updated_at). order_items(id, tenant_id, order_id fk orders on delete cascade, menu_item_id uuid null fk menu_items on delete set null, name_snapshot text not null, unit_price_snapshot int not null, qty int not null check >=1, note text, status text check in ('queued','preparing','ready','served','cancelled') default 'queued', cancel_reason text null, cancelled_by uuid null, prepared_at timestamptz null, created_at). order_item_modifiers(id, tenant_id, order_item_id fk on delete cascade, option_id uuid null, name_snapshot text not null, price_delta_snapshot int not null default 0). RLS bật + policy auth_tenant_ids() cả 4 bảng (mẫu 0004). Index: orders(tenant_id,status), orders(tenant_id,table_session_id), order_items(tenant_id,order_id), order_items(tenant_id,status), table_sessions(tenant_id,table_id). Cuối file: alter publication supabase_realtime add table public.orders, public.order_items.</action>
  <verify>supabase db push áp không lỗi; RLS bật 4 bảng; insert 2 phiên open cùng bàn bị chặn (unique)</verify>
  <acceptance_criteria>
    - 4 bảng tạo đủ cột snapshot (name_snapshot, unit_price_snapshot, price_delta_snapshot)
    - Partial unique: 1 table_session open/bàn
    - orders có confirmed_at (đo ≤3s ở 03-03) + cancel_reason; order_items có cancel_reason + cancelled_by (03-04)
    - Publication supabase_realtime chứa orders + order_items
  </acceptance_criteria>
  <done>Schema orders + RLS + realtime sẵn</done>
</task>

<task type="auto">
  <name>Task 2: Types + đọc menu khách + tạo order (server, D15)</name>
  <files>lib/orders/types.ts, lib/orders/status.ts, lib/orders/customer-menu.ts, lib/orders/create-order.ts</files>
  <read_first>lib/supabase/admin.ts, lib/menu/types.ts, lib/tenant/settings.ts</read_first>
  <action>types.ts: TableSession, Order, OrderItem, OrderItemModifier, OrderStatus, OrderItemStatus + CartLine (client: itemId, qty, note, optionIds[]). status.ts: ORDER_FLOW/ITEM_FLOW + canTransition(from,to) (máy trạng thái §3.4 — dùng chung POS/KDS). customer-menu.ts (server, service role): getCustomerMenu(slug) → tenant(name,logo) + categories active (sort) + items active (cả is_available=false để hiện 'Hết') + modifier groups/options gắn theo món — CHỈ cột công khai. resolveTable(slug, qrToken) → {tenant_id, table} hoặc null. create-order.ts (server, service role): createQrOrder({slug, qrToken, lines, note}) → resolve bàn; validate từng line: item tồn tại + active + is_available, option thuộc group gắn với món + is_available, đếm chọn/group thỏa min/max/required; tìm/tạo table_session open (set bàn occupied); đọc settings.qr_order_auto_send → status 'confirmed' (kèm confirmed_at=now) hoặc 'pending_confirm'; insert orders + order_items (snapshot) + order_item_modifiers (snapshot); trả {orderId} hoặc {error}.</action>
  <verify>Đơn giá snapshot = base_price + Σ price_delta; line món hết trả error; auto_send bật → status confirmed</verify>
  <acceptance_criteria>
    - create-order validate available + min/max/required Ở SERVER, không tin payload client
    - Snapshot đủ name/price vào order_items + order_item_modifiers
    - Mở/ghép table_session đúng (2 order liên tiếp cùng bàn → cùng session_id); bàn occupied
  </acceptance_criteria>
  <done>Lõi tạo order an toàn cho khách anon</done>
</task>

<task type="auto">
  <name>Task 3: Route Handlers order (POST tạo + GET theo dõi)</name>
  <files>app/r/[slug]/api/order/route.ts, app/r/[slug]/api/order/[id]/route.ts</files>
  <read_first>lib/orders/create-order.ts</read_first>
  <action>POST /r/[slug]/api/order: body {qrToken, lines[], note} → createQrOrder → 200 {orderId} | 400 {error} (thông báo tiếng Việt: món hết, thiếu tùy chọn bắt buộc…). Chặn payload rỗng/quá 50 dòng. GET /r/[slug]/api/order/[id]: service role đọc order + items + modifiers CHỈ theo id (uuid không đoán được — đủ V1) và slug khớp tenant; trả trạng thái cho stepper (không trả dữ liệu tenant khác). Cả 2 route KHÔNG dùng cookie phiên.</action>
  <verify>curl POST tạo order được; GET trả trạng thái; slug sai/id sai → 404</verify>
  <acceptance_criteria>
    - POST validate + trả lỗi tiếng Việt rõ ràng
    - GET scope slug→tenant khớp order.tenant_id, không rò dữ liệu
  </acceptance_criteria>
  <done>API khách anon hoạt động</done>
</task>

<task type="auto">
  <name>Task 4: Menu khách mobile-first + giỏ + gửi</name>
  <files>app/r/[slug]/(customer)/menu/page.tsx, components/customer/MenuBrowser.tsx, components/customer/ModifierSheet.tsx, components/customer/CartSheet.tsx, components/customer/QtyStepper.tsx</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md (§4.1, §5.2), app/r/[slug]/(customer)/page.tsx</read_first>
  <action>menu/page.tsx (server): đọc searchParams.t; getCustomerMenu + resolveTable; không có t hoặc token sai → vẫn hiện menu chế độ chỉ-xem + banner "Quét QR tại bàn để gọi món". Header: logo+tên tenant + tên bàn + nút giỏ (badge N). MenuBrowser (client): chip danh mục cuộn ngang (pill-tab), thẻ món (ảnh, tên, giá, nút '+' ≥44px); món is_available=false → mờ + nhãn 'Hết' + disable. Chạm món/'+': món có modifier → mở ModifierSheet (radio cho max=1, checkbox cho max>1, disable option hết, chặn vượt max, báo thiếu min/required), qty-stepper, ghi chú, nút "Thêm vào giỏ • {giá}" — món không modifier → thêm thẳng. Giỏ (state + sessionStorage theo qrToken): CartSheet sticky bottom "Xem giỏ (N món · tổng)" → sheet dòng món+tùy chọn+SL (sửa/xóa), ghi chú chung, nút "Gửi order" → POST → chuyển /r/[slug]/order/[id] + xóa giỏ. Lỗi POST → hiện tại chỗ, giữ giỏ. Đếm chạm luồng chuẩn: món → (tùy chọn) → Thêm → Xem giỏ → Gửi ≤6.</action>
  <verify>DevTools 360px: không tràn ngang, nút ≥44px; món hết không thêm được; gửi xong sang trang theo dõi</verify>
  <acceptance_criteria>
    - Menu chỉ cho đặt món available (MENU-02 phần khách)
    - ModifierSheet chặn vi phạm min/max/required ở client
    - Giỏ sticky bottom; gửi ≤6 chạm; 360px không vỡ; touch ≥44px
  </acceptance_criteria>
  <done>Khách gọi món đầu-cuối (ORDER-01)</done>
</task>

<task type="auto">
  <name>Task 5: Trang theo dõi order (polling)</name>
  <files>app/r/[slug]/(customer)/order/[id]/page.tsx, components/customer/OrderStatusStepper.tsx</files>
  <read_first>docs/30-KeHoach/P3/00-TongQuan.md (quyết định #1)</read_first>
  <action>page.tsx (server): render khung + id. OrderStatusStepper (client): poll GET /api/order/[id] mỗi 4s; stepper pending_confirm("Chờ xác nhận")→confirmed("Đã xác nhận")→preparing("Đang làm")→ready("Sẵn sàng")→served("Đã phục vụ"); cancelled → thông báo đỏ + lý do. Danh sách món + SL + tùy chọn (từ snapshot). Nút "Gọi thêm món" → quay lại /menu?t= (giữ token trong sessionStorage). Dừng poll khi served/cancelled.</action>
  <verify>Đổi orders.status trong DB → trang tự cập nhật trong ≤5s không reload</verify>
  <acceptance_criteria>
    - Stepper phản ánh đúng status; dừng poll ở trạng thái cuối
    - Có đường quay lại menu để gọi thêm (cùng phiên bàn)
  </acceptance_criteria>
  <done>Khách theo dõi được món (nền cho A5)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Luồng khách gọi món QR. Dev localhost:3000, DevTools mobile 360px.</what-built>
  <how-to-verify>
    1. /r/pho-viet/admin/tables → copy qr_token 1 bàn (hoặc quét QR in ở P2) → mở /r/pho-viet/menu?t=[token] ở chế độ mobile 360px.
    2. Header hiện logo + tên nhà hàng + tên bàn. Menu nhóm theo danh mục, chip cuộn ngang.
    3. Chạm "Phở bò" (có nhóm Size bắt buộc) → sheet tùy chọn: không chọn Size mà bấm Thêm → bị chặn; chọn "Lớn (+10.000)" + SL 2 + ghi chú "ít hành" → Thêm. Đếm chạm cả luồng tới Gửi ≤6.
    4. Bật "hết món" 1 món ở /admin/menu → reload menu khách: món mờ + "Hết", không thêm được.
    5. "Xem giỏ" → sửa SL, xóa 1 dòng → "Gửi order" → sang trang theo dõi "Chờ xác nhận".
    6. Kiểm DB (hoặc chờ 03-02): orders status=pending_confirm, có table_session open, bàn occupied; đơn giá = giá + phụ thu.
    7. Gọi thêm lần 2 cùng bàn → vẫn cùng table_session (ghép phiên).
    8. Token sai/thiếu → menu chỉ-xem, không gửi được order.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (validate tùy chọn, giỏ, phiên bàn, 360px, món hết).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0008 áp không lỗi; RLS 4 bảng; realtime publication có orders/order_items
- [ ] Khách gọi món ≤6 chạm, 360px không vỡ, nút ≥44px (ORDER-01)
- [ ] Món hết không đặt được từ khách (MENU-02)
- [ ] Order pending_confirm + snapshot giá/tên; mở/ghép phiên bàn, bàn occupied (TABLE-02 phần mở)
- [ ] Trang theo dõi polling cập nhật ≤5s
</verification>

<success_criteria>
- Checkpoint "approved"
- ORDER-01 chứng minh được trên mobile; schema orders sẵn cho 03-02..05 và P4
</success_criteria>

<output>
Tạo docs/30-KeHoach/P3/03-01-SUMMARY.md (file đã đổi, trạng thái cam kết, bằng chứng + số chạm đếm được).
</output>
