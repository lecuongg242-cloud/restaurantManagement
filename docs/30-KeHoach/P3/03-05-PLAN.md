---
phase: 03-loi-order
plan: 05
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - supabase/migrations/0009_print_jobs.sql
  - lib/print/adapter.ts
  - lib/print/kitchen-ticket.ts
  - app/r/[slug]/print/kitchen/[orderId]/page.tsx
  - components/pos/PendingOrdersDrawer.tsx
  - components/pos/OrderPanel.tsx
autonomous: false
requirements: [PRINT-01, PRINT-02]
user_setup:
  - "Không cần máy in — nghiệm thu bằng PDF preview của window.print() (rủi ro phần cứng đã ghi ở Roadmap)."
must_haves:
  truths:
    - "Sau khi duyệt order (hoặc từ panel bàn), bấm 'In phiếu bếp' → mở /r/[slug]/print/kitchen/[orderId] và gọi window.print()"
    - "Phiếu bếp: logo+tên nhà hàng (OPS-06), bàn, giờ, số phiếu, món+SL+tùy chọn+ghi chú; khổ 58mm và 80mm không tràn, JetBrains Mono đen trắng"
    - "Mỗi lần in ghi 1 dòng print_jobs (type=kitchen_ticket, payload snapshot, status=printed) — log cho cầu in V1.x"
    - "Đổi adapter không sửa nghiệp vụ: POS chỉ gọi PrintAdapter.printKitchenTicket()"
  artifacts:
    - "lib/print/adapter.ts: interface PrintAdapter {printReceipt, printKitchenTicket} + BrowserPrintAdapter (V1) + chỗ BridgePrintAdapter (V1.x, chưa implement)"
    - "0009_print_jobs.sql: print_jobs + RLS tenant"
    - "Route in: CSS @media print + @page size 58/80mm, không theme app"
  key_links:
    - "printKitchenTicket(orderId): ghi print_jobs (server action) rồi mở route in (client window.open + print)"
    - "Route in guard membership (owner/manager/station...) — nội dung phiếu lấy snapshot từ order_items (không đọc lại menu)"
    - "?w=58|80 chọn khổ; mặc định 80"
---

<objective>
Lớp in trừu tượng (D1) + phiếu bếp thật: duyệt order xong bấm in, phiếu 58/80mm đủ nội dung cho bếp làm món không cần nhìn màn hình. Chừa sẵn đường cắm cầu in ESC/POS tự động (V1.x) mà không sửa nghiệp vụ.

Purpose: Bằng chứng PRINT-01 + PRINT-02 + OPS-06 (logo ở phiếu bếp); nền cho hóa đơn P4 (PRINT-03 tái dùng adapter + pattern route in).
Output: Migration print_jobs, PrintAdapter + BrowserPrintAdapter, route in phiếu bếp, nút in trên POS.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.7, §7)
@docs/10-BanThietKe/02-FrontendChiTiet.md (§1 print profile, §5.3)
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D1)
@docs/30-KeHoach/P3/00-TongQuan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration print_jobs</name>
  <files>supabase/migrations/0009_print_jobs.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md (§3.7), supabase/migrations/0008_orders_core.sql (mẫu RLS)</read_first>
  <action>print_jobs(id uuid pk, tenant_id fk tenants on delete cascade, type text check in ('receipt','kitchen_ticket'), target_station text null, payload jsonb not null, status text check in ('pending','printed','failed') default 'pending', created_at default now(), printed_at timestamptz null). Index (tenant_id, status, created_at). RLS + policy auth_tenant_ids() (mẫu 0008).</action>
  <verify>db push áp không lỗi; RLS bật</verify>
  <acceptance_criteria>
    - Bảng đúng §3.7 (type/status check, payload jsonb)
    - RLS + policy tenant
  </acceptance_criteria>
  <done>Hàng đợi in (log V1, poll V1.x) sẵn</done>
</task>

<task type="auto">
  <name>Task 2: PrintAdapter + nội dung phiếu bếp</name>
  <files>lib/print/adapter.ts, lib/print/kitchen-ticket.ts</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md (§7)</read_first>
  <action>adapter.ts: type KitchenTicketView {orderId, tenantName, logoUrl, tableName, confirmedAt, ticketNo, items[{name, qty, modifiers[], note}]}; type BillView để trống khai báo (P4 điền). interface PrintAdapter {printReceipt(bill): Promise<void>; printKitchenTicket(ticket): Promise<void>}. BrowserPrintAdapter (client): printKitchenTicket → window.open(`/r/[slug]/print/kitchen/[orderId]?w=80`) (route tự gọi window.print onload). getPrintAdapter() trả BrowserPrintAdapter (V1); comment chỗ BridgePrintAdapter (V1.x — ghi print_jobs pending để cầu in poll; KHÔNG implement). kitchen-ticket.ts (server): buildKitchenTicket(orderId, tenantId) → đọc order + items (loại cancelled) + modifiers snapshot + bàn + tenant(name, logo_url) qua RLS; logKitchenTicketPrint(orderId) server action: insert print_jobs {type:'kitchen_ticket', payload: ticket, status:'printed', printed_at: now}.</action>
  <verify>Import không lỗi; buildKitchenTicket trả đủ trường; log ghi print_jobs</verify>
  <acceptance_criteria>
    - Interface PrintAdapter đúng §7; BrowserPrintAdapter mặc định; Bridge chừa chỗ không implement (PRINT-01)
    - Payload phiếu từ SNAPSHOT order_items (không join lại menu)
  </acceptance_criteria>
  <done>Lớp in trừu tượng (PRINT-01)</done>
</task>

<task type="auto">
  <name>Task 3: Route in phiếu bếp 58/80mm</name>
  <files>app/r/[slug]/print/kitchen/[orderId]/page.tsx</files>
  <read_first>app/r/[slug]/print/qr/page.tsx (mẫu route in P2), docs/10-BanThietKe/02-FrontendChiTiet.md (§5.3)</read_first>
  <action>page.tsx (server): guard getSessionMembership (owner/manager/station/cashier-đường-station); buildKitchenTicket; searchParams.w = 58|80 (mặc định 80). Render KHÔNG theme app: JetBrains Mono, đen trắng — đầu phiếu: logo (nếu có, ảnh nhỏ grayscale) + tên NH (OPS-06) + "PHIẾU BẾP" + số phiếu (6 ký tự cuối orderId) + bàn + giờ (HH:mm); thân: mỗi món `SLx TÊN` đậm, tùy chọn thụt 2 space, ghi chú `>> ghi chú` nổi bật; chân: dòng kẻ + tổng số món. CSS: @page {size: [58|80]mm auto; margin ≤3mm}; width cố định theo khổ; font 12–13px (58mm) / 13–14px (80mm); không tràn — tên dài wrap, không cắt chữ có dấu. Script nhỏ: window.print() onload (client component wrapper) + nút "In lại"/"Đóng" (class no-print). Khi mở route → gọi logKitchenTicketPrint (1 lần, qua action từ nút hoặc onload) ghi print_jobs.</action>
  <verify>Mở ?w=80 → PDF preview đúng khổ 80mm không tràn; ?w=58 tương tự; print_jobs +1 dòng</verify>
  <acceptance_criteria>
    - Đủ nội dung PRINT-02: bàn, giờ, món+SL+tùy chọn+ghi chú (+ logo/tên NH — OPS-06)
    - 58mm và 80mm đều không tràn khổ (PDF preview); mono đen trắng, không theme
    - Mỗi lần in ghi print_jobs printed
  </acceptance_criteria>
  <done>Phiếu bếp in được (PRINT-02)</done>
</task>

<task type="auto">
  <name>Task 4: Nút in trên POS</name>
  <files>components/pos/PendingOrdersDrawer.tsx, components/pos/OrderPanel.tsx</files>
  <read_first>components/pos/PendingOrdersDrawer.tsx (03-02)</read_first>
  <action>PendingOrdersDrawer: sau "Duyệt" thành công → hiện nút "In phiếu bếp" (hoặc auto-mở tab in kèm toggle nhỏ "tự mở khi duyệt" lưu localStorage — mặc định bật) gọi BrowserPrintAdapter.printKitchenTicket. OrderPanel: mỗi order confirmed trở đi có nút "In phiếu bếp" (in lại được — phiếu ghi 'IN LẠI' nếu print_jobs đã có dòng cho order). POS KHÔNG gọi route trực tiếp — chỉ qua PrintAdapter (đổi Bridge sau không sửa UI).</action>
  <verify>Duyệt → tab in mở + preview đúng; in lại từ panel có nhãn IN LẠI</verify>
  <acceptance_criteria>
    - POS gọi qua PrintAdapter interface, không hardcode window.open rải rác
    - Duyệt xong in được ngay; in lại từ panel bàn
  </acceptance_criteria>
  <done>Luồng duyệt→in khép kín</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PrintAdapter + phiếu bếp 58/80mm. Dev localhost:3000, dùng PDF preview (Save as PDF).</what-built>
  <how-to-verify>
    1. Điện thoại gửi order (món có tùy chọn + ghi chú "ít hành") → POS duyệt → tab in tự mở, hộp thoại in hiện.
    2. Chọn "Save as PDF": phiếu có logo+tên Phở Việt, số phiếu, bàn, giờ, "2x Phở bò" + "  + Lớn" + ">> ít hành". Không tràn khổ 80mm.
    3. Mở lại với ?w=58 → vẫn không tràn, chữ đọc được.
    4. In lại từ panel bàn → phiếu có nhãn "IN LẠI".
    5. DB: print_jobs có dòng kitchen_ticket status=printed mỗi lần in.
    6. Kiểm code: PendingOrdersDrawer/OrderPanel chỉ import PrintAdapter (không window.open trực tiếp) — PRINT-01.
    7. Món đã hủy (03-04) KHÔNG xuất hiện trên phiếu in lại.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (tràn khổ, thiếu nội dung, print_jobs, adapter).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0009 áp không lỗi; RLS print_jobs
- [ ] PrintAdapter interface + BrowserPrintAdapter mặc định; Bridge chừa chỗ (PRINT-01)
- [ ] Phiếu bếp đủ nội dung, 58/80mm không tràn (PDF preview) + logo/tên NH (PRINT-02, OPS-06)
- [ ] print_jobs ghi log mỗi lần in; món hủy không lên phiếu
</verification>

<success_criteria>
- Checkpoint "approved"
- PRINT-01 + PRINT-02 chứng minh được; OPS-06 phần phiếu bếp đóng (còn hóa đơn → P4)
- Pattern route in + adapter sẵn cho hóa đơn 80mm (P4, PRINT-03)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P3/03-05-SUMMARY.md. Ghi rõ: OPS-06 phần "đầu hóa đơn" chuyển P4; nghiệm thu "tự in ≤5s" hoãn tới khi có cầu in + phần cứng (Roadmap rủi ro).
</output>
