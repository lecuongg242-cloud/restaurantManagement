---
phase: 03-loi-order
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/orders/pos.ts
  - app/r/[slug]/pos/page.tsx
  - app/r/[slug]/pos/actions.ts
  - components/pos/PosBoard.tsx
  - components/pos/TableMap.tsx
  - components/pos/PendingOrdersDrawer.tsx
  - components/pos/OrderPanel.tsx
  - components/pos/AddItemsSheet.tsx
autonomous: false
requirements: [ORDER-02, ORDER-03]
user_setup:
  - "Cần order pending_confirm từ 03-01 (tạo bằng điện thoại/tab mobile trước khi test POS)."
must_haves:
  truths:
    - "POS /r/[slug]/pos hiện sơ đồ bàn theo khu vực (màu status: available/occupied/reserved/cleaning) cập nhật realtime"
    - "Order QR pending_confirm hiện trong drawer 'Chờ duyệt (N)' realtime; 'Duyệt' → confirmed (+confirmed_at, confirmed_by); 'Từ chối' → cancelled + lý do"
    - "Chọn bàn occupied → panel món của phiên; 'Thêm món' thay khách → order source=staff vào thẳng confirmed (bỏ duyệt)"
    - "POS đánh 'Đã phục vụ' mức món; 'Đóng phiên' thủ công → bàn về available"
  artifacts:
    - "pos/actions.ts: approveOrder/rejectOrder/createStaffOrder/serveItem/closeSession — kiểm transition máy trạng thái + role ở server"
    - "PosBoard.tsx subscribe postgres_changes (orders+order_items+tables) filter tenant_id, cập nhật không reload"
    - "AddItemsSheet tái dùng dữ liệu menu + validate modifier như khách"
  key_links:
    - "approveOrder chỉ nhận order đang pending_confirm (canTransition); ghi confirmed_at=now() làm mốc đo 03-03"
    - "Mọi action đổi trạng thái gọi broadcastOrderStatus(orderId) sau UPDATE — khách thấy realtime (quyết định P3 #1)"
    - "createStaffOrder ghép vào table_session open của bàn (mở nếu chưa có) — cùng logic 03-01"
    - "Thao tác gắn staff_membership_id: nhân viên hiện hành chọn qua PIN (P1) hoặc membership của owner/manager đăng nhập"
---

<objective>
POS thành trung tâm điều phối: sơ đồ bàn realtime, duyệt/từ chối order QR (D8), thêm món thay khách (bỏ duyệt), đánh đã-phục-vụ, đóng phiên thủ công. Lát cắt "khách gửi → POS duyệt" nối 03-01 với KDS (03-03).

Purpose: Bằng chứng ORDER-02 + ORDER-03; sinh order confirmed làm nguồn cho KDS + phiếu bếp.
Output: Trang POS đầy đủ (thay placeholder P1), actions duyệt/tạo/phục vụ/đóng phiên, realtime.
</objective>

<context>
@docs/10-BanThietKe/03-LuongFrontend.md   (§D)
@docs/10-BanThietKe/02-FrontendChiTiet.md (§4.2 POS, §2.2 màu bàn/status)
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D7, D8)
@docs/30-KeHoach/P3/00-TongQuan.md        (quyết định #2, #6, #7)
@docs/30-KeHoach/P3/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Truy vấn POS + actions duyệt/từ chối</name>
  <files>lib/orders/pos.ts, app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/orders/status.ts, lib/auth/session.ts, app/r/[slug]/station-actions.ts, components/staff/StationScreen.tsx (khung PIN P1)</read_first>
  <action>pos.ts (server, phiên RLS): getPosSnapshot(tenantId) → areas + tables + table_sessions open + orders (pending_confirm/confirmed/preparing/ready + items) gom theo bàn. actions.ts ("use server", guard getSessionMembership + canAccess('pos')): approveOrder(orderId, staffId) — kiểm status=pending_confirm → confirmed + confirmed_at=now() + confirmed_by=staffId, items giữ queued; rejectOrder(orderId, staffId, reason) — bắt buộc reason → orders.status=cancelled + cancel_reason, items→cancelled; serveItem(itemId, staffId) — ready→served, order→served khi mọi item served/cancelled; closeSession(sessionId, staffId) — confirm phía UI, set status=closed+closed_at, bàn→available. Mọi update scope .eq(tenant_id). staffId = membership id (từ picker PIN P1 hoặc membership người đăng nhập) — validate thuộc đúng tenant. **Sau mỗi UPDATE thành công (approve/reject/serve): gọi `broadcastOrderStatus(orderId)` (lib/orders/broadcast.ts, 03-01) để trang theo dõi của khách nhận realtime — quyết định P3 #1.**</action>
  <verify>approveOrder trên order đã confirmed → lỗi "trạng thái không hợp lệ"; reject thiếu lý do → chặn</verify>
  <acceptance_criteria>
    - Actions kiểm canTransition + scope tenant, không tin client
    - rejectOrder bắt buộc lý do; approveOrder ghi confirmed_at + confirmed_by
    - closeSession đóng phiên + bàn về available
  </acceptance_criteria>
  <done>Nghiệp vụ duyệt/phục vụ/đóng phiên an toàn</done>
</task>

<task type="auto">
  <name>Task 2: Sơ đồ bàn + drawer chờ duyệt (realtime)</name>
  <files>app/r/[slug]/pos/page.tsx, components/pos/PosBoard.tsx, components/pos/TableMap.tsx, components/pos/PendingOrdersDrawer.tsx</files>
  <read_first>app/r/[slug]/pos/page.tsx (placeholder P1), lib/supabase/client.ts, docs/10-BanThietKe/02-FrontendChiTiet.md (§2.2 màu bàn)</read_first>
  <action>pos/page.tsx (server): guard canAccess('pos') (P1) + getPosSnapshot → PosBoard (client) nhận initial + tenantId. PosBoard: supabase browser client subscribe postgres_changes INSERT/UPDATE trên orders + order_items (filter tenant_id=eq.) và UPDATE tables → merge vào state (không reload). Thanh trên: tên trạm + nút "Order QR chờ duyệt (N)" (badge status-new) + nhân viên hiện hành. TableMap: tab khu vực, lưới table-tile ≥44px màu theo status (§2.2: available viền hairline, occupied nền cream, reserved viền primary, cleaning surface+muted); tile hiện tên + số món chưa phục vụ. PendingOrdersDrawer: danh sách order pending_confirm (bàn, giờ, món+SL+tùy chọn+ghi chú) + nút "Duyệt" / "Từ chối" (dialog nhập lý do bắt buộc); order mới đến ⟳ hiện ngay + badge tăng.</action>
  <verify>2 cửa sổ: điện thoại gửi order → drawer POS hiện ≤3s không reload; duyệt → biến khỏi drawer, bàn đổi trạng thái</verify>
  <acceptance_criteria>
    - Realtime: order QR mới hiện không cần reload
    - Duyệt/Từ chối chạy đúng; từ chối có lý do; khách thấy stepper đổi (polling 03-01)
    - Sơ đồ bàn đúng 4 màu status, tab khu vực
  </acceptance_criteria>
  <done>Duyệt order QR trên POS (ORDER-02)</done>
</task>

<task type="auto">
  <name>Task 3: Panel bàn + thêm món thay khách + phục vụ + đóng phiên</name>
  <files>components/pos/OrderPanel.tsx, components/pos/AddItemsSheet.tsx, app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/orders/create-order.ts (tái dùng validate/snapshot), docs/30-KeHoach/P3/00-TongQuan.md (quyết định #2, #7)</read_first>
  <action>Chọn bàn trên TableMap → OrderPanel (cột phải): thông tin phiên (giờ mở), danh sách order_items của mọi order trong phiên (tên+SL+tùy chọn+ghi chú+badge trạng thái item), nút "Đã phục vụ" trên item ready (serveItem), nút "Thêm món", nút "Đóng phiên" (chỉ khi mọi item served/cancelled — kèm confirm; ghi chú P4 sẽ tự đóng khi thanh toán). AddItemsSheet: menu đủ danh mục (chỉ món available) + ModifierSheet/QtyStepper tái dùng từ components/customer; xác nhận → actions.createStaffOrder(tableId, lines, staffId): validate/snapshot như create-order (tách hàm dùng chung trong lib/orders/create-order.ts), source='staff', status='confirmed' + confirmed_at=now, ghép session open (mở nếu bàn trống → bàn occupied).</action>
  <verify>Bàn trống thêm món qua POS → phiên mở + order confirmed ngay (không qua pending); item ready → bấm phục vụ → served</verify>
  <acceptance_criteria>
    - createStaffOrder bỏ duyệt (source=staff → confirmed) — ORDER-03
    - Validate modifier + snapshot dùng CHUNG code với luồng khách (không copy)
    - Đóng phiên chặn khi còn item chưa served/cancelled
  </acceptance_criteria>
  <done>POS thêm món + phục vụ + vòng đời phiên (ORDER-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>POS duyệt order + thêm món. Dev localhost:3000, 2 cửa sổ (POS + điện thoại giả lập).</what-built>
  <how-to-verify>
    1. Cửa sổ A: đăng nhập station (station@pho-viet.test / StationPass123!) → /r/pho-viet/pos, chọn nhân viên + PIN (P1). Cửa sổ B: mobile 360px mở menu khách theo qr_token.
    2. B gửi order → A: badge "Chờ duyệt" tăng + drawer hiện order ≤3s KHÔNG reload; bàn chuyển occupied (cream).
    3. A bấm "Duyệt" → order sang confirmed; B (trang theo dõi) đổi "Đã xác nhận" NGAY (≤1–2s, broadcast — không reload).
    4. B gửi order thứ 2 → A "Từ chối" bỏ trống lý do → bị chặn; nhập lý do → order cancelled; B thấy thông báo hủy + lý do.
    5. A chọn bàn trống khác → "Thêm món" (món có Size) → xác nhận → order hiện ngay trong panel, KHÔNG qua chờ duyệt (source=staff).
    6. (Tạm thời chưa có KDS) Đổi item sang ready trong DB hoặc chờ 03-03 → nút "Đã phục vụ" → item served; mọi item served → "Đóng phiên" → bàn về available.
    7. Owner Bún Bò mở /r/bun-bo/pos → KHÔNG thấy bàn/order của Phở Việt (RLS).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (realtime, duyệt/từ chối, thêm món staff, đóng phiên, RLS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Order QR pending_confirm hiện realtime trên POS; duyệt → confirmed (+confirmed_at); từ chối bắt buộc lý do
- [ ] POS thêm món thay khách: source=staff, vào thẳng confirmed, ghép/mở phiên đúng
- [ ] Sơ đồ bàn 4 màu status + tab khu vực; phục vụ mức món; đóng phiên thủ công
- [ ] Cách ly tenant giữ nguyên
</verification>

<success_criteria>
- Checkpoint "approved"
- ORDER-02 + ORDER-03 chứng minh được; nguồn order confirmed sẵn cho KDS (03-03) + phiếu bếp (03-05)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P3/03-02-SUMMARY.md. Ghi rõ: đóng-phiên-khi-thanh-toán (TABLE-02 phần còn lại) chuyển P4.
</output>
