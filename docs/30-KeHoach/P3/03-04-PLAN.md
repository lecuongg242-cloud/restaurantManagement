---
phase: 03-loi-order
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/auth/pin-gate.ts
  - app/r/[slug]/pos/actions.ts
  - components/pos/CancelItemDialog.tsx
  - components/pos/OrderPanel.tsx
autonomous: false
requirements: [ORDER-05]
user_setup:
  - "Cần 1 nhân viên vai trò cashier có PIN (P1 /admin/staff) + 1 nhân viên waiter (để chứng minh bị chặn)."
must_haves:
  truths:
    - "Trên POS, món đã gửi bếp (order confirmed trở đi) chỉ hủy được sau khi nhập PIN của membership vai trò manager/cashier + lý do bắt buộc"
    - "Nhân viên waiter/kitchen nhập PIN → bị từ chối (không đủ quyền); bỏ trống lý do → bị chặn"
    - "Item hủy: status=cancelled + cancel_reason + cancelled_by (log truy vết được ai hủy); KDS gạch/ẩn vé tương ứng realtime"
    - "'Sửa món' = hủy dòng cũ (kiểm soát) + thêm dòng mới — không edit-in-place (quyết định P3 #3)"
  artifacts:
    - "lib/auth/pin-gate.ts: verifyPinForRoles(tenantId, membershipId, pin, roles[]) so bcrypt — dùng lại cho P4 (đóng bill)"
    - "cancelOrderItem trong pos/actions.ts: gate PIN server-side + transition hợp lệ + ghi cancel_reason/cancelled_by"
    - "CancelItemDialog: chọn người hủy (manager/cashier) + PIN + lý do"
  key_links:
    - "PIN xác thực Ở SERVER (bcrypt so pin_hash memberships) — client không giữ hash (D7)"
    - "Item cancelled → roll-up: mọi item còn lại ready → order ready; mọi item cancelled → order cancelled"
    - "KDS (03-03) nhận UPDATE item cancelled qua realtime → bỏ khỏi vé"
---

<objective>
Kiểm soát thất thoát: hủy món đã gửi bếp phải qua PIN vai trò manager/cashier + lý do, ghi log truy vết (D9). Đóng vòng "gọi nhầm/khách đổi ý" mà không phá dữ liệu bếp.

Purpose: Bằng chứng ORDER-05; dựng pin-gate tái dùng cho P4 (đóng bill, giảm giá).
Output: PIN gate server-side, action hủy món, dialog hủy trên POS, log truy vết.
</objective>

<context>
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D7 + làm rõ, D9)
@docs/10-BanThietKe/03-LuongFrontend.md   (§D bước 5)
@docs/30-KeHoach/P3/00-TongQuan.md        (quyết định #3, #6)
@docs/30-KeHoach/P3/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PIN gate theo vai trò (server)</name>
  <files>lib/auth/pin-gate.ts</files>
  <read_first>lib/auth/pin.ts (P1), app/r/[slug]/station-actions.ts (mẫu verify PIN P1)</read_first>
  <action>verifyPinForRoles({tenantId, membershipId, pin, allowedRoles}): đọc membership (active, đúng tenant) qua phiên RLS; role ∈ allowedRoles; bcrypt.compare(pin, pin_hash) — sai bất kỳ điều kiện nào trả {ok:false, error} (thông điệp chung "PIN hoặc quyền không hợp lệ", không lộ điều kiện nào sai). Trả {ok:true, staffId: membershipId}. LƯU Ý: owner/manager đăng nhập email không có pin_hash → cho phép đường tắt: nếu session hiện tại là owner/manager thì gate đạt với staffId = membership của chính họ (không cần PIN).</action>
  <verify>PIN đúng + role cashier → ok; PIN đúng + role waiter → từ chối; PIN sai → từ chối cùng thông điệp</verify>
  <acceptance_criteria>
    - So bcrypt ở server; thông điệp lỗi không phân biệt sai-PIN vs sai-quyền
    - Owner/manager phiên email đi qua gate không cần PIN (có membership id truy vết)
  </acceptance_criteria>
  <done>PIN gate tái dùng được (P4 dùng lại)</done>
</task>

<task type="auto">
  <name>Task 2: Action hủy món + roll-up</name>
  <files>app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/orders/status.ts, lib/auth/pin-gate.ts</read_first>
  <action>cancelOrderItem({itemId, gate: {membershipId, pin}, reason}): guard canAccess('pos'); reason.trim() bắt buộc; verifyPinForRoles(allowedRoles=['manager','cashier']); item chưa served/cancelled → status='cancelled' + cancel_reason=reason + cancelled_by=staffId; roll-up order: mọi item cancelled → order cancelled (+cancel_reason 'tất cả món bị hủy'); còn item khác → giữ, nếu các item còn lại đều ready → order ready. Sau UPDATE thành công: gọi `broadcastOrderStatus(orderId)` (03-01) — khách thấy món hủy/đơn hủy realtime. revalidate + trả kết quả cho UI. (Order pending_confirm: dùng rejectOrder 03-02 — không đi đường này.)</action>
  <verify>Hủy thiếu lý do → chặn; PIN waiter → chặn; PIN cashier → item cancelled + đủ cancel_reason/cancelled_by trong DB</verify>
  <acceptance_criteria>
    - Gate server-side (không tin client); lý do bắt buộc
    - Log: cancel_reason + cancelled_by truy vết được (ORDER-05 "có log")
    - Roll-up order đúng khi hủy 1 phần / toàn bộ
  </acceptance_criteria>
  <done>Hủy món có kiểm soát</done>
</task>

<task type="auto">
  <name>Task 3: UI hủy trên POS + phản chiếu KDS</name>
  <files>components/pos/CancelItemDialog.tsx, components/pos/OrderPanel.tsx</files>
  <read_first>components/staff/PinPad.tsx, components/staff/StaffPicker.tsx (P1)</read_first>
  <action>OrderPanel: mỗi item chưa served/cancelled có nút "Hủy" (text status-late) → CancelItemDialog: (1) chọn người duyệt hủy — danh sách membership manager/cashier active (+ ghi chú "đăng nhập owner/manager: bỏ qua PIN"), (2) PinPad 4 số (ẩn nếu owner/manager), (3) ô lý do bắt buộc (placeholder "Khách đổi ý / gọi nhầm…"), nút "Xác nhận hủy" đỏ. Lỗi gate → hiện trong dialog, cho nhập lại. Thành công → item gạch + nhãn "Đã hủy · [lý do]" trong panel; KDS tự cập nhật qua realtime (03-03 đã subscribe UPDATE) — kiểm chứng vé bỏ món hủy. Ghi chú UI "Cần sửa món? Hủy dòng này rồi Thêm món mới."</action>
  <verify>2 cửa sổ: hủy trên POS → KDS bỏ món khỏi vé không reload; item cancelled hiện gạch + lý do ở panel</verify>
  <acceptance_criteria>
    - Dialog đủ 3 bước: người duyệt → PIN → lý do; thông báo lỗi tại chỗ
    - KDS phản chiếu hủy realtime; panel hiện log hủy
  </acceptance_criteria>
  <done>Luồng hủy đầu-cuối trên POS + KDS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hủy món có kiểm soát (PIN + lý do). Dev localhost:3000, POS + KDS 2 cửa sổ.</what-built>
  <how-to-verify>
    1. Chuẩn bị: /admin/staff có "Lan" (cashier, PIN 1234) và "Hùng" (waiter, PIN 5678). Tạo order 2 món, duyệt → vé hiện KDS.
    2. POS panel bàn → "Hủy" 1 món: bỏ trống lý do → bị chặn.
    3. Chọn "Hùng" (waiter) + PIN 5678 + lý do → bị từ chối "PIN hoặc quyền không hợp lệ".
    4. Chọn "Lan" (cashier) + PIN sai → từ chối (cùng thông điệp). PIN 1234 + lý do "khách đổi ý" → hủy thành công.
    5. KDS: món biến khỏi vé KHÔNG reload; POS panel: dòng gạch + "Đã hủy · khách đổi ý".
    6. DB: order_items.cancel_reason + cancelled_by = membership Lan (truy vết).
    7. Hủy nốt món còn lại → order cancelled; khách (trang theo dõi) thấy đơn hủy.
    8. Đăng nhập owner → hủy không cần PIN (vẫn bắt lý do).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (gate quyền, PIN, lý do, log, realtime KDS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Hủy món đã gửi: chỉ manager/cashier (PIN server-side) + lý do bắt buộc
- [ ] Log truy vết: cancel_reason + cancelled_by đúng người
- [ ] KDS phản chiếu hủy realtime; roll-up order đúng
- [ ] Waiter/kitchen bị chặn; thông điệp lỗi không lộ thông tin
</verification>

<success_criteria>
- Checkpoint "approved"
- ORDER-05 chứng minh được; pin-gate sẵn cho P4 (đóng bill/giảm giá)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P3/03-04-SUMMARY.md.
</output>
