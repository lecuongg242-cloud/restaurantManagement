---
phase: 02-du-lieu-nha-hang
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - app/r/[slug]/admin/(protected)/onboarding/page.tsx
  - app/r/[slug]/admin/(protected)/onboarding/actions.ts
  - components/onboarding/OnboardingWizard.tsx
  - components/onboarding/steps/StepInfo.tsx
  - components/onboarding/steps/StepMenu.tsx
  - components/onboarding/steps/StepTables.tsx
  - components/onboarding/steps/StepDone.tsx
  - lib/onboarding/sample-menu.ts
  - app/r/[slug]/admin/(protected)/page.tsx
autonomous: false
requirements: [TENANT-03]
user_setup:
  - "Cần 1 người NGOÀI team để đo onboarding thực tế ≤15' (tiêu chí TENANT-03)."
must_haves:
  truths:
    - "Owner mới mở /r/[slug]/admin/onboarding thấy wizard 4 bước: Thông tin → Menu mẫu → Bàn+QR → Xong"
    - "Bước Menu mẫu có nút seed vài danh mục/món có sẵn để sửa nhanh (không phải gõ từ đầu)"
    - "Người ngoài team hoàn tất (thông tin NH + ~10 món + ~5 bàn + xuất QR) trong ≤15 phút"
  artifacts:
    - "OnboardingWizard.tsx điều phối 4 bước với tiến trình + điều hướng tiến/lùi"
    - "lib/onboarding/sample-menu.ts dữ liệu menu mẫu; actions.seedSampleMenu chèn cho tenant"
    - "dashboard hiện CTA onboarding khi chưa hoàn tất (settings.onboarding_done)"
  key_links:
    - "wizard tái dùng action của 02-01 (menu), 02-03 (bàn/QR), 02-04 (thông tin/logo)"
    - "markOnboardingDone ghi tenants.settings.onboarding_done = true"
---

<objective>
Gộp mọi thứ P2 thành một luồng dẫn dắt để chủ nhà hàng mới tự cấu hình đủ dữ liệu phục vụ trong ≤15 phút. Capstone của P2: "wizard 4 bước → seed menu mẫu → tạo bàn + xuất QR → xong". Đây là tiêu chí nghiệm thu TENANT-03.

Purpose: Bằng chứng TENANT-03 (onboarding ≤15'); nghiệm thu tổng P2.
Output: Wizard 4 bước, seed menu mẫu, cờ hoàn tất, CTA ở dashboard.
</objective>

<context>
@docs/10-BanThietKe/03-LuongFrontend.md
@docs/30-KeHoach/P2/02-01-PLAN.md
@docs/30-KeHoach/P2/02-03-PLAN.md
@docs/30-KeHoach/P2/02-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dữ liệu menu mẫu + seed action</name>
  <files>lib/onboarding/sample-menu.ts, app/r/[slug]/admin/(protected)/onboarding/actions.ts</files>
  <read_first>docs/30-KeHoach/P2/02-01-PLAN.md</read_first>
  <action>lib/onboarding/sample-menu.ts: mảng danh mục + món mẫu tiếng Việt (vd "Món chính": Phở bò 45000, Cơm gà 40000, Bún chả 50000; "Đồ uống": Trà đá 5000, Cà phê 25000; "Tráng miệng": Chè 20000) — đủ ~10 món. actions.ts (server): seedSampleMenu() guard canManage('onboarding'); CHỈ seed khi tenant chưa có danh mục nào (tránh trùng); chèn danh mục + món (giá integer) qua RLS scope tenant. markOnboardingDone()/getOnboardingState() đọc/ghi tenants.settings.onboarding_done. (Không seed ảnh — owner thêm sau.)</action>
  <verify>Bấm seed 1 lần → có ~10 món; bấm lần 2 (đã có danh mục) → không nhân đôi</verify>
  <acceptance_criteria>
    - sample-menu.ts có >= 8 món chia >= 2 danh mục
    - seedSampleMenu chỉ chèn khi tenant chưa có danh mục (idempotent an toàn)
    - markOnboardingDone ghi settings.onboarding_done = true
  </acceptance_criteria>
  <done>Seed menu mẫu sẵn</done>
</task>

<task type="auto">
  <name>Task 2: Wizard 4 bước</name>
  <files>app/r/[slug]/admin/(protected)/onboarding/page.tsx, components/onboarding/OnboardingWizard.tsx, components/onboarding/steps/StepInfo.tsx, StepMenu.tsx, StepTables.tsx, StepDone.tsx</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md (§F Onboarding), docs/30-KeHoach/P2/02-04-PLAN.md</read_first>
  <action>onboarding/page.tsx (server): guard canManage('onboarding'); đọc trạng thái (đã có menu? đã có bàn? logo?) để tô tiến trình. OnboardingWizard.tsx (client): thanh tiến trình 4 bước + nội dung theo bước (query ?step= hoặc state). StepInfo: tên NH + logo (tái dùng action 02-04). StepMenu: nút "Dùng menu mẫu" (seedSampleMenu) + link nhanh tới /admin/menu để sửa. StepTables: tạo nhanh N bàn (tái dùng action 02-03) + nút "Xuất QR" mở /print/qr. StepDone: tóm tắt (số món, số bàn) + nút "Hoàn tất" (markOnboardingDone) → về dashboard. Mỗi bước có nút Tiếp/Quay lại; cho phép bỏ qua bước.</action>
  <verify>Đi hết 4 bước: đặt tên+logo → seed menu → tạo 5 bàn+QR → hoàn tất → về dashboard</verify>
  <acceptance_criteria>
    - Wizard có đúng 4 bước với tiến trình + điều hướng tiến/lùi
    - StepMenu gọi seedSampleMenu; StepTables tạo bàn + mở /print/qr
    - StepDone gọi markOnboardingDone
  </acceptance_criteria>
  <done>Wizard hoạt động đầu-cuối</done>
</task>

<task type="auto">
  <name>Task 3: CTA onboarding ở dashboard</name>
  <files>app/r/[slug]/admin/(protected)/page.tsx</files>
  <read_first>docs/30-KeHoach/P1/01-02-PLAN.md, docs/30-KeHoach/P2/02-04-PLAN.md</read_first>
  <action>Dashboard: nếu settings.onboarding_done != true → hiện banner/CTA "Hoàn tất thiết lập nhà hàng" link tới /admin/onboarding; nếu đã xong → ẩn. Giúp owner mới thấy lối vào wizard ngay.</action>
  <verify>Owner chưa onboard thấy CTA; sau khi hoàn tất, CTA biến mất</verify>
  <acceptance_criteria>
    - Dashboard hiện CTA onboarding khi settings.onboarding_done chưa true
    - CTA ẩn sau khi hoàn tất
  </acceptance_criteria>
  <done>Lối vào onboarding rõ ràng</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wizard onboarding 4 bước. Dev localhost:3000. Cần 1 người NGOÀI team để đo thời gian.</what-built>
  <how-to-verify>
    1. (Chuẩn bị) Super-admin tạo 1 nhà hàng MỚI + owner mới (để onboard từ đầu, tránh dữ liệu Phở Việt cũ).
    2. Đưa người NGOÀI team đăng nhập owner mới → dashboard hiện CTA → mở /admin/onboarding. BẤM ĐỒNG HỒ.
    3. Bước 1: đặt tên + logo. Bước 2: "Dùng menu mẫu" → có ~10 món, sửa vài giá/tên. Bước 3: tạo ~5 bàn → "Xuất QR" → in/tải. Bước 4: "Hoàn tất".
    4. DỪNG ĐỒNG HỒ khi về dashboard (CTA đã ẩn). Ghi lại thời gian — tiêu chí ≤15 phút.
    5. Kiểm dữ liệu: /admin/menu có ~10 món, /admin/tables có ~5 bàn + QR xuất được.
  </how-to-verify>
  <resume-signal>Gõ "approved" + thời gian đo được (vd "approved, 11 phút") hoặc mô tả điểm nghẽn làm chậm >15'.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Wizard 4 bước chạy đầu-cuối; seed menu mẫu không nhân đôi
- [ ] Tạo bàn + xuất QR trong wizard
- [ ] Cờ onboarding_done ghi đúng; CTA dashboard ẩn/hiện theo cờ
- [ ] Người ngoài team hoàn tất ≤15' (đo thật)
- [ ] Mọi thao tác scope theo tenant (RLS)
</verification>

<success_criteria>
- Checkpoint "approved" + thời gian ≤15'
- TENANT-03 (onboarding ≤15') chứng minh bằng đo thực tế
- P2 hoàn tất: nhà hàng tự cấu hình đủ dữ liệu để sẵn sàng phục vụ (nền cho P3)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P2/02-05-SUMMARY.md. Sau khi cả 5 plan xong, tick P2 trong docs/00-TongQuan/Roadmap.md và cập nhật trạng thái yêu cầu (MENU-*, TABLE-01, OPS-06, TENANT-03) trong 20-DanhSachYeuCau.
</output>
