---
phase: 02-du-lieu-nha-hang
plan: 04
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - lib/tenant/settings.ts
  - app/r/[slug]/admin/(protected)/settings/page.tsx
  - app/r/[slug]/admin/(protected)/settings/actions.ts
  - components/settings/LogoUpload.tsx
  - components/admin/AdminShell.tsx
  - app/r/[slug]/(customer)/page.tsx
autonomous: false
requirements: [OPS-06]
user_setup: []
must_haves:
  truths:
    - "Owner cập nhật tên + logo nhà hàng ở /r/[slug]/admin/settings; logo hiện ở header admin và header khách"
    - "Owner cấu hình %phí phục vụ, %VAT, footer hóa đơn, và toggle 'duyệt order QR' — lưu vào tenants.settings"
    - "Cấu hình chỉ áp cho tenant hiện tại (RLS); tenant khác không đổi"
  artifacts:
    - "lib/tenant/settings.ts định nghĩa shape settings + default + đọc/ghi an toàn"
    - "settings/actions.ts updateSettings + uploadLogo (validate ảnh 2 lớp, service role ghi Storage)"
    - "AdminShell + (customer)/page.tsx hiển thị logo_url + name (OPS-06)"
  key_links:
    - "settings lưu trong tenants.settings jsonb (không cần migration mới); logo_url ở cột tenants.logo_url"
    - "guard canManage('settings'); chỉ owner/manager (V1 ưu tiên owner)"
---

<objective>
Cho owner đặt nhận diện nhà hàng (tên + logo) và cấu hình vận hành (phí phục vụ, VAT, footer hóa đơn, bật/tắt duyệt order QR). Lát cắt dọc "vào /admin/settings → đổi logo/tên → chỉnh %phí/%VAT → lưu → thấy logo ở shell". Đây là plan bổ sung để đóng OPS-06 trong P2 (quyết định 21/07/2026, xem 00-TongQuan §2).

Purpose: Bằng chứng OPS-06; đặt sẵn cấu hình bill (`service_charge_pct`, `vat_pct`, `receipt_footer`) cho P4 và cờ `qr_order_auto_send` cho luồng duyệt order QR (P3, D8).
Output: Trang cài đặt, upload logo, ghi tenants.settings, hiển thị logo ở shell admin + khách.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/10-BanThietKe/03-LuongFrontend.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P2/00-TongQuan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shape settings + đọc/ghi an toàn</name>
  <files>lib/tenant/settings.ts</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md (§3.1 settings), docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D6)</read_first>
  <action>lib/tenant/settings.ts: type TenantSettings { currency:'VND'; service_charge_pct:number; vat_pct:number; allow_discount:boolean; qr_order_auto_send:boolean; receipt_footer:string }. DEFAULT_SETTINGS (service_charge_pct 0, vat_pct 0, allow_discount true, qr_order_auto_send false, receipt_footer ''). parseSettings(jsonb) → merge default + validate (pct trong [0,100], clamp). serializeSettings(input). Không tạo migration — dùng cột tenants.settings jsonb (đã có 0001).</action>
  <verify>parseSettings với jsonb thiếu field → trả default đủ; pct ngoài [0,100] bị clamp</verify>
  <acceptance_criteria>
    - Export TenantSettings + DEFAULT_SETTINGS + parseSettings/serializeSettings
    - Validate/clamp service_charge_pct & vat_pct trong [0,100]
  </acceptance_criteria>
  <done>Đọc/ghi settings an toàn</done>
</task>

<task type="auto">
  <name>Task 2: Trang cài đặt + lưu cấu hình + upload logo</name>
  <files>app/r/[slug]/admin/(protected)/settings/page.tsx, settings/actions.ts, components/settings/LogoUpload.tsx</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md (§F Cài đặt), docs/30-KeHoach/P2/02-01-PLAN.md</read_first>
  <action>settings/page.tsx (server): guard canManage('settings'); đọc tenant (name, logo_url, settings) qua RLS. actions.ts: updateProfile(name), updateSettings(service_charge_pct, vat_pct, receipt_footer, qr_order_auto_send, allow_discount) → merge parseSettings → update tenants scope tenant_id (RLS); uploadLogo(FormData file) → validateImage (tái dùng lib/storage/images.ts, ≤2MB) → upload menu-images path {tenant_id}/logo-{rand} (service role) → cập nhật tenants.logo_url (+ xóa logo cũ). LogoUpload.tsx (client): giống ImageUpload nhưng cho logo (validate client + preview). Kích hoạt mục "Cài đặt" trong AdminNav (bỏ "chờ").</action>
  <verify>Đổi tên + upload logo → lưu; chỉnh VAT 8% + phí 5% + footer → lưu; đọc lại đúng</verify>
  <acceptance_criteria>
    - updateSettings ghi tenants.settings scope tenant_id (RLS), clamp pct
    - uploadLogo validate ảnh ở server trước khi ghi Storage; cập nhật logo_url
    - AdminNav: "Cài đặt" là link hoạt động
  </acceptance_criteria>
  <done>Cài đặt lưu được</done>
</task>

<task type="auto">
  <name>Task 3: Hiển thị logo + tên ở shell admin & header khách (OPS-06)</name>
  <files>components/admin/AdminShell.tsx, app/r/[slug]/(customer)/page.tsx</files>
  <read_first>docs/20-DanhSachYeuCau/00-Requirements.md (OPS-06), docs/30-KeHoach/P1/01-02-PLAN.md</read_first>
  <action>AdminShell.tsx: đã render logo_url + name (P1) — đảm bảo cập nhật sau khi đổi (revalidate). (customer)/page.tsx: header khách hiển thị logo_url (nếu có) + tên tenant thay cho ký tự đầu slug; giữ theme sản phẩm cố định (QD-006 F2, không override màu theo tenant). Ghi chú: hiển thị logo ở "đầu hóa đơn/phiếu bếp" thuộc P3/P4 (khi có route /print/*) — nêu ở SUMMARY là phần OPS-06 còn lại chuyển mốc.</action>
  <verify>Đổi logo → header admin + header khách (/r/[slug]) đều hiện logo mới</verify>
  <acceptance_criteria>
    - AdminShell hiển thị logo_url + name (cập nhật sau khi đổi)
    - (customer)/page.tsx header hiển thị logo_url + tên tenant
    - Chrome giữ theme sản phẩm cố định (không đổi màu theo tenant)
  </acceptance_criteria>
  <done>Logo tenant hiện ở shell (OPS-06 phần P2)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Trang cài đặt + logo + cấu hình tenant. Dev localhost:3000, owner demo.</what-built>
  <how-to-verify>
    1. Owner Phở Việt → /r/pho-viet/admin/settings → đổi tên hiển thị, upload logo (<2MB) → lưu.
    2. Kiểm header admin (sidebar) hiện logo + tên mới; mở /r/pho-viet (header khách) cũng hiện logo + tên.
    3. Đặt %phí phục vụ = 5, %VAT = 8, footer = "Cảm ơn quý khách", tắt "duyệt order QR" → lưu → tải lại trang thấy giữ giá trị.
    4. Thử %VAT = 150 → bị clamp/từ chối (không lưu giá trị vô lý).
    5. Owner Bún Bò mở settings → thấy cấu hình riêng của Bún Bò, không ảnh hưởng bởi Phở Việt (RLS).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (logo, lưu cấu hình, hiển thị shell, hoặc RLS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Cập nhật tên + logo → hiện ở header admin + khách
- [ ] Lưu %phí/%VAT/footer/toggle vào tenants.settings, clamp pct
- [ ] Upload logo validate ≤2MB (client + server)
- [ ] Cấu hình scope theo tenant (RLS)
</verification>

<success_criteria>
- Checkpoint "approved"
- OPS-06 (logo tenant ở header admin + khách) chứng minh được ở P2
- Cấu hình bill (phí/VAT/footer) + cờ duyệt-QR sẵn cho P3/P4
</success_criteria>

<output>
Tạo docs/30-KeHoach/P2/02-04-SUMMARY.md. Ghi rõ phần OPS-06 "logo ở đầu hóa đơn/phiếu bếp" chuyển mốc P3/P4 (khi có route in).
</output>
