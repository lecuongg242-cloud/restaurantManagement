---
phase: 02-du-lieu-nha-hang
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - supabase/migrations/0004_menu_core.sql
  - supabase/migrations/0005_storage_menu_images.sql
  - lib/auth/rbac.ts
  - lib/menu/types.ts
  - lib/storage/images.ts
  - app/r/[slug]/admin/(protected)/menu/page.tsx
  - app/r/[slug]/admin/(protected)/menu/actions.ts
  - app/r/[slug]/admin/(protected)/menu/CategoryManager.tsx
  - app/r/[slug]/admin/(protected)/menu/ItemDialog.tsx
  - components/menu/ImageUpload.tsx
  - components/menu/AvailabilityToggle.tsx
  - next.config.ts
autonomous: false
requirements: [MENU-01, MENU-02]
user_setup:
  - "Không cần thao tác hạ tầng thủ công — bucket Storage tạo bằng migration 0005 (chạy qua supabase db push)."
must_haves:
  truths:
    - "Owner/manager tạo/sửa/xóa danh mục và món (tên, mô tả, giá VND, ảnh ≤2MB) ở /r/[slug]/admin/menu; sắp xếp được"
    - "Bật/tắt 'hết món' (is_available) trên từng món → đổi ngay, lưu DB"
    - "Ảnh upload lên Supabase Storage bucket menu-images, hiển thị lại qua next/image; file >2MB hoặc sai loại bị từ chối (client + server)"
  artifacts:
    - "0004_menu_core.sql tạo menu_categories, menu_items + RLS tenant"
    - "0005_storage_menu_images.sql tạo bucket public menu-images + policy ghi qua server"
    - "menu/actions.ts server action CRUD dùng phiên RLS; upload ảnh validate 2 lớp"
  key_links:
    - "menu/page.tsx guard owner/manager qua getSessionMembership + canManage('menu')"
    - "ImageUpload.tsx validate size/type trước khi gửi; server re-validate trước khi ghi Storage"
---

<objective>
Cho owner/manager dựng thực đơn: danh mục + món (ảnh, giá, mô tả, thứ tự) và nút "hết món". Lát cắt dọc "vào /admin/menu → thêm danh mục → thêm món có ảnh → bật hết món" — bấm được đầu-cuối trong khu admin.

Purpose: Bằng chứng MENU-01 + MENU-02; đặt nền dữ liệu menu cho modifier (02-02), onboarding (02-05) và gọi món (P3).
Output: Migration menu core + Storage, trang quản lý menu, upload ảnh, toggle hết món.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/10-BanThietKe/03-LuongFrontend.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P2/00-TongQuan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration menu core</name>
  <files>supabase/migrations/0004_menu_core.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Tạo theo §3.2: menu_categories(id uuid pk, tenant_id fk tenants, name, sort_order int default 0, active bool default true, created_at, updated_at); menu_items(id, tenant_id, category_id fk menu_categories on delete cascade, name, description, base_price int not null check >=0 (VND), image_url text null, is_available bool default true, sort_order int default 0, active bool default true, created_at, updated_at). Index menu_categories(tenant_id, sort_order), menu_items(tenant_id, category_id, sort_order). RLS: bật + policy đọc/ghi theo auth_tenant_ids() (mẫu như 0002). super_admin không cần ở đây.</action>
  <verify>supabase db push áp không lỗi; RLS bật trên cả 2 bảng</verify>
  <acceptance_criteria>
    - File chứa "create table" cho menu_categories và menu_items
    - menu_items có is_available bool và base_price check >= 0
    - Có "enable row level security" + policy tham chiếu auth_tenant_ids() cho cả 2 bảng
  </acceptance_criteria>
  <done>Schema menu core + RLS trên Supabase dev</done>
</task>

<task type="auto">
  <name>Task 2: Migration Storage bucket menu-images</name>
  <files>supabase/migrations/0005_storage_menu_images.sql</files>
  <read_first>docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md, docs/30-KeHoach/P2/00-TongQuan.md</read_first>
  <action>insert into storage.buckets (id, name, public) values ('menu-images','menu-images', true) on conflict do nothing. Policy storage.objects cho bucket menu-images: SELECT cho public (ảnh menu công khai); INSERT/UPDATE/DELETE chỉ role service_role (ghi/xóa đi qua server action dùng service role — D15/QD-005 §4). KHÔNG cho anon/authenticated ghi trực tiếp. Ghi chú: logo tenant (02-04) dùng chung bucket này (path {tenant_id}/logo-*).</action>
  <verify>Bucket tồn tại; đọc public URL của 1 object test được</verify>
  <acceptance_criteria>
    - File tạo bucket 'menu-images' public = true
    - Có policy SELECT public và policy ghi giới hạn service_role
  </acceptance_criteria>
  <done>Storage sẵn cho upload ảnh</done>
</task>

<task type="auto">
  <name>Task 3: RBAC quản lý + tiện ích Storage/ảnh</name>
  <files>lib/auth/rbac.ts, lib/storage/images.ts, lib/menu/types.ts, next.config.ts</files>
  <read_first>lib/auth/rbac.ts, lib/supabase/admin.ts</read_first>
  <action>rbac.ts: thêm canManage(role, section) — owner/manager quản lý 'menu'|'tables'|'settings'|'onboarding'; nhân viên khác false (tái dùng ở guard các plan P2). lib/storage/images.ts (server): validateImage(file) (≤2MB, mime image/png|jpeg|webp), uploadMenuImage(tenantId, itemId, file) dùng admin client ghi vào menu-images trả public URL; deleteMenuImage(path). lib/menu/types.ts: type Category, Item (khớp cột DB). next.config.ts: thêm images.remotePatterns cho host Supabase Storage ({project}.supabase.co /storage/v1/object/public/**) để next/image hiển thị.</action>
  <verify>Import không lỗi build; canManage trả đúng</verify>
  <acceptance_criteria>
    - rbac.ts export canManage phủ owner/manager cho 'menu'
    - lib/storage/images.ts validate ≤2MB + mime; upload trả public URL
    - next.config.ts có remotePatterns cho Supabase Storage
  </acceptance_criteria>
  <done>Công cụ RBAC + ảnh sẵn</done>
</task>

<task type="auto">
  <name>Task 4: Trang quản lý menu + CRUD danh mục</name>
  <files>app/r/[slug]/admin/(protected)/menu/page.tsx, menu/actions.ts, menu/CategoryManager.tsx</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md (§F), docs/30-KeHoach/P2/00-TongQuan.md</read_first>
  <action>menu/page.tsx (server): guard canManage('menu') else redirect route mặc định; đọc danh mục + món của tenant qua phiên RLS, gom món theo danh mục. actions.ts (server, "use server"): createCategory/renameCategory/deleteCategory/reorderCategory — dùng client RLS (createClient server), scope eq tenant_id = session.tenant.id, revalidatePath. CategoryManager.tsx (client): danh sách danh mục, thêm/sửa tên inline, nút lên/xuống đổi sort_order. Kích hoạt mục "Thực đơn" trong AdminNav (đổi từ placeholder "chờ" sang link thật).</action>
  <verify>Owner Phở Việt tạo danh mục "Món nước" → hiện trong danh sách</verify>
  <acceptance_criteria>
    - menu/page.tsx guard bằng canManage('menu')
    - actions.ts CRUD danh mục scope theo tenant_id (RLS), có revalidatePath
    - AdminNav: mục "Thực đơn" là link hoạt động (không còn "chờ")
  </acceptance_criteria>
  <done>CRUD danh mục hoạt động</done>
</task>

<task type="auto">
  <name>Task 5: CRUD món + upload ảnh + toggle hết món</name>
  <files>app/r/[slug]/admin/(protected)/menu/ItemDialog.tsx, menu/actions.ts, components/menu/ImageUpload.tsx, components/menu/AvailabilityToggle.tsx</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md, docs/30-KeHoach/P2/00-TongQuan.md</read_first>
  <action>ItemDialog.tsx (client): form thêm/sửa món (tên, mô tả, giá VND số nguyên, danh mục, ảnh). actions.ts: createItem/updateItem/deleteItem/reorderItem + setItemAvailable(id, bool) + uploadItemImage (nhận FormData file → validateImage → uploadMenuImage → cập nhật image_url; xóa ảnh cũ nếu thay). ImageUpload.tsx (client): input file, validate ≤2MB + loại ảnh TRƯỚC khi gửi (báo lỗi tại chỗ), preview; gửi qua server action (server re-validate). AvailabilityToggle.tsx (client): switch gọi setItemAvailable, phản hồi optimistic + trạng thái. Hiển thị ảnh bằng next/image. Món hết = thẻ mờ + nhãn "Hết".</action>
  <verify>Thêm món "Phở bò" giá 45000 + ảnh <2MB → hiện thẻ có ảnh; bật "hết" → nhãn Hết; thử ảnh 3MB → bị chặn</verify>
  <acceptance_criteria>
    - actions.ts gọi validateImage phía server trước khi ghi Storage (chặn >2MB/sai loại)
    - setItemAvailable đổi is_available và lưu DB
    - ImageUpload validate size/type ở client trước khi upload
    - giá lưu integer VND (không phần lẻ)
  </acceptance_criteria>
  <done>CRUD món + ảnh + hết món hoạt động (MENU-01, MENU-02)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Trang quản lý thực đơn ở /r/[slug]/admin/menu. Dev localhost:3000, đăng nhập owner demo.</what-built>
  <how-to-verify>
    1. Đăng nhập owner Phở Việt (ownerA@pho-viet.test / DemoPass123!) → mở /r/pho-viet/admin/menu.
    2. Tạo danh mục "Món nước" và "Món khô"; đổi thứ tự (lên/xuống) → thứ tự lưu.
    3. Thêm món "Phở bò" (giá 45000, mô tả, ảnh <2MB) → thẻ hiện ảnh + giá đúng. Sửa giá → lưu.
    4. Thử upload ảnh >2MB hoặc file .pdf → bị từ chối, báo lỗi rõ.
    5. Bật "hết món" trên "Phở bò" → thẻ mờ + nhãn "Hết"; tắt lại → bình thường.
    6. Đăng nhập owner Bún Bò → /r/bun-bo/admin/menu KHÔNG thấy món của Phở Việt (RLS).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (CRUD, ảnh, hết món, hoặc lẫn dữ liệu tenant).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0004 + 0005 áp Supabase dev không lỗi
- [ ] CRUD danh mục + món hoạt động, scope theo tenant (RLS)
- [ ] Upload ảnh ≤2MB OK; >2MB/sai loại bị chặn ở client + server
- [ ] Toggle hết món đổi is_available, lưu DB
- [ ] Owner tenant A không thấy menu tenant B
</verification>

<success_criteria>
- Checkpoint "approved"
- MENU-01 (CRUD danh mục/món + ảnh + sắp xếp) + MENU-02 (hết món) chứng minh được ở admin
- Nền menu sẵn cho 02-02 (modifier) và 02-05 (onboarding)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P2/02-01-SUMMARY.md (file đã đổi, trạng thái từng cam kết, bằng chứng).
</output>
