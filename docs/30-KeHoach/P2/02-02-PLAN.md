---
phase: 02-du-lieu-nha-hang
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - supabase/migrations/0006_modifiers.sql
  - lib/menu/types.ts
  - app/r/[slug]/admin/(protected)/menu/modifiers/page.tsx
  - app/r/[slug]/admin/(protected)/menu/modifiers/actions.ts
  - app/r/[slug]/admin/(protected)/menu/modifiers/GroupEditor.tsx
  - components/menu/ModifierGroupPicker.tsx
  - app/r/[slug]/admin/(protected)/menu/actions.ts
autonomous: false
requirements: [MENU-03]
user_setup: []
must_haves:
  truths:
    - "Owner/manager tạo nhóm tùy chọn (name, min_select, max_select, required) + các option có phụ thu (price_delta) ở /admin/menu/modifiers"
    - "Gắn 1+ nhóm tùy chọn vào món (N-N); gỡ gắn được"
    - "Option có thể bật/tắt 'hết' (is_available) riêng"
  artifacts:
    - "0006_modifiers.sql tạo modifier_groups, modifier_options, menu_item_modifier_groups + RLS"
    - "GroupEditor.tsx CRUD nhóm + option với ràng buộc min/max/required"
    - "ModifierGroupPicker.tsx gắn/gỡ nhóm vào món (dùng trong ItemDialog)"
  key_links:
    - "menu_item_modifier_groups(item_id, group_id) link N-N; xóa gắn kết khi xóa món/nhóm (cascade)"
    - "actions.ts validate max_select >= min_select và required ⇒ min_select >= 1"
---

<objective>
Cho món có tùy chọn (size, topping, mức đường/đá…) với phụ thu, theo D4. Lát cắt dọc "tạo nhóm tùy chọn + option → gắn vào món" — bấm được ở admin. Ràng buộc min/max/required chuẩn bị cho validate giỏ hàng ở P3.

Purpose: Bằng chứng MENU-03; hoàn thiện mô hình menu để gọi món (P3) tính đúng phụ thu.
Output: Migration modifiers, trang quản lý nhóm/option, gắn N-N vào món.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P2/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration modifiers</name>
  <files>supabase/migrations/0006_modifiers.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Theo §3.2: modifier_groups(id, tenant_id, name, min_select int default 0, max_select int default 1, required bool default false, sort_order int default 0, created_at, updated_at) check max_select >= min_select; modifier_options(id, tenant_id, group_id fk modifier_groups on delete cascade, name, price_delta int default 0, is_available bool default true, sort_order int default 0, created_at); menu_item_modifier_groups(item_id fk menu_items on delete cascade, group_id fk modifier_groups on delete cascade, sort_order int default 0, primary key(item_id, group_id)). RLS bật cả 3 + policy auth_tenant_ids(). Với bảng link (không có tenant_id trực tiếp): policy dựa trên EXISTS món cùng tenant, HOẶC thêm tenant_id vào bảng link cho đơn giản RLS — chọn thêm tenant_id vào menu_item_modifier_groups để policy đồng nhất. Index modifier_options(group_id, sort_order), link(group_id).</action>
  <verify>db push áp không lỗi; RLS bật cả 3 bảng</verify>
  <acceptance_criteria>
    - File tạo modifier_groups, modifier_options, menu_item_modifier_groups
    - modifier_groups có check max_select >= min_select
    - Cả 3 bảng enable RLS + policy theo auth_tenant_ids()
  </acceptance_criteria>
  <done>Schema modifiers + RLS</done>
</task>

<task type="auto">
  <name>Task 2: CRUD nhóm tùy chọn + option</name>
  <files>app/r/[slug]/admin/(protected)/menu/modifiers/page.tsx, modifiers/actions.ts, modifiers/GroupEditor.tsx, lib/menu/types.ts</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md, docs/30-KeHoach/P2/02-01-PLAN.md</read_first>
  <action>modifiers/page.tsx (server): guard canManage('menu'); liệt kê nhóm + option của tenant (RLS). actions.ts: createGroup/updateGroup/deleteGroup (validate max>=min, required⇒min>=1); addOption/updateOption/deleteOption/setOptionAvailable (price_delta integer, có thể âm để giảm giá tùy chọn? V1 giữ >=0 hoặc cho phép âm — mặc định >=0). GroupEditor.tsx (client): panel mỗi nhóm với min/max/required + danh sách option (tên + phụ thu + toggle hết). types.ts: thêm ModifierGroup, ModifierOption.</action>
  <verify>Tạo nhóm "Size" (required, min1 max1) + option "S(+0)", "L(+10000)" → hiện đúng</verify>
  <acceptance_criteria>
    - actions.ts chặn max_select < min_select và required với min_select = 0
    - CRUD option lưu price_delta integer
    - Trang guard canManage('menu')
  </acceptance_criteria>
  <done>Quản lý nhóm/option hoạt động</done>
</task>

<task type="auto">
  <name>Task 3: Gắn nhóm tùy chọn vào món (N-N)</name>
  <files>components/menu/ModifierGroupPicker.tsx, app/r/[slug]/admin/(protected)/menu/actions.ts, app/r/[slug]/admin/(protected)/menu/ItemDialog.tsx</files>
  <read_first>docs/30-KeHoach/P2/02-01-PLAN.md</read_first>
  <action>ModifierGroupPicker.tsx (client): trong ItemDialog, chọn nhiều nhóm tùy chọn để gắn vào món (checkbox danh sách nhóm tenant). menu/actions.ts: attachGroup(itemId, groupId)/detachGroup(itemId, groupId) ghi menu_item_modifier_groups (kèm tenant_id). Khi sửa món, hiển thị nhóm đã gắn + cho gắn/gỡ. Cascade: xóa món hoặc nhóm tự gỡ link (FK on delete cascade).</action>
  <verify>Mở "Phở bò" → gắn nhóm "Size" → lưu → mở lại thấy "Size" đã gắn; gỡ được</verify>
  <acceptance_criteria>
    - attach/detach ghi/xóa đúng dòng menu_item_modifier_groups (scope tenant)
    - ItemDialog hiển thị nhóm đã gắn của món
  </acceptance_criteria>
  <done>Gắn tùy chọn vào món (MENU-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quản lý nhóm tùy chọn + gắn vào món. Dev localhost:3000, owner demo.</what-built>
  <how-to-verify>
    1. Owner Phở Việt → /r/pho-viet/admin/menu/modifiers → tạo nhóm "Size" (bắt buộc, chọn 1) với option "Nhỏ (+0)", "Lớn (+10000)"; tạo nhóm "Topping" (không bắt buộc, chọn tối đa 3).
    2. Thử tạo nhóm có max < min → bị chặn.
    3. Mở món "Phở bò" → gắn "Size" + "Topping" → lưu → mở lại thấy 2 nhóm đã gắn. Gỡ "Topping" → lưu → còn "Size".
    4. Tắt option "Lớn" (hết) → trạng thái đổi.
    5. Owner Bún Bò không thấy nhóm tùy chọn của Phở Việt (RLS).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (ràng buộc min/max, gắn N-N, hoặc RLS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0006 áp không lỗi; RLS bật cả 3 bảng
- [ ] CRUD nhóm/option với ràng buộc min/max/required
- [ ] Gắn/gỡ nhóm vào món (N-N) hoạt động
- [ ] Option toggle hết được
- [ ] Cách ly tenant giữ nguyên
</verification>

<success_criteria>
- Checkpoint "approved"
- MENU-03 (modifier group/option + phụ thu, gắn vào món) chứng minh được
- Mô hình menu đủ để P3 tính phụ thu khi gọi món
</success_criteria>

<output>
Tạo docs/30-KeHoach/P2/02-02-SUMMARY.md.
</output>
