---
phase: 02-du-lieu-nha-hang
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - supabase/migrations/0007_areas_tables.sql
  - package.json
  - lib/tables/qr.ts
  - lib/tables/types.ts
  - app/r/[slug]/admin/(protected)/tables/page.tsx
  - app/r/[slug]/admin/(protected)/tables/actions.ts
  - app/r/[slug]/admin/(protected)/tables/AreaTableManager.tsx
  - app/r/[slug]/print/qr/page.tsx
  - components/tables/QrDownload.tsx
  - components/tables/PrintButton.tsx
autonomous: false
requirements: [TABLE-01]
user_setup: []
must_haves:
  truths:
    - "Owner/manager tạo khu vực và bàn (tên, số ghế) ở /r/[slug]/admin/tables; mỗi bàn có qr_token unique tự sinh"
    - "Xuất QR: mở /r/[slug]/print/qr in gộp lưới A4 (window.print) cho mọi bàn; và tải PNG/SVG từng bàn"
    - "QR mã hoá URL tuyệt đối /r/[slug]/menu?t=[qr_token] (host lấy từ request)"
  artifacts:
    - "0007_areas_tables.sql tạo areas, tables (qr_token unique) + RLS"
    - "lib/tables/qr.ts sinh QR SVG/PNG + dựng URL tuyệt đối từ host"
    - "app/r/[slug]/print/qr/page.tsx trang in gộp; components/tables/QrDownload.tsx tải file"
  key_links:
    - "tables.qr_token default ngẫu nhiên không đoán được (encode(gen_random_bytes(9),'hex'))"
    - "print/qr guard membership tenant; dựng absolute URL bằng header host"
---

<objective>
Cho owner/manager khai báo sơ đồ bàn (khu vực + bàn) và xuất mã QR để dán lên bàn. Lát cắt dọc "tạo khu vực → tạo bàn → xuất QR (in gộp + tải)" — bấm được ở admin. QR trỏ tới menu theo bàn (chuẩn bị luồng gọi món P3).

Purpose: Bằng chứng TABLE-01; nền cho phiên bàn + gọi món QR (P3, TABLE-02/ORDER-01).
Output: Migration areas/tables, quản lý khu vực/bàn, trang in QR gộp, tải PNG/SVG.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/10-BanThietKe/03-LuongFrontend.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P2/00-TongQuan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration areas + tables</name>
  <files>supabase/migrations/0007_areas_tables.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Theo §3.3: areas(id, tenant_id, name, sort_order int default 0, created_at, updated_at); tables(id, tenant_id, area_id fk areas on delete set null null, name, seats int default 2 check >=1, qr_token text not null unique default encode(gen_random_bytes(9),'hex'), status text default 'available' check in ('available','occupied','reserved','cleaning'), sort_order int default 0, created_at, updated_at). Index tables(tenant_id, area_id, sort_order), unique tables(qr_token). RLS bật + policy auth_tenant_ids() cả 2 bảng. (table_sessions KHÔNG tạo ở đây — P3.)</action>
  <verify>db push áp không lỗi; qr_token tự sinh khi insert không truyền</verify>
  <acceptance_criteria>
    - File tạo areas, tables; tables.qr_token unique + default ngẫu nhiên
    - tables.status check gồm 4 giá trị; seats check >= 1
    - RLS bật + policy auth_tenant_ids() cả 2 bảng
  </acceptance_criteria>
  <done>Schema khu vực/bàn + RLS</done>
</task>

<task type="auto">
  <name>Task 2: Thư viện QR + tiện ích URL</name>
  <files>package.json, lib/tables/qr.ts, lib/tables/types.ts</files>
  <read_first>docs/30-KeHoach/P2/00-TongQuan.md</read_first>
  <action>Cài `qrcode` (+ @types/qrcode dev). lib/tables/qr.ts: menuUrlForToken(host, slug, token) → `https://${host}/r/${slug}/menu?t=${token}` (dùng https; local có thể http — lấy protocol từ header x-forwarded-proto nếu có, mặc định https). qrSvg(text) → chuỗi SVG (qrcode.toString type 'svg'); qrPngDataUrl(text) → data URL PNG (qrcode.toDataURL). lib/tables/types.ts: Area, Table.</action>
  <verify>qrSvg trả chuỗi <svg>; URL dựng đúng dạng tuyệt đối</verify>
  <acceptance_criteria>
    - package.json có dependency qrcode
    - lib/tables/qr.ts export qrSvg + menuUrlForToken dựng URL tuyệt đối
  </acceptance_criteria>
  <done>Sinh QR sẵn</done>
</task>

<task type="auto">
  <name>Task 3: Quản lý khu vực + bàn</name>
  <files>app/r/[slug]/admin/(protected)/tables/page.tsx, tables/actions.ts, tables/AreaTableManager.tsx</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md, docs/30-KeHoach/P2/02-01-PLAN.md</read_first>
  <action>tables/page.tsx (server): guard canManage('tables'); đọc areas + tables tenant (RLS), gom bàn theo khu vực. actions.ts: createArea/renameArea/deleteArea/reorderArea; createTable(area, name, seats)/updateTable/deleteTable/reorderTable — scope tenant_id, revalidate. AreaTableManager.tsx (client): cột khu vực + lưới bàn (hiện tên + số ghế + mã token rút gọn). Kích hoạt mục "Bàn & QR" trong AdminNav (bỏ "chờ").</action>
  <verify>Tạo khu vực "Tầng 1" + bàn "B1" (4 ghế) → hiện; qr_token có giá trị</verify>
  <acceptance_criteria>
    - Trang guard canManage('tables')
    - CRUD khu vực/bàn scope tenant (RLS)
    - AdminNav: "Bàn & QR" là link hoạt động
  </acceptance_criteria>
  <done>CRUD khu vực/bàn hoạt động</done>
</task>

<task type="auto">
  <name>Task 4: Xuất QR — trang in gộp + tải PNG/SVG</name>
  <files>app/r/[slug]/print/qr/page.tsx, components/tables/PrintButton.tsx, components/tables/QrDownload.tsx</files>
  <read_first>docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md (D1), docs/30-KeHoach/P2/00-TongQuan.md</read_first>
  <action>print/qr/page.tsx (server, ngoài route group (protected) nhưng vẫn dưới /r/[slug]/): guard getSessionMembership(slug) (owner/manager/station) else redirect; đọc host từ headers(); render lưới A4 các thẻ QR (mỗi bàn: tên khu vực+bàn + QR SVG qrSvg(menuUrl)). CSS @media print khổ A4, ẩn chrome. PrintButton.tsx (client): nút gọi window.print(). QrDownload.tsx (client): mỗi bàn có nút "Tải PNG"/"Tải SVG" — tạo Blob từ data URL/SVG và download (đặt tên theo tên bàn). Thêm nút "Xuất QR" ở trang /admin/tables mở /print/qr (target _blank).</action>
  <verify>Mở /r/pho-viet/print/qr → thấy lưới QR mọi bàn; window.print ra khổ A4; tải PNG 1 bàn được</verify>
  <acceptance_criteria>
    - /print/qr render QR (SVG) cho từng bàn, có CSS @media print A4
    - PrintButton gọi window.print; QrDownload tải được PNG và SVG từng bàn
    - QR mã hoá URL tuyệt đối /r/[slug]/menu?t=[qr_token]
  </acceptance_criteria>
  <done>Xuất QR in được + tải được (TABLE-01)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quản lý khu vực/bàn + xuất QR. Dev localhost:3000, owner demo.</what-built>
  <how-to-verify>
    1. Owner Phở Việt → /r/pho-viet/admin/tables → tạo khu vực "Tầng 1", "Sân vườn"; tạo 5 bàn (B1..B5) với số ghế.
    2. Bấm "Xuất QR" → mở /r/pho-viet/print/qr → thấy lưới QR đủ 5 bàn (tên khu vực + bàn dưới mỗi QR).
    3. Bấm "In" → xem preview khổ A4 gọn, không tràn; (tùy chọn) In ra PDF.
    4. Tải PNG và SVG của 1 bàn → mở file thấy QR; quét QR bằng điện thoại → ra URL /r/pho-viet/menu?t=... (P2 trang menu khách chưa có nên có thể 404/placeholder — chỉ cần URL đúng).
    5. Owner Bún Bò không thấy bàn của Phở Việt (RLS).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (CRUD bàn, QR in/tải, URL sai, hoặc RLS).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0007 áp không lỗi; qr_token tự sinh unique
- [ ] CRUD khu vực/bàn scope tenant (RLS)
- [ ] /print/qr in gộp A4 + tải PNG/SVG từng bàn
- [ ] QR mã hoá URL tuyệt đối đúng slug + token
- [ ] Cách ly tenant giữ nguyên
</verification>

<success_criteria>
- Checkpoint "approved"
- TABLE-01 (khu vực + bàn + xuất QR in được) chứng minh được
- qr_token + URL sẵn cho luồng gọi món QR (P3)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P2/02-03-SUMMARY.md.
</output>
