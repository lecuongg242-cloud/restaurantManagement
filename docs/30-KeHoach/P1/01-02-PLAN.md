---
phase: 01-nen-tang
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - supabase/migrations/0001_core_tenant.sql
  - supabase/migrations/0002_rls_baseline.sql
  - lib/supabase/admin.ts
  - lib/auth/session.ts
  - app/super/page.tsx
  - app/super/new/page.tsx
  - app/super/actions.ts
  - app/r/[slug]/admin/login/page.tsx
  - app/r/[slug]/admin/page.tsx
  - app/r/[slug]/admin/layout.tsx
  - components/admin/AdminShell.tsx
autonomous: false
requirements: [TENANT-01, TENANT-04, AUTH-01]
user_setup:
  - "Đảm bảo có SUPABASE_SERVICE_ROLE_KEY trong env server (Vercel dev + .env.local)"
  - "Tạo 1 tài khoản super-admin đầu tiên (seed) — email của chủ SaaS"
must_haves:
  truths:
    - "Tại /super, super-admin tạo được nhà hàng (tên + slug) và tài khoản owner (email + mật khẩu tạm)"
    - "Owner đăng nhập tại /r/[slug]/admin/login → vào /r/[slug]/admin thấy tên + logo tenant của mình"
    - "Owner tenant A truy cập /r/[slug-B]/admin bị chặn (không phải tenant của họ)"
  artifacts:
    - "supabase/migrations/0001_core_tenant.sql tạo tenants, profiles, memberships, super_admins"
    - "supabase/migrations/0002_rls_baseline.sql bật RLS + hàm auth_tenant_ids()"
    - "app/super/new/page.tsx form tạo tenant gọi server action dùng service role"
  key_links:
    - "app/r/[slug]/admin/layout.tsx kiểm tra membership của user với tenant theo slug (RLS + guard)"
    - "lib/auth/session.ts đọc user Supabase + membership role"
---

<objective>
Cho super-admin tạo nhà hàng + owner, và cho owner đăng nhập vào đúng khu quản trị tenant của mình. Đây là lát cắt dọc "tạo tenant → đăng nhập → thấy admin shell" — bấm được đầu-cuối.

Purpose: Bằng chứng TENANT-01 + AUTH-01; đặt schema lõi + RLS nền cho mọi plan sau.
Output: Migration schema lõi + RLS, trang /super, đăng nhập owner, admin shell hiển thị tenant.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P1/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration schema lõi</name>
  <files>supabase/migrations/0001_core_tenant.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Tạo bảng theo §3.1: tenants(id uuid pk, slug text unique, name, subdomain text null unique, logo_url null, settings jsonb default '{}', status text default 'active', created_at); profiles(id uuid pk ref auth.users, full_name, phone, created_at); memberships(id, tenant_id ref tenants, user_id uuid NULL ref auth.users, role text check in (owner,manager,cashier,waiter,kitchen,station), display_name, pin_hash text null, active bool default true, created_at) — user_id NULL cho nhân viên PIN-only (cashier/waiter/kitchen); owner/manager/station có user_id; super_admins(user_id uuid pk ref auth.users). Index memberships(tenant_id), memberships(user_id), tenants(slug).</action>
  <verify>supabase db push (hoặc migration apply) không lỗi; \d tenants đúng cột</verify>
  <acceptance_criteria>
    - File chứa "create table" cho tenants, profiles, memberships, super_admins
    - memberships có cột pin_hash, user_id NULLABLE, và check role gồm 'station'
    - tenants.slug có unique
  </acceptance_criteria>
  <done>Schema lõi tồn tại trên Supabase dev</done>
</task>

<task type="auto">
  <name>Task 2: RLS nền + hàm auth_tenant_ids()</name>
  <files>supabase/migrations/0002_rls_baseline.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Theo §4: tạo function auth_tenant_ids() returns setof uuid, language sql stable security definer, trả tenant_id từ memberships where user_id = auth.uid() and active. Bật RLS trên memberships, tenants. Policy: memberships select/all khi tenant_id in (select auth_tenant_ids()). tenants: owner/thành viên đọc tenant của mình; super_admins toàn quyền (policy dựa trên exists trong super_admins). profiles: user đọc/sửa profile của chính mình.</action>
  <verify>Áp migration không lỗi; test nhanh bằng SQL với 2 user</verify>
  <acceptance_criteria>
    - File chứa "create function auth_tenant_ids"
    - File chứa "enable row level security" cho tenants và memberships
    - Có policy tham chiếu super_admins cho tenants
  </acceptance_criteria>
  <done>RLS nền sẵn (test đầy đủ ở plan 01-04)</done>
</task>

<task type="auto">
  <name>Task 3: Super-admin tạo tenant + owner</name>
  <files>app/super/page.tsx, app/super/new/page.tsx, app/super/actions.ts, lib/supabase/admin.ts</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md, docs/30-KeHoach/P1/01-01-PLAN.md</read_first>
  <action>lib/supabase/admin.ts: client service role (chỉ server). app/super/page.tsx: liệt kê tenants (chỉ super-admin xem được — guard bằng bảng super_admins). app/super/new/page.tsx: form tên + slug + email owner + mật khẩu tạm. app/super/actions.ts (server action): tạo auth user owner (admin API), tạo tenants, tạo memberships(role=owner). Slug tự sinh từ tên nếu trống (dùng gsd generate-slug-style: hạ chữ, gạch nối). Chặn trang nếu user không nằm trong super_admins → redirect.</action>
  <verify>Đăng nhập super-admin, tạo "Phở Việt" → xuất hiện trong danh sách</verify>
  <acceptance_criteria>
    - app/super/actions.ts tạo user qua admin API + insert memberships role 'owner'
    - Trang /super redirect nếu user không thuộc super_admins
    - Form có trường slug + email owner
  </acceptance_criteria>
  <done>Tạo tenant thủ công hoạt động (D13)</done>
</task>

<task type="auto">
  <name>Task 4: Owner đăng nhập + Admin shell hiển thị tenant</name>
  <files>app/r/[slug]/admin/login/page.tsx, app/r/[slug]/admin/layout.tsx, app/r/[slug]/admin/page.tsx, components/admin/AdminShell.tsx, lib/auth/session.ts</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md, docs/10-BanThietKe/03-LuongFrontend.md</read_first>
  <action>login/page.tsx: form email+mật khẩu (Supabase Auth signInWithPassword). admin/layout.tsx: server-guard — lấy user, tra membership với tenant theo slug; nếu user không có membership ở tenant này → redirect về login/403. AdminShell.tsx: sidebar (Dashboard/Menu/Bàn&QR/Nhân viên/Đặt bàn/Đơn online/Cài đặt — chỉ Dashboard hoạt động ở P1, còn lại là mục chờ) + header hiện logo_url + name tenant (OPS-06). admin/page.tsx: dashboard rỗng "Chào [tên nhà hàng]". lib/auth/session.ts: helper getSessionMembership(slug).</action>
  <verify>Đăng nhập owner Phở Việt → /r/pho-viet/admin thấy tên+logo Phở Việt</verify>
  <acceptance_criteria>
    - admin/layout.tsx gọi getSessionMembership và redirect khi không có quyền
    - AdminShell hiển thị tenant.name và tenant.logo_url
    - login dùng signInWithPassword
  </acceptance_criteria>
  <done>Owner vào đúng tenant (AUTH-01, TENANT-04)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Trang super-admin + đăng nhập owner + admin shell. Dev server localhost:3000.</what-built>
  <how-to-verify>
    1. Đăng nhập super-admin, mở /super/new → tạo nhà hàng "Phở Việt" (slug pho-viet), owner email test + mật khẩu.
    2. Đăng xuất, mở /r/pho-viet/admin/login → đăng nhập bằng email owner vừa tạo → phải vào /r/pho-viet/admin thấy "Phở Việt" ở header.
    3. Tạo thêm tenant thứ 2 "Bún Bò" (slug bun-bo) với owner khác. Đăng nhập owner Phở Việt rồi thử mở /r/bun-bo/admin → phải bị chặn (redirect/403), KHÔNG vào được.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (tạo tenant, đăng nhập, hoặc chặn chéo tenant sai).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration áp thành công trên Supabase dev
- [ ] Tạo được tenant + owner ở /super
- [ ] Owner đăng nhập vào đúng /r/[slug]/admin, thấy tên+logo
- [ ] Owner tenant A KHÔNG mở được admin tenant B
- [ ] Service role key không lộ ra client
</verification>

<success_criteria>
- Checkpoint "approved"
- TENANT-01 (super-admin tạo tenant), AUTH-01 (owner email), TENANT-04 (slug→tenant) chứng minh được
- RLS nền đặt xong (test đầy đủ ở 01-04)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P1/01-02-SUMMARY.md.
</output>
