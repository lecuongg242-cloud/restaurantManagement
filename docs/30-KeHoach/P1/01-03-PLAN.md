---
phase: 01-nen-tang
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - supabase/migrations/0003_station_pin.sql
  - lib/auth/pin.ts
  - lib/auth/rbac.ts
  - app/r/[slug]/admin/staff/page.tsx
  - app/r/[slug]/admin/staff/actions.ts
  - app/r/[slug]/pos/login/page.tsx
  - app/r/[slug]/pos/page.tsx
  - app/r/[slug]/kds/login/page.tsx
  - app/r/[slug]/kds/page.tsx
  - components/staff/PinPad.tsx
  - components/staff/StaffPicker.tsx
  - middleware.ts
autonomous: false
requirements: [AUTH-02, AUTH-03, AUTH-04, OPS-06]
user_setup: []
must_haves:
  truths:
    - "Owner/manager tạo được nhân viên (cashier/waiter/kitchen) kèm PIN 4 số ở /admin/staff"
    - "Thiết bị đăng nhập tài khoản trạm 1 lần tại /r/[slug]/pos/login; sau đó chọn tên nhân viên + gõ PIN → vào shell POS, thao tác gắn staff_id"
    - "Điều hướng theo vai trò: owner/manager→admin, cashier/waiter→pos, kitchen→kds; vai trò sai bị chặn"
  artifacts:
    - "supabase/migrations/0003_station_pin.sql thêm role station + cột pin_hash sử dụng"
    - "lib/auth/pin.ts băm/so khớp PIN bằng bcrypt phía server"
    - "components/staff/PinPad.tsx bàn phím số 4 ô, target ≥44px"
  key_links:
    - "lib/auth/rbac.ts map role→route mặc định, dùng ở middleware + login redirect"
    - "PIN xác thực ở server action, KHÔNG so khớp phía client"
---

<objective>
Cho thiết bị POS/KDS đăng nhập theo nhà hàng một lần rồi nhân viên thao tác nhanh bằng PIN, và định tuyến người dùng theo vai trò. Lát cắt dọc "tạo nhân viên → đăng nhập trạm → chọn tên + PIN → vào POS/KDS".

Purpose: Bằng chứng AUTH-02/03/04; hiện thực D7 (PIN = phân định người + gate, không phải ranh giới tenant).
Output: role station + PIN, pin-pad, staff picker, RBAC redirect, trang staff CRUD tối thiểu.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P1/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration PIN + tài khoản trạm</name>
  <files>supabase/migrations/0003_station_pin.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Đảm bảo role 'station' đã có trong check + user_id NULLABLE (từ 0001). Thêm index memberships(tenant_id, role). Chốt mô hình (QD-005 "Làm rõ D7"): membership role='station' CÓ user_id (tài khoản đăng nhập dùng chung thiết bị — cửa RLS); membership cashier/waiter/kitchen có user_id=NULL + pin_hash (nhân viên PIN-only, không có tài khoản Supabase riêng). Mặc định 1 tài khoản trạm/nhà hàng (dùng cả POS/KDS); có thể tạo thêm trạm bếp nếu muốn tách thiết bị.</action>
  <verify>Migration áp không lỗi</verify>
  <acceptance_criteria>
    - File tồn tại và áp được; có index memberships(tenant_id, role)
  </acceptance_criteria>
  <done>Sẵn cột PIN + role station</done>
</task>

<task type="auto">
  <name>Task 2: Tiện ích PIN (bcrypt) + RBAC</name>
  <files>lib/auth/pin.ts, lib/auth/rbac.ts</files>
  <read_first>docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md</read_first>
  <action>lib/auth/pin.ts: hashPin(pin) + verifyPin(pin, hash) dùng bcryptjs (chỉ chạy server). lib/auth/rbac.ts: defaultRouteForRole(role) → owner/manager: `/r/[slug]/admin`, cashier/waiter: `/r/[slug]/pos`, kitchen: `/r/[slug]/kds`, station: theo thiết bị. canAccess(role, section) chặn chéo (kitchen không vào admin).</action>
  <verify>Unit nhỏ: verifyPin đúng/sai; defaultRouteForRole trả đúng</verify>
  <acceptance_criteria>
    - lib/auth/pin.ts import bcryptjs, không dùng ở client component
    - lib/auth/rbac.ts map đủ 5 vai trò
  </acceptance_criteria>
  <done>Có công cụ PIN + RBAC</done>
</task>

<task type="auto">
  <name>Task 3: Admin tạo nhân viên + PIN</name>
  <files>app/r/[slug]/admin/staff/page.tsx, app/r/[slug]/admin/staff/actions.ts</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md, docs/30-KeHoach/P1/01-02-PLAN.md</read_first>
  <action>staff/page.tsx: bảng nhân viên của tenant (guard: chỉ owner/manager). Nút "Thêm nhân viên": tên hiển thị + vai trò (cashier/waiter/kitchen) + PIN 4 số. actions.ts (server): băm PIN qua lib/auth/pin, insert memberships (role tương ứng, pin_hash, display_name; **user_id = NULL** — nhân viên PIN-only, không cấp tài khoản Supabase riêng; truy vết qua staff_membership_id ở P3/P4). Cho phép tạo NHIỀU nhân viên cùng vai trò (vd 3 thu ngân), mỗi người 1 PIN riêng → truy vết từng người. Sửa/xóa/đặt lại PIN.</action>
  <verify>Tạo cashier "Lan" PIN 1234 → hiện trong bảng, pin_hash lưu (không lộ PIN)</verify>
  <acceptance_criteria>
    - actions.ts gọi hashPin trước khi insert (không lưu PIN thô)
    - Trang chặn nếu role không phải owner/manager
  </acceptance_criteria>
  <done>Quản lý nhân viên + PIN hoạt động</done>
</task>

<task type="auto">
  <name>Task 4: Đăng nhập trạm + PinPad + StaffPicker (POS & KDS)</name>
  <files>app/r/[slug]/pos/login/page.tsx, app/r/[slug]/pos/page.tsx, app/r/[slug]/kds/login/page.tsx, app/r/[slug]/kds/page.tsx, components/staff/PinPad.tsx, components/staff/StaffPicker.tsx</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md, docs/10-BanThietKe/03-LuongFrontend.md</read_first>
  <action>pos/login: đăng nhập tài khoản trạm (email+mật khẩu station) 1 lần/thiết bị (Supabase Auth). Sau đăng nhập trạm: StaffPicker hiển thị danh sách nhân viên tenant, chọn tên → PinPad (bàn phím số, ô ≥44px) → server action verifyPin → lưu staff hiện tại vào cookie/bộ nhớ tab → vào pos/page.tsx (shell POS tạm: "Trạm POS — [tenant] — nhân viên: Lan"). kds/login tương tự → kds/page.tsx shell KDS tạm. Dùng biến thể dày đặc (Inter, nút to) theo QD-006 F3.</action>
  <verify>Đăng nhập trạm, chọn Lan + PIN 1234 → vào POS thấy tên Lan; PIN sai báo lỗi</verify>
  <acceptance_criteria>
    - PinPad.tsx nút số có min-height ≥44px
    - Xác thực PIN qua server action (fetch/route), không so khớp ở client
    - pos/page.tsx hiển thị tên nhân viên đang thao tác
  </acceptance_criteria>
  <done>Đăng nhập trạm + PIN hoạt động (AUTH-02, AUTH-03)</done>
</task>

<task type="auto">
  <name>Task 5: RBAC redirect theo vai trò</name>
  <files>middleware.ts, lib/auth/rbac.ts, app/r/[slug]/admin/layout.tsx</files>
  <read_first>docs/30-KeHoach/P1/01-02-PLAN.md, lib/auth/rbac.ts</read_first>
  <action>Sau đăng nhập, dùng defaultRouteForRole để đưa người dùng tới khu đúng vai trò. Bổ sung guard: nếu membership.role không được phép vào section hiện tại (canAccess=false) → redirect về route mặc định của họ. Ví dụ: kitchen mở /admin → đẩy về /kds.</action>
  <verify>Đăng nhập kitchen mở /admin → bị đẩy về /kds</verify>
  <acceptance_criteria>
    - admin/layout.tsx dùng canAccess và redirect khi sai vai trò
  </acceptance_criteria>
  <done>Điều hướng theo vai trò (AUTH-04)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Quản lý nhân viên + PIN, đăng nhập trạm POS/KDS, RBAC. Dev localhost:3000.</what-built>
  <how-to-verify>
    1. Owner Phở Việt vào /r/pho-viet/admin/staff → tạo "Lan" (cashier, PIN 1234) và "Hùng" (kitchen, PIN 5678).
    2. Mở /r/pho-viet/pos/login → đăng nhập tài khoản trạm → chọn "Lan" → gõ PIN 1234 → vào shell POS thấy "nhân viên: Lan". Thử PIN sai → báo lỗi, không vào.
    3. Mở /r/pho-viet/kds/login → vào shell KDS.
    4. Đăng nhập bằng tài khoản có vai trò kitchen rồi mở /r/pho-viet/admin → phải bị đẩy về /kds (không vào admin).
    5. Kiểm tra nút PIN đủ to để bấm bằng ngón tay (≥44px).
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (tạo NV, PIN, đăng nhập trạm, hoặc RBAC sai).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Tạo nhân viên + PIN, pin_hash không lưu PIN thô
- [ ] Đăng nhập trạm + chọn nhân viên + PIN vào được POS/KDS
- [ ] PIN sai bị từ chối (xác thực phía server)
- [ ] RBAC: vai trò sai bị redirect đúng
- [ ] Nút PIN ≥44px
</verification>

<success_criteria>
- Checkpoint "approved"
- AUTH-02/03/04 + OPS-06 (logo tenant ở shell) chứng minh được
- Hiện thực đúng D7 (PIN không thay RLS)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P1/01-03-SUMMARY.md.
</output>
