---
phase: 01-nen-tang
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - supabase/seed.sql
  - tests/rls/setup.ts
  - tests/rls/tenant-isolation.test.ts
  - vitest.config.ts
  - package.json
  - app/r/[slug]/admin/data-scope/page.tsx
  - .github/workflows/ci.yml
autonomous: false
requirements: [TENANT-02]
user_setup:
  - "Tạo Supabase project test/ephemeral hoặc cho phép chạy test trên dev (schema đã có từ 01-02)"
must_haves:
  truths:
    - "Chạy `npm run test:rls` cho kết quả PASS: user tenant A không đọc/ghi được dữ liệu tenant B"
    - "Trang /r/[slug]/admin/data-scope chỉ liệt kê dữ liệu của tenant hiện tại — mở bằng owner A và owner B cho 2 danh sách khác nhau"
    - "Có sẵn 2 tenant demo (Phở Việt, Bún Bò) qua seed"
  artifacts:
    - "tests/rls/tenant-isolation.test.ts chứa ca kiểm A đọc B, A ghi B, đều bị chặn"
    - "supabase/seed.sql tạo 2 tenant demo + owner mỗi bên"
    - "app/r/[slug]/admin/data-scope/page.tsx render danh sách phạm vi dữ liệu tenant"
  key_links:
    - ".github/workflows/ci.yml chạy test:rls trên mỗi push"
    - "test dùng client anon đăng nhập từng owner để chứng minh RLS thật (không service role)"
---

<objective>
Chứng minh cách ly tenant bằng test tự động VÀ bằng một màn hình bấm được, đồng thời seed 2 tenant demo. Đây là tiêu chí nghiệm thu quan trọng nhất của P1 (TENANT-02).

Purpose: Bằng chứng RLS thật; nền cho "2 tenant demo trên prod" ở P6.
Output: seed 2 tenant, bộ test RLS Vitest, trang data-scope, CI chạy test.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md
@docs/30-KeHoach/P1/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed 2 tenant demo</name>
  <files>supabase/seed.sql</files>
  <read_first>docs/30-KeHoach/P1/01-02-PLAN.md</read_first>
  <action>Viết seed tạo 2 tenant: "Phở Việt" (slug pho-viet) + owner A, "Bún Bò Huế" (slug bun-bo) + owner B. Tạo auth user cho 2 owner (email cố định demo), profiles, memberships(role=owner) cho mỗi tenant. Thêm vài dòng dữ liệu vô hại (vd 1 membership station mỗi tenant) để test đọc chéo có mục tiêu.</action>
  <verify>Chạy seed → 2 tenant + 2 owner tồn tại</verify>
  <acceptance_criteria>
    - seed.sql tạo tenant slug 'pho-viet' và 'bun-bo'
    - Mỗi tenant có 1 membership role 'owner'
  </acceptance_criteria>
  <done>Có 2 tenant demo</done>
</task>

<task type="auto">
  <name>Task 2: Bộ test RLS cách ly tenant</name>
  <files>vitest.config.ts, tests/rls/setup.ts, tests/rls/tenant-isolation.test.ts, package.json</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Cài vitest. setup.ts: hàm signInAs(email,password) trả supabase client (anon key) đã đăng nhập. tenant-isolation.test.ts với các ca: (1) owner A select memberships của tenant B → trả 0 dòng (RLS lọc); (2) owner A update/insert vào tenant B → bị từ chối (error hoặc 0 rows affected); (3) owner A select tenant của mình → có dữ liệu (đối chứng dương). Dùng CLIENT ANON + phiên đăng nhập thật, KHÔNG service role (service role bỏ qua RLS, sẽ cho kết quả sai). Thêm script "test:rls": "vitest run tests/rls".</action>
  <verify>npm run test:rls → tất cả ca PASS</verify>
  <acceptance_criteria>
    - test có ca "A đọc B → 0 rows" và ca "A ghi B → bị chặn"
    - test dùng anon key + signIn, không dùng SUPABASE_SERVICE_ROLE_KEY
    - package.json có script test:rls
  </acceptance_criteria>
  <done>TENANT-02 chứng minh bằng test tự động</done>
</task>

<task type="auto">
  <name>Task 3: Trang "Phạm vi dữ liệu" (bằng chứng bấm được)</name>
  <files>app/r/[slug]/admin/data-scope/page.tsx</files>
  <read_first>docs/10-BanThietKe/03-LuongFrontend.md, docs/30-KeHoach/P1/01-02-PLAN.md</read_first>
  <action>Trang server component dùng client Supabase theo phiên owner (RLS): truy vấn memberships + thông tin tenant hiện tại và render danh sách "Dữ liệu bạn thấy: tenant [name], nhân viên: [...]". Vì RLS, khi mở bằng owner A chỉ thấy Phở Việt; owner B chỉ thấy Bún Bò. Thêm mục nav "Phạm vi dữ liệu" vào AdminShell.</action>
  <verify>Owner A và owner B mở trang → 2 danh sách khác nhau, không lẫn</verify>
  <acceptance_criteria>
    - page.tsx dùng client Supabase theo phiên (RLS), KHÔNG service role
    - Trang hiển thị tên tenant + danh sách membership của tenant hiện tại
  </acceptance_criteria>
  <done>Cách ly tenant quan sát được trên UI</done>
</task>

<task type="auto">
  <name>Task 4: CI chạy test RLS</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Workflow CI: trên push/PR, cài deps, áp migration + seed vào Supabase test, chạy `npm run build` + `npm run test:rls`. Env test qua secrets. Fail CI nếu test RLS đỏ.</action>
  <verify>Push → CI xanh, log có "test:rls ... passed"</verify>
  <acceptance_criteria>
    - ci.yml chạy bước "npm run test:rls"
    - ci.yml chạy "npm run build"
  </acceptance_criteria>
  <done>RLS được bảo vệ liên tục</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>2 tenant demo + test RLS + trang data-scope. Dev localhost:3000.</what-built>
  <how-to-verify>
    1. Chạy `npm run test:rls` ở terminal → xác nhận tất cả ca PASS (đặc biệt "A đọc B → 0 rows", "A ghi B → bị chặn").
    2. Trình duyệt 1: đăng nhập owner A (Phở Việt) → mở /r/pho-viet/admin/data-scope → chỉ thấy dữ liệu Phở Việt.
    3. Trình duyệt 2 (ẩn danh): đăng nhập owner B (Bún Bò) → mở /r/bun-bo/admin/data-scope → chỉ thấy dữ liệu Bún Bò.
    4. Xác nhận owner A KHÔNG thấy bất kỳ dữ liệu nào của Bún Bò và ngược lại.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (test đỏ, hoặc thấy lẫn dữ liệu chéo tenant).</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run test:rls PASS (đọc chéo + ghi chéo đều bị chặn)
- [ ] Test dùng anon + phiên đăng nhập, không service role
- [ ] Trang data-scope hiện đúng phạm vi từng tenant
- [ ] CI chạy test:rls và build
- [ ] 2 tenant demo tồn tại
</verification>

<success_criteria>
- Checkpoint "approved"
- TENANT-02 chứng minh bằng CẢ test tự động lẫn quan sát UI
- Nền "2 tenant demo" cho P6
</success_criteria>

<output>
Tạo docs/30-KeHoach/P1/01-04-SUMMARY.md. Sau khi cả 4 plan xong, tick P1 trong docs/00-TongQuan/Roadmap.md và cập nhật trạng thái yêu cầu trong 20-DanhSachYeuCau.
</output>
