---
phase: 01-nen-tang
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - app/globals.css
  - app/layout.tsx
  - app/(marketing)/page.tsx
  - app/style-guide/page.tsx
  - app/r/[slug]/layout.tsx
  - middleware.ts
  - lib/fonts.ts
  - lib/supabase/client.ts
  - lib/supabase/server.ts
  - lib/design/tokens.css
  - components/ui/* (shadcn)
  - .env.local.example
  - supabase/config.toml
  - .github/ or vercel pipeline config
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03, OPS-05, TENANT-04]
user_setup:
  - "Tạo project Supabase dev (lấy URL + anon key + service_role key)"
  - "Nối repo với Vercel, đặt env cho branch dev; bật preview cho branch `dev`"
must_haves:
  truths:
    - "Mở http://localhost:3000/style-guide hiển thị đủ token màu/chữ/spacing/radius Mistral + 4 profile bề mặt"
    - "Cùng trang mở được trên URL Vercel dev (preview branch dev)"
    - "Truy cập /r/pho-viet (slug bất kỳ) → middleware nhận diện slug, không 500"
  artifacts:
    - "app/style-guide/page.tsx render component thật (không placeholder)"
    - "lib/design/tokens.css chứa biến CSS cho toàn bộ token Mistral"
    - "middleware.ts phân giải tenant theo slug, có nhánh subdomain (tắt)"
  key_links:
    - "tailwind.config.ts đọc biến từ tokens.css; shadcn components dùng biến này"
    - "lib/fonts.ts nạp Fraunces/Inter/JetBrains Mono qua next/font, gắn vào layout"
---

<objective>
Dựng khung dự án chạy được trên local + Vercel dev, và một trang `/style-guide` render toàn bộ design system Mistral (token + 4 profile bề mặt). Đây là "walking skeleton" của P1: chứng minh 3 môi trường + pipeline + design system bằng một trang bấm/xem được.

Purpose: Đặt nền tảng kỹ thuật + hình ảnh cho mọi plan sau; là bằng chứng OPS-01/02/03/05.
Output: Next.js scaffold, Tailwind+shadcn, fonts, Supabase client wiring, middleware tenant (slug, chừa subdomain), route groups, trang style-guide, pipeline dev.
</objective>

<context>
@docs/10-BanThietKe/02-FrontendChiTiet.md
@docs/DESIGN-mistral.ai.md
@docs/15-QuyetDinh/QD-006-DesignSystemFrontend.md
@docs/10-BanThietKe/01-KyThuatChiTiet.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js + Tailwind + shadcn</name>
  <files>package.json, next.config.ts, tailwind.config.ts, app/globals.css, app/layout.tsx, components/ui/</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md</read_first>
  <action>Khởi tạo Next.js 15 App Router + TypeScript. Cài Tailwind + shadcn/ui (init). Thêm route groups thư mục: app/(marketing), app/r/[slug]/(customer), app/r/[slug]/pos, app/r/[slug]/kds, app/r/[slug]/admin, app/super. Chưa cần nội dung, chỉ layout rỗng để cây route tồn tại. Đặt `lang="vi"` ở html.</action>
  <verify>npm run build chạy không lỗi; npm run dev mở được localhost:3000</verify>
  <acceptance_criteria>
    - app/layout.tsx chứa `lang="vi"`
    - Tồn tại thư mục app/r/[slug]/pos, app/r/[slug]/kds, app/r/[slug]/admin, app/super
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>Build xanh, cây route groups tồn tại</done>
</task>

<task type="auto">
  <name>Task 2: Nạp token Mistral thành CSS variables + fonts</name>
  <files>lib/design/tokens.css, app/globals.css, tailwind.config.ts, lib/fonts.ts, app/layout.tsx</files>
  <read_first>docs/DESIGN-mistral.ai.md, docs/10-BanThietKe/02-FrontendChiTiet.md</read_first>
  <action>Tạo lib/design/tokens.css khai báo biến CSS cho: màu (primary #fa520f, primary-deep #cc3a05, cream #fff8e0, cream-deeper #fff0c2, ink #1f1f1f, slate/steel/muted, hairline, beige-deep) + màu status mở rộng (status-new, status-active=#fa520f, status-ready=#1a7f4b, status-late=#c0341d, status-done=#6a6a6a) + spacing (4→120) + radius (xs4..full) theo QD-006 mục 2. Thêm biến `--tenant-primary: var(--color-primary)` (V1 = mặc định). tailwind.config.ts map các biến này vào theme.extend. lib/fonts.ts nạp Fraunces (display), Inter (sans, mặc định), JetBrains Mono (mono) qua next/font/google và export biến; layout.tsx gắn className font.</action>
  <verify>Kiểm tra tokens.css chứa --color-primary: #fa520f; layout dùng biến font</verify>
  <acceptance_criteria>
    - lib/design/tokens.css chứa "--color-primary: #fa520f"
    - lib/design/tokens.css chứa "--status-ready: #1a7f4b" và "--tenant-primary"
    - lib/fonts.ts import Fraunces, Inter, JetBrains_Mono từ next/font/google
    - tailwind.config.ts tham chiếu var(--color-primary)
  </acceptance_criteria>
  <done>Token + font sẵn sàng dùng qua Tailwind/shadcn</done>
</task>

<task type="auto">
  <name>Task 3: Middleware nhận diện tenant theo slug (chừa subdomain)</name>
  <files>middleware.ts, app/r/[slug]/layout.tsx</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>middleware.ts: với path bắt đầu `/r/[slug]`, trích slug, gắn header `x-tenant-slug`. Viết SẴN nhánh nhận diện theo subdomain (đọc host, nếu là `*.domain.com` thì lấy subdomain) nhưng để CỜ TẮT (const ENABLE_SUBDOMAIN = false). Chưa tra DB (chưa có bảng ở plan này) — chỉ chuyển slug xuống layout. app/r/[slug]/layout.tsx đọc slug từ params, hiển thị tạm tên slug ở header để chứng minh routing.</action>
  <verify>Mở /r/pho-viet → trang không 500, thấy slug "pho-viet"</verify>
  <acceptance_criteria>
    - middleware.ts chứa "x-tenant-slug" và "ENABLE_SUBDOMAIN = false"
    - app/r/[slug]/layout.tsx đọc params.slug
  </acceptance_criteria>
  <done>Routing theo slug hoạt động, subdomain chừa sẵn</done>
</task>

<task type="auto">
  <name>Task 4: Wiring Supabase client (chưa dùng nghiệp vụ)</name>
  <files>lib/supabase/client.ts, lib/supabase/server.ts, .env.local.example, supabase/config.toml</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Cài @supabase/ssr + @supabase/supabase-js. lib/supabase/client.ts (browser client) + server.ts (server client dùng cookies). .env.local.example liệt kê NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY (chỉ server). Init supabase CLI (supabase/config.toml) để plan sau chạy migration. KHÔNG commit secrets.</action>
  <verify>Import client không lỗi build; .env.local.example không chứa giá trị thật</verify>
  <acceptance_criteria>
    - lib/supabase/server.ts dùng @supabase/ssr createServerClient
    - .env.local.example liệt kê 3 biến, không có key thật
    - Tồn tại supabase/config.toml
  </acceptance_criteria>
  <done>Supabase sẵn sàng cho plan 01-02</done>
</task>

<task type="auto">
  <name>Task 5: Trang style-guide render 4 profile bề mặt</name>
  <files>app/style-guide/page.tsx, components/design/*</files>
  <read_first>docs/10-BanThietKe/02-FrontendChiTiet.md, docs/DESIGN-mistral.ai.md</read_first>
  <action>Tạo app/style-guide/page.tsx render: (1) bảng token màu (kể cả status), (2) thang chữ Fraunces/Inter/JetBrains Mono, (3) spacing + radius, (4) bộ nút (primary/dark/secondary/cream/link), thẻ (base/feature/cream), input, badge, pill-tab; (5) 4 khối "profile bề mặt" minh hoạ: Customer (hero Fraunces + thẻ kem), POS (nút to ≥44px, mật độ cao), KDS (vé trạng thái màu status, chữ lớn), Admin (bảng+form). Dùng component shadcn đã map token.</action>
  <verify>Mở /style-guide thấy đủ 5 khối, đúng màu cam/kem, đúng 3 font</verify>
  <acceptance_criteria>
    - app/style-guide/page.tsx render nút nền var(--color-primary)
    - Trang có 4 khối nhãn "Customer", "POS", "KDS", "Admin"
    - Nút bấm ở khối POS có min-height ≥ 44px
  </acceptance_criteria>
  <done>Style-guide là bằng chứng OPS-03/OPS-05</done>
</task>

<task type="auto">
  <name>Task 6: Pipeline 3 môi trường (dev)</name>
  <files>vercel config, README.md, .env.local.example</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md</read_first>
  <action>Cấu hình Vercel: branch `dev` → preview, `main` → production. Ghi README ngắn cách set env trên Vercel (URL/anon/service_role) và cách chạy `supabase db push` khi merge. Đảm bảo branch `dev` deploy được trang /style-guide.</action>
  <verify>Push branch dev → Vercel build xanh → mở URL dev /style-guide</verify>
  <acceptance_criteria>
    - README.md có mục "Môi trường" mô tả dev/prod + biến env
    - Vercel dev URL mở được /style-guide (xác nhận ở checkpoint)
  </acceptance_criteria>
  <done>OPS-01/OPS-02 chứng minh được</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Khung dự án + trang style-guide. Dev server ở http://localhost:3000, và URL Vercel dev (branch `dev`).</what-built>
  <how-to-verify>
    1. Mở http://localhost:3000/style-guide → xác nhận: màu cam #fa520f ở nút, nền kem, chữ tiêu đề serif (Fraunces), chữ UI Inter; đủ 4 khối Customer/POS/KDS/Admin; nút khối POS to (dễ chạm).
    2. Mở http://localhost:3000/r/pho-viet → thấy header hiển thị slug "pho-viet", không lỗi.
    3. Mở URL Vercel dev /style-guide → giống hệt local.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (màu/font/khối/route sai).</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build exit 0, không warning mới
- [ ] /style-guide render đủ token + 4 profile (local + dev)
- [ ] /r/[slug] phân giải slug qua middleware, không 500
- [ ] Không commit secret; .env.local.example không chứa key thật
</verification>

<success_criteria>
- Tất cả task xong, checkpoint được "approved"
- App chạy local + Vercel dev cùng codebase (OPS-01)
- Style-guide là bằng chứng design system Mistral + 4 profile (OPS-03, OPS-05)
- Routing tenant theo slug hoạt động, subdomain chừa sẵn (TENANT-04 phần định tuyến)
</success_criteria>

<output>
Sau khi xong, tạo docs/30-KeHoach/P1/01-01-SUMMARY.md (file đã đổi, bằng chứng ảnh /style-guide, trạng thái từng cam kết).
</output>
