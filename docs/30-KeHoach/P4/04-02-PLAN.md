---
phase: 04-dong-tien
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/billing/split.ts
  - lib/billing/bill.ts
  - app/r/[slug]/pos/actions.ts
  - components/pos/BillPanel.tsx
  - components/pos/SplitBillDialog.tsx
  - components/pos/MergeTablesDialog.tsx
  - components/pos/PosBoard.tsx
autonomous: false
requirements: [BILL-02]
user_setup:
  - "1 bàn có bill 'open' với ≥2 món SL>1 (để tách theo món + chia đều)."
  - "2 bàn cùng có phiên mở + món đã phục vụ (để gộp bàn)."
must_haves:
  truths:
    - "Tách theo món: chọn (order_item, số suất) chuyển sang bill mới → 2 bill, tổng suất mỗi món giữ nguyên"
    - "Chia đều N: 1 bill → N bill, mỗi bill = tổng/N (phân bổ chênh lệch làm tròn vào bill cuối, không mất/dư đồng nào)"
    - "Gộp bàn: chọn nhiều phiên bàn → 1 bill gom order_items các bàn; các bàn đánh dấu đang gộp"
    - "Bất biến sau mọi thao tác: Σ qty_allocated per order_item (mọi bill open) = qty (order_item chưa hủy)"
  artifacts:
    - "lib/billing/split.ts: splitByItems + splitEvenly + mergeSessions thuần logic phân bổ (test được)"
    - "components/pos/SplitBillDialog.tsx + MergeTablesDialog.tsx"
  key_links:
    - "Tách = di chuyển/nhân bản bill_items giữa các bill cùng phiên; KHÔNG sửa order_items (nguồn suất bất biến)"
    - "Chia đều theo TIỀN (chia total), không chia món — mỗi bill giữ tham chiếu order_items gốc + phần amount"
    - "Chỉ tách/gộp khi mọi bill liên quan status='open' (đã 'paid' thì khóa)"
    - "Sau tách/gộp: tính lại totals từng bill bằng computeBillTotals (04-01)"
---

<objective>
Tách 1 bàn thành nhiều bill (theo món hoặc chia đều N người) và gộp nhiều bàn thành 1 bill. Giữ bất biến số suất khi đối chiếu doanh thu.

Purpose: Bằng chứng BILL-02. Hoàn thiện linh hoạt chốt tiền thực tế (khách chia tiền, nhóm gộp bàn).
Output: Module phân bổ thuần + 2 dialog thao tác trên BillPanel.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.5 — tách/gộp qua bill_items.qty_allocated)
@docs/30-KeHoach/P4/00-TongQuan.md  (Bất biến tách/gộp)
@docs/15-QuyetDinh/QD-007-DongTienP4.md  (D-P4-2 làm đầy đủ)
@lib/billing/bill.ts  (openBillForSession, getBillView — 04-01)
@lib/billing/compute.ts  (computeBillTotals)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Module phân bổ tách/gộp (thuần)</name>
  <files>lib/billing/split.ts</files>
  <read_first>lib/billing/types.ts, lib/billing/compute.ts</read_first>
  <action>split.ts — logic thuần (nhận danh sách bill_items hiện tại, trả kế hoạch thay đổi; không I/O): (1) planSplitByItems(sourceItems, picks:{orderItemId, qty}[]) → validate mỗi pick.qty ≤ qty_allocated còn lại của dòng nguồn; trả {toNewBill: bill_items mới (qty picked), remainInSource: cập nhật qty_allocated dòng nguồn}; dòng nguồn về 0 → xóa khỏi bill nguồn. (2) planSplitEvenly(totalItems, n) → chia THEO TIỀN: mỗi bill giữ toàn bộ tham chiếu order_items nhưng dùng "share" — thực dụng V1: tạo n bill, mỗi bill có bill_items sao chép nhưng amount phân bổ = floor(amount/n), phần dư dồn bill cuối; đảm bảo Σ amount n bill = subtotal gốc. (Ghi chú: chia đều KHÔNG đổi qty_allocated nguồn — dùng cột amount để mang phần chia; giữ order_item_id để truy vết). (3) planMergeSessions(sessions[]) → gom order_items chưa phân bổ của mọi phiên vào 1 danh sách bill_items cho bill gộp. Kèm assertInvariant(orderItems, allBillItems) kiểm Σ qty_allocated = qty. Viết split.test.ts phủ: tách 1 phần SL, tách hết dòng (xóa khỏi nguồn), chia đều 3 lẻ (dư dồn bill cuối), gộp 2 phiên.</action>
  <verify>npm test split.test.ts xanh; bất biến tổng suất/tổng tiền giữ nguyên mọi ca</verify>
  <acceptance_criteria>
    - planSplitByItems chặn pick vượt qty còn lại; dòng nguồn về 0 thì loại
    - planSplitEvenly: Σ amount các bill = subtotal gốc (dư dồn bill cuối)
    - assertInvariant phát hiện lệch suất
  </acceptance_criteria>
  <done>Lõi tách/gộp có test</done>
</task>

<task type="auto">
  <name>Task 2: Server actions tách/gộp (transaction)</name>
  <files>lib/billing/bill.ts, app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/billing/split.ts, app/r/[slug]/pos/actions.ts</read_first>
  <action>bill.ts thêm: splitBillByItems(tenant, billId, picks) — tải bill_items nguồn (chỉ khi bill open), planSplitByItems, trong 1 lượt ghi: tạo bill mới (bill_no mới, cùng table_session_id) + INSERT bill_items mới + UPDATE/DELETE dòng nguồn + tính lại totals 2 bill. splitBillEvenly(tenant, billId, n) — plan + tạo n bill, gán bill_items, tính totals. mergeSessionsIntoBill(tenant, sessionIds[]) — planMergeSessions, tạo 1 bill (table_session_id=null khi >1 phiên), INSERT bill_items, tính totals; đánh dấu (không đóng phiên tới khi paid). Dùng supabase (RLS phiên station); gói thao tác qua RPC hoặc thứ tự ghi an toàn (kiểm mọi bill liên quan còn 'open' TRƯỚC khi ghi; nếu 1 bill đã paid → abort trả lỗi). actions.ts: splitByItemsAction/splitEvenlyAction/mergeTablesAction (auth canAccess 'pos', revalidate). Không cho tách/gộp khi có bill status='paid' liên quan.</action>
  <verify>Tách → 2 bill tổng suất giữ nguyên; gộp 2 bàn → 1 bill có món cả 2; thao tác trên bill đã paid bị chặn</verify>
  <acceptance_criteria>
    - Kiểm "mọi bill liên quan open" trước khi ghi; bill paid → abort
    - Sau tách/gộp totals từng bill tính lại đúng
    - Gộp >1 phiên → table_session_id=null, truy bàn qua bill_items
  </acceptance_criteria>
  <done>Tách/gộp an toàn dữ liệu</done>
</task>

<task type="auto">
  <name>Task 3: Dialog tách + gộp trên POS</name>
  <files>components/pos/SplitBillDialog.tsx, components/pos/MergeTablesDialog.tsx, components/pos/BillPanel.tsx, components/pos/PosBoard.tsx</files>
  <read_first>components/pos/BillPanel.tsx (04-01), components/pos/PosBoard.tsx</read_first>
  <design>BÁM QD-006 token; POS nhẹ (không vaul/motion). Dialog = center modal desktop-tablet (khớp ModifierSheet presentation="dialog" của POS). SplitBillDialog: 2 tab "Theo món" (list món + stepper chọn số suất chuyển sang bill B, xem trước tổng 2 bill) / "Chia đều" (chọn N 2..8, xem trước mỗi người trả bao nhiêu). MergeTablesDialog: list phiên bàn đang mở (checkbox), xem trước tổng bill gộp. Số tiền tabular-nums.</design>
  <action>BillPanel thêm nút "Tách bill" + "Gộp bàn" (khi bill open). SplitBillDialog: chọn kiểu tách → gọi splitByItemsAction/splitEvenlyAction → đóng, refresh panel (hiện danh sách bill của bàn). MergeTablesDialog mở từ panel bàn (hoặc toolbar POS): chọn phiên → mergeTablesAction → mở bill gộp. PosBoard: quản lý danh sách bill/bàn khi 1 bàn có >1 bill (sau tách) — panel liệt kê "Bill #1, Bill #2"; realtime bills refresh.</action>
  <verify>360°: tách theo món → 2 bill hiện trong panel; chia đều 3 → 3 bill mỗi bill 1/3; gộp 2 bàn → 1 bill; tổng khớp</verify>
  <acceptance_criteria>
    - Tách theo món chọn được số suất; xem trước tổng 2 bill trước khi xác nhận
    - Chia đều N hiện số tiền mỗi người; Σ = tổng gốc
    - Gộp bàn liệt kê phiên mở, tạo 1 bill gom đủ món
  </acceptance_criteria>
  <done>Thu ngân tách/gộp bill trên POS (BILL-02)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tách/gộp bill. Dev localhost:3000, POS.</what-built>
  <how-to-verify>
    1. Bàn B1 có bill open với "Phở bò ×2" + "Trà đá ×3". Panel → "Tách bill" → tab Theo món → chuyển 1 Phở + 2 Trà sang bill B → xác nhận. Panel hiện Bill#1 (1 Phở, 1 Trà) + Bill#2 (1 Phở, 2 Trà); tổng 2 bill = tổng gốc.
    2. Bill khác → "Tách" → Chia đều 3 → 3 bill, mỗi bill tổng/3 (bill cuối ôm phần lẻ); cộng lại = gốc.
    3. B1 và B2 đều có món phục vụ → "Gộp bàn" → chọn B1+B2 → 1 bill gom đủ món 2 bàn.
    4. (DB) kiểm Σ qty_allocated mỗi order_item = qty; không món nào mất/nhân đôi.
    5. Thử "Tách" trên bill đã (giả lập) paid → bị chặn.
    6. `npm test` split.test.ts xanh.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (lệch suất, mất tiền khi chia lẻ, gộp thiếu món).</resume-signal>
</task>

</tasks>

<verification>
- [ ] split.test.ts xanh (tách phần/hết, chia đều lẻ, gộp)
- [ ] Tách theo món giữ nguyên tổng suất; xem trước 2 bill
- [ ] Chia đều N: Σ = tổng gốc, không mất/dư đồng
- [ ] Gộp nhiều bàn → 1 bill; bất biến qty_allocated=qty
- [ ] Chặn thao tác khi có bill paid liên quan
</verification>

<success_criteria>
- Checkpoint "approved"
- BILL-02 chứng minh đủ 3 kiểu (theo món / chia đều / gộp bàn)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P4/04-02-SUMMARY.md (file đã đổi, bằng chứng bất biến suất/tiền, kết quả test).
</output>
