---
phase: 04-dong-tien
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - lib/billing/bill.ts
  - lib/billing/receipt-view.ts
  - lib/print/adapter.ts
  - app/r/[slug]/pos/actions.ts
  - app/r/[slug]/print/receipt/[billId]/page.tsx
  - app/r/[slug]/print/receipt/actions.ts
  - components/pos/PaymentDialog.tsx
  - components/print/ReceiptDoc.tsx
  - components/pos/BillPanel.tsx
autonomous: false
requirements: [BILL-04, PRINT-03, TABLE-02]
user_setup:
  - "1 bill open đã có món (chạy 04-01); settings có tên+logo+footer hóa đơn."
must_haves:
  truths:
    - "Trên bill open: 'Thu tiền' → chọn tiền mặt/chuyển khoản, nhập số tiền nhận (mặt: tính tiền thối) → đóng bill (paid)"
    - "Đóng bill ≤5s: bill status=paid + paid_at + ghi payments; khi mọi bill của phiên paid → phiên closed + bàn về available"
    - "In hóa đơn 80mm: tên+logo NH, bàn/bill_no, ngày giờ, món+đơn giá+thành tiền, dòng giảm giá/phí/VAT, tổng, footer — không tràn khổ"
    - "Bill đã paid khóa cứng: không tách/gộp/điều chỉnh; in lại hóa đơn được"
  artifacts:
    - "PaymentDialog.tsx: chọn phương thức + số tiền + tiền thối; ReceiptDoc.tsx khổ 58/80mm"
    - "app/r/[slug]/print/receipt/[billId]/page.tsx + BrowserPrintAdapter.printReceipt implement"
  key_links:
    - "printReceipt (đang ném lỗi 'P4') → mở /r/[slug]/print/receipt/[billId]?w=80; dùng lại hạ tầng khổ giấy phiếu bếp (03-05)"
    - "Ghi print_jobs type='receipt' status='printed' khi in (mẫu logKitchenTicketPrint)"
    - "Đóng phiên: mọi bills của table_session paid + không còn order_item chưa thu → table_sessions closed, tables.status='available' (TABLE-02 phần còn)"
    - "Chuyển khoản = chỉ ghi nhận (QD-007 D-P4-1) — không sinh QR"
---

<objective>
Thu tiền (mặt/chuyển khoản) và đóng bill, tự đóng phiên bàn + in hóa đơn khổ 80mm. Hoàn tất vòng đời tiền của 1 bàn.

Purpose: Bằng chứng BILL-04 (thu tiền + đóng bill ≤5s) + PRINT-03 (hóa đơn 80mm đủ nội dung) + TABLE-02 (đóng phiên khi thanh toán). Đóng chuỗi giá trị order→bếp→tiền.
Output: PaymentDialog, action thu tiền + đóng phiên, route + ReceiptDoc in hóa đơn, implement printReceipt.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.5 payments, §7 in hóa đơn — nội dung)
@docs/15-QuyetDinh/QD-007-DongTienP4.md  (D-P4-1 chuyển khoản ghi nhận)
@lib/print/adapter.ts  (printReceipt stub cần implement; KitchenWidth tái dùng)
@components/print/KitchenTicketDoc.tsx  (mẫu SIZE + @media print + toolbar khổ)
@app/r/[slug]/print/kitchen/[orderId]/page.tsx  (mẫu route in + guard)
@lib/billing/bill.ts  (getBillView — 04-01)
@lib/tenant/settings.ts  (receipt_footer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Action thu tiền + đóng bill + đóng phiên</name>
  <files>lib/billing/bill.ts, app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/billing/bill.ts (04-01), app/r/[slug]/pos/actions.ts (mẫu đóng phiên P3), supabase/migrations/0008_orders_core.sql (table_sessions/tables)</read_first>
  <action>bill.ts thêm: payBill(tenant, billId, {method, amountReceived, note}, actorMembershipId) — chỉ bill 'open': INSERT payments(method, amount=bill.total, received_by=actor, note); UPDATE bills status='paid', paid_at=now, closed_by=actor; tính tiền thối = amountReceived - total (chỉ hiển thị, không lưu bắt buộc). Sau paid: gọi closeSessionIfSettled(tenant, sessionIds liên quan) — với mỗi table_session của bill (single: table_session_id; gộp: suy từ bill_items→order→session), kiểm mọi order_item (≠cancelled) của phiên đã nằm trong 1 bill 'paid' → nếu đủ: UPDATE table_sessions status='closed', closed_at; UPDATE tables status='available'. voidBill(tenant, billId) (tùy chọn, cho bill sai) — chỉ open. Chặn payBill nếu total ≤0 và không có món (bill rỗng). actions.ts: payBillAction(slug, billId, {method, amountReceived, note}, membershipId) — auth canAccess('pos') (thu tiền thường KHÔNG cần PIN riêng — QD-007 D-P4-3); revalidate POS. Trả {ok, change} để UI hiện tiền thối + nút in.</action>
  <verify>payBill → payments ghi đúng total; bill paid; phiên đủ điều kiện → closed + bàn available; đo thời gian từ bấm tới paid ≤5s</verify>
  <acceptance_criteria>
    - payBill ghi payments (method cash/transfer) = total; bill paid + paid_at
    - closeSessionIfSettled: mọi bill phiên paid → phiên closed + bàn available (TABLE-02)
    - Bill paid không cho pay lại/điều chỉnh/tách
  </acceptance_criteria>
  <done>Thu tiền + đóng bill + đóng phiên</done>
</task>

<task type="auto">
  <name>Task 2: PaymentDialog trên POS</name>
  <files>components/pos/PaymentDialog.tsx, components/pos/BillPanel.tsx</files>
  <read_first>components/pos/BillPanel.tsx (04-01/03), components/pos/AdjustBillDialog.tsx (mẫu dialog)</read_first>
  <design>BÁM QD-006 token; POS nhẹ. PaymentDialog = center modal: hiển thị TỔNG PHẢI THU đậm to; 2 nút chọn phương thức (Tiền mặt / Chuyển khoản); nếu tiền mặt → ô "Khách đưa" + nút mệnh giá nhanh (đủ, 100k, 200k, 500k) → hiện "Tiền trả lại"; nếu chuyển khoản → chỉ xác nhận "Đã nhận đủ" (QD-007: không QR). Nút "Xác nhận thu • đóng bill". Sau thành công → hiện tiền thối + nút "In hóa đơn" + "Xong".</design>
  <action>BillPanel: nút "Thu tiền" (bật, thay chỗ chừa ở 04-01) mở PaymentDialog khi bill open. PaymentDialog: chọn method + amountReceived (mặc định = total cho CK); "Xác nhận" → payBillAction → nếu ok: hiện tiền thối (mặt) + nút "In hóa đơn" (gọi getPrintAdapter().printReceipt({slug, billId})) + "Xong" (đóng, refresh POS — bàn về available). Bill paid → BillPanel chuyển chế độ chỉ-đọc + nút "In lại hóa đơn".</action>
  <verify>Thu tiền mặt: nhập khách đưa 500k, total 320k → thối 180k; CK: xác nhận đóng; sau đóng bàn về available; nút in hiện</verify>
  <acceptance_criteria>
    - Chọn mặt/CK; tiền mặt tính thối đúng; CK xác nhận đủ
    - Đóng bill ≤5s (bấm→paid); panel chuyển chỉ-đọc + in lại
    - Không sinh QR (chỉ ghi nhận)
  </acceptance_criteria>
  <done>Thu ngân thu tiền + đóng bill (BILL-04)</done>
</task>

<task type="auto">
  <name>Task 3: In hóa đơn 80mm + implement printReceipt</name>
  <files>lib/billing/receipt-view.ts, lib/print/adapter.ts, app/r/[slug]/print/receipt/[billId]/page.tsx, app/r/[slug]/print/receipt/actions.ts, components/print/ReceiptDoc.tsx</files>
  <read_first>components/print/KitchenTicketDoc.tsx (mẫu SIZE+@media print), app/r/[slug]/print/kitchen/[orderId]/page.tsx (guard+route), app/r/[slug]/print/kitchen/actions.ts (logKitchenTicketPrint)</read_first>
  <design>Hóa đơn khổ nhiệt 58/80mm (mặc định 80) — dùng lại kết cấu SIZE + @media @page của KitchenTicketDoc. JetBrains Mono, đen trắng, không tràn khổ. Nội dung §7: logo+tên NH (căn giữa), "HÓA ĐƠN", Bàn + #bill_no, ngày giờ (VN), bảng món (tên / SL×đơn giá / thành tiền — canh phải số), dòng Tạm tính, Giảm giá (nếu >0), Phí phục vụ x%, VAT x%, **TỔNG** đậm, phương thức TT + tiền thối (nếu mặt), footer (receipt_footer). Nút toolbar (ẩn khi in): In lại, Đóng, chọn khổ 58/80.</design>
  <action>receipt-view.ts (server): buildReceiptView(billId, tenantId) → tenant(name,logo), bill (bill_no, totals, discount/pct), lines (name_snapshot, qty_allocated, unit, amount), payment (method, amount, change nếu có), settings.receipt_footer, paid_at. adapter.ts: implement BrowserPrintAdapter.printReceipt({slug, billId, width='80'}) → window.open(`/r/${slug}/print/receipt/${billId}?w=${width}`); mở rộng type PrintReceiptArgs {slug, billId, width?:KitchenWidth}; BillView giữ. receipt/actions.ts: logReceiptPrint(slug, billId) → INSERT print_jobs type='receipt' status='printed' (mẫu logKitchenTicketPrint). page.tsx: guard membership canAccess('pos') (thu ngân); buildReceiptView; render ReceiptDoc. ReceiptDoc.tsx (client): giống KitchenTicketDoc (SIZE 58/80, useEffect auto window.print + logReceiptPrint 1 lần, toolbar khổ). Số tiền canh phải, tabular.</action>
  <verify>Mở /print/receipt/[billId]?w=80 → hóa đơn đủ mục, không tràn 80mm (PDF preview); w=58 co gọn; print_jobs ghi 1 dòng</verify>
  <acceptance_criteria>
    - Hóa đơn 80mm đủ: tên NH, bàn, món+giá+thành tiền, giảm giá/phí/VAT, tổng, phương thức, footer (PRINT-03)
    - Không tràn khổ; 58mm co đúng; JetBrains Mono đen trắng
    - printReceipt implement (hết ném lỗi P4); ghi print_jobs type=receipt
  </acceptance_criteria>
  <done>In hóa đơn khách (PRINT-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Thu tiền + đóng bill + đóng phiên + in hóa đơn 80mm. Dev localhost:3000, POS.</what-built>
  <how-to-verify>
    1. Bàn B1 có bill open total 320.000. Panel → "Thu tiền" → Tiền mặt → khách đưa 500.000 → thối 180.000 → "Xác nhận thu". Bấm→paid trong ≤5s.
    2. Sau đóng: bàn B1 về "available" (sơ đồ POS); bill chuyển chỉ-đọc.
    3. "In hóa đơn" → tab in 80mm: đủ tên+logo NH, Bàn B1 #bill_no, ngày giờ, các món + đơn giá + thành tiền, (giảm giá nếu có), Phí 5%, VAT 8%, TỔNG, "Tiền mặt · thối 180.000", footer. Không tràn khổ (PDF preview). Đổi 58mm → co gọn.
    4. Bill khác → "Thu tiền" → Chuyển khoản → "Đã nhận đủ" → đóng (không QR).
    5. Thử điều chỉnh/tách bill đã paid → bị chặn. "In lại hóa đơn" mở lại được.
    6. (DB) payments ghi method+amount=total; bills paid+paid_at; table_sessions closed; print_jobs type=receipt.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (bàn không về available, hóa đơn tràn/thiếu mục, thu tiền không khóa bill).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Thu tiền mặt (thối đúng) / chuyển khoản (ghi nhận, không QR); đóng bill ≤5s
- [ ] Mọi bill phiên paid → phiên closed + bàn available (TABLE-02)
- [ ] Hóa đơn 80mm đủ nội dung §7, không tràn; 58mm co đúng (PRINT-03)
- [ ] printReceipt implement + ghi print_jobs type=receipt
- [ ] Bill paid khóa (không tách/gộp/điều chỉnh); in lại được
</verification>

<success_criteria>
- Checkpoint "approved"
- BILL-04 + PRINT-03 + TABLE-02 (đóng) chứng minh trên trình duyệt + PDF preview
</success_criteria>

<output>
Tạo docs/30-KeHoach/P4/04-04-SUMMARY.md (file đã đổi, bằng chứng đóng bill ≤5s + ảnh/PDF hóa đơn 80mm).
</output>
