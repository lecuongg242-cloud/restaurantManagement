---
phase: 04-dong-tien
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - package.json
  - lib/billing/reports.ts
  - app/r/[slug]/admin/(protected)/reports/page.tsx
  - components/admin/reports/RevenueChart.tsx
  - components/admin/reports/TopItemsTable.tsx
  - components/admin/reports/PaymentBreakdown.tsx
  - components/admin/reports/RangePicker.tsx
  - components/admin/AdminNav.tsx
autonomous: false
requirements: [REPORT-01, REPORT-02, REPORT-03, BILL-05]
user_setup:
  - "≥20 bill đã 'paid' rải vài ngày (mặt + chuyển khoản) để có số liệu đối chiếu."
must_haves:
  truths:
    - "/admin/reports hiện doanh thu ngày/tuần/tháng (tổng, số bill, TB/bill) theo mốc ngày Việt Nam (UTC+7)"
    - "Biểu đồ doanh thu theo thời gian (recharts) + bảng món bán chạy (SL + doanh thu) + tách tiền mặt vs chuyển khoản"
    - "Doanh thu kỳ = Σ bills status='paid' trong kỳ = tổng dashboard (đối chiếu 20 bill khớp 100%)"
    - "Chỉ manager/owner xem báo cáo; số liệu scope đúng tenant (RLS)"
  artifacts:
    - "lib/billing/reports.ts: getRevenueSummary + getRevenueSeries + getTopItems + getPaymentBreakdown (server, RLS admin)"
    - "components/admin/reports/* dùng recharts; RangePicker ngày/tuần/tháng"
  key_links:
    - "recharts import CHỈ trong client component khu /admin/reports (không đụng bundle khách/POS)"
    - "Mốc thời gian ngày VN: gom theo (paid_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date"
    - "Doanh thu = bills.total (paid); món bán chạy = từ bill_items (qty_allocated, amount) của bill paid"
    - "Phương thức TT = payments.method (cash/transfer) của bill paid — REPORT-03 đối soát ca"
---

<objective>
Dashboard báo cáo cho quản lý: doanh thu theo ngày/tuần/tháng, món bán chạy, tách theo phương thức thanh toán. Doanh thu khớp 100% với tổng bill đã đóng.

Purpose: Bằng chứng REPORT-01/02/03 + BILL-05 (doanh thu khớp). Đóng P4 — chủ nhà hàng thấy dòng tiền.
Output: Module tổng hợp báo cáo (server) + trang /admin/reports (recharts) + link trong AdminNav.
</objective>

<context>
@docs/20-DanhSachYeuCau/00-Requirements.md  (REPORT-01..03, BILL-05)
@docs/15-QuyetDinh/QD-007-DongTienP4.md  (D-P4-4 recharts + mốc ngày VN)
@lib/billing/types.ts  (Bill, Payment — 04-01)
@app/r/[slug]/admin/(protected)/settings/page.tsx  (mẫu trang admin protected + layout)
@components/admin/AdminNav.tsx  (nav admin hiện có)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cài recharts + module tổng hợp báo cáo</name>
  <files>package.json, lib/billing/reports.ts</files>
  <read_first>lib/supabase/server.ts, lib/billing/types.ts, app/r/[slug]/admin/(protected)/settings/actions.ts (mẫu đọc tenant scope)</read_first>
  <action>npm i recharts. reports.ts (server, phiên admin RLS — chỉ tenant hiện hành): getRevenueSummary(tenantId, {from, to}) → {totalRevenue=Σ bills.total paid, billCount, avgPerBill}. getRevenueSeries(tenantId, {from, to, bucket:'day'|'week'|'month'}) → [{label/date, revenue, billCount}] gom theo (paid_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date rồi cuộn theo bucket. getTopItems(tenantId, {from, to, limit=10}) → join bill_items (bill paid) group theo name_snapshot → [{name, qty=Σqty_allocated, revenue=Σamount}] sort desc. getPaymentBreakdown(tenantId, {from, to}) → [{method, amount=Σpayments.amount, count}] cho cash/transfer. Mọi truy vấn lọc bills.status='paid' + paid_at trong [from,to] (biên theo ngày VN). Dùng RPC/SQL hoặc select + gom ở JS (dữ liệu V1 nhỏ). Chuẩn hóa from/to = đầu/cuối ngày VN.</action>
  <verify>Với 20 bill test: getRevenueSummary.totalRevenue = Σ thủ công bills.total paid; series cộng lại = summary; breakdown cash+transfer = total</verify>
  <acceptance_criteria>
    - Tổng hợp lọc bills paid + mốc ngày VN (UTC+7)
    - Doanh thu = Σ bills.total; đối chiếu 20 bill khớp 100% (BILL-05)
    - getPaymentBreakdown tách cash/transfer đúng
  </acceptance_criteria>
  <done>Số liệu báo cáo chính xác, scope tenant</done>
</task>

<task type="auto">
  <name>Task 2: Trang /admin/reports + biểu đồ</name>
  <files>app/r/[slug]/admin/(protected)/reports/page.tsx, components/admin/reports/RevenueChart.tsx, components/admin/reports/TopItemsTable.tsx, components/admin/reports/PaymentBreakdown.tsx, components/admin/reports/RangePicker.tsx, components/admin/AdminNav.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/settings/page.tsx, components/admin/AdminNav.tsx, lib/design/tokens.css</read_first>
  <design>BÁM QD-006 token (bg-canvas/surface, text-ink/steel, border-hairline, rounded-lg, shadow-card, tabular-nums). Admin desktop. Recharts: LineChart/BarChart màu token primary (#fa520f) + steel; tooltip formatVnd; responsive (ResponsiveContainer). RangePicker: nút Ngày / Tuần / Tháng (+ điều hướng kỳ trước/sau). Thẻ KPI (Doanh thu / Số bill / TB-bill) trên cùng. TopItemsTable: bảng + thanh bar CSS phần trăm. PaymentBreakdown: 2 thẻ mặt/CK + % .</design>
  <action>page.tsx (server, guard manager/owner — canManage hoặc canAccess admin): đọc searchParams (bucket, from, to; default = tháng hiện tại theo ngày VN); gọi 4 hàm reports.ts; truyền xuống client components. RevenueChart.tsx ("use client"): recharts LineChart doanh thu theo series. TopItemsTable.tsx: bảng xếp hạng + bar. PaymentBreakdown.tsx: mặt/CK. RangePicker.tsx ("use client"): đổi bucket/kỳ → cập nhật URL (router.push searchParams). AdminNav: thêm mục "Báo cáo" → /admin/reports (chỉ hiện với manager/owner). Empty state khi chưa có bill paid.</action>
  <verify>/admin/reports (owner) render KPI + biểu đồ + bảng; đổi Ngày/Tuần/Tháng cập nhật; waiter không vào được</verify>
  <acceptance_criteria>
    - KPI + biểu đồ doanh thu (recharts) + món bán chạy + tách phương thức TT
    - RangePicker ngày/tuần/tháng đổi số liệu; empty state khi trống
    - Chỉ manager/owner truy cập; recharts chỉ ở khu này
  </acceptance_criteria>
  <done>Dashboard báo cáo (REPORT-01..03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard báo cáo. Dev localhost:3000, đăng nhập owner/manager.</what-built>
  <how-to-verify>
    1. Tạo ≥20 bill paid rải 3–4 ngày (mặt + CK) qua luồng 04-04.
    2. /r/pho-viet/admin/reports (owner) → thẻ Doanh thu / Số bill / TB-bill đúng; biểu đồ doanh thu theo ngày hiển thị.
    3. Đổi Tuần / Tháng → số liệu gom lại đúng; điều hướng kỳ trước.
    4. Bảng "Món bán chạy": xếp hạng theo SL + doanh thu khớp.
    5. "Theo phương thức": tiền mặt + chuyển khoản, cộng lại = tổng doanh thu.
    6. **BILL-05:** cộng tay total của 20 bill paid = Doanh thu dashboard (khớp 100%).
    7. Đăng nhập waiter → không thấy/không vào được /admin/reports.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (số lệch, mốc ngày sai, quyền, biểu đồ vỡ).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Doanh thu ngày/tuần/tháng + số bill + TB/bill (REPORT-01), mốc ngày VN
- [ ] Món bán chạy xếp hạng SL + doanh thu (REPORT-02)
- [ ] Tách tiền mặt vs chuyển khoản (REPORT-03)
- [ ] Doanh thu dashboard = Σ bill paid, đối chiếu 20 bill khớp 100% (BILL-05)
- [ ] Chỉ manager/owner; recharts chỉ ở /admin/reports
</verification>

<success_criteria>
- Checkpoint "approved"
- REPORT-01..03 + BILL-05 chứng minh; P4 hoàn tất (dòng tiền khép kín)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P4/04-05-SUMMARY.md + cập nhật Roadmap P4 → [x]. Ghi bằng chứng đối chiếu 20 bill (bảng số).
</output>
