---
phase: 04-dong-tien
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/billing/bill.ts
  - app/r/[slug]/pos/actions.ts
  - components/pos/BillPanel.tsx
  - components/pos/AdjustBillDialog.tsx
  - components/pos/PinPrompt.tsx
autonomous: false
requirements: [BILL-03]
user_setup:
  - "1 nhân viên vai trò manager hoặc cashier có PIN (từ /admin/staff)."
  - "settings.allow_discount bật; đặt sẵn %phí/%VAT ở /admin/settings."
must_haves:
  truths:
    - "Trên bill open: áp giảm giá theo số tiền HOẶC % → tổng tính lại đúng công thức; cần PIN manager/cashier"
    - "Sửa %phí phục vụ / %VAT ngay trên bill (mặc định từ settings) → tổng cập nhật realtime trong panel"
    - "Void 1 dòng bill (bỏ khỏi hóa đơn) cần PIN manager/cashier + ghi log actor"
    - "settings.allow_discount=false → ẩn hẳn giảm giá; PIN sai/sai quyền → thông báo chung, không áp"
  artifacts:
    - "AdjustBillDialog.tsx: form giảm giá (none/amount/percent) + %phí + %VAT + xem trước tổng"
    - "PinPrompt.tsx: nhập PIN (bàn phím số) tái dùng cho giảm giá + void (giống 03-04)"
  key_links:
    - "verifyPinForRoles({allowedRoles:['manager','cashier']}) từ lib/auth/pin-gate.ts (03-04) — KHÔNG viết mới"
    - "Điều chỉnh chỉ trên bill status='open'; tính lại bằng computeBillTotals (04-01); ghi discount_type/value + pct vào bills"
    - "discount_amount kẹp ≤ subtotal; pct kẹp [0,100] (dùng clamp như settings)"
    - "Void dòng = xóa bill_items (order_item vẫn còn — chỉ bỏ khỏi bill này); trả suất về 'chưa phân bổ'"
---

<objective>
Điều chỉnh bill trước khi thu: giảm giá (số tiền/%), sửa %phí phục vụ + %VAT theo tenant, void dòng — các thao tác giảm tiền cần PIN manager/cashier.

Purpose: Bằng chứng BILL-03 (điều chỉnh + công thức). Kiểm soát thất thoát bằng PIN gate (QD-007 D-P4-3).
Output: Dialog điều chỉnh + PIN prompt tái dùng, action tính lại tổng.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.5 discount_type/value + công thức)
@docs/15-QuyetDinh/QD-005-KienTrucKyThuat.md  (D9 PIN gate thao tác nhạy cảm)
@docs/15-QuyetDinh/QD-007-DongTienP4.md  (D-P4-3)
@lib/auth/pin-gate.ts  (verifyPinForRoles — tái dùng)
@lib/billing/compute.ts  (computeBillTotals)
@lib/billing/bill.ts  (getBillView, openBillForSession — 04-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Action điều chỉnh + PIN gate</name>
  <files>lib/billing/bill.ts, app/r/[slug]/pos/actions.ts</files>
  <read_first>lib/auth/pin-gate.ts, app/r/[slug]/pos/actions.ts (mẫu action hủy món 03-04), lib/tenant/settings.ts (clampPct)</read_first>
  <action>bill.ts thêm: applyBillAdjustment(tenant, billId, {discountType, discountValue, serviceChargePct, vatPct}) — chỉ khi bill 'open'; clamp pct [0,100], discountValue ≥0; tính lại bằng computeBillTotals; UPDATE bills (discount_type/value/amount, service_charge_pct/amount, vat_pct/amount, total, updated_at). voidBillItem(tenant, billId, billItemId) — chỉ bill 'open'; DELETE bill_items → order_item về "chưa phân bổ" (openBillForSession sau sẽ không tự thêm lại nếu không gọi; ghi chú: void = cố ý bỏ khỏi hóa đơn); tính lại totals. actions.ts: applyDiscountAction(slug, billId, payload, membershipId, pin) — verifyPinForRoles({tenant, membershipId, pin, allowedRoles:['manager','cashier']}); ok=false → trả error chung, KHÔNG áp; ok=true → applyBillAdjustment (phần discount) + log actor (created_by/updated_by nếu có cột, hoặc note). setChargePctAction(slug, billId, {serviceChargePct, vatPct}) — sửa phí/VAT KHÔNG cần PIN (không phải giảm tiền trực tiếp — nhưng nếu muốn siết, để cùng gate; theo QD chỉ giảm giá + void cần PIN). voidBillItemAction(slug, billId, billItemId, membershipId, pin) — PIN gate như trên. Kiểm settings.allow_discount trước khi cho áp giảm giá (false → trả error).</action>
  <verify>Áp giảm 10% có PIN đúng → tổng giảm đúng; PIN sai → không đổi + lỗi chung; allow_discount=false → chặn</verify>
  <acceptance_criteria>
    - Giảm giá + void: verifyPinForRoles(['manager','cashier']); sai → không áp
    - Tính lại tổng bằng computeBillTotals; ghi đủ discount_*/pct/amount
    - allow_discount=false ẩn/chặn giảm giá
  </acceptance_criteria>
  <done>Điều chỉnh bill an toàn có kiểm soát</done>
</task>

<task type="auto">
  <name>Task 2: PinPrompt + AdjustBillDialog</name>
  <files>components/pos/PinPrompt.tsx, components/pos/AdjustBillDialog.tsx, components/pos/BillPanel.tsx</files>
  <read_first>components/pos/BillPanel.tsx (04-01), thành phần PIN của 03-04 (hủy món) nếu có — tái dùng</read_first>
  <design>BÁM QD-006 token; POS nhẹ (không vaul/motion). PinPrompt = center modal + bàn phím số on-screen (khớp yêu cầu P3 "nhập bằng bàn phím" + nút số), ô ẩn PIN, nút Xác nhận/Hủy; nhận allowedRolesLabel để hiển thị "PIN quản lý/thu ngân". AdjustBillDialog = center modal: chọn kiểu giảm (Không / Số tiền / %), ô nhập giá trị, 2 ô %phí + %VAT (default settings), khối xem trước 4 dòng tổng cập nhật khi gõ (dùng computeBillTotals client-side để preview). Nếu !allow_discount → ẩn phần giảm giá.</design>
  <action>PinPrompt.tsx: props (open, title, onSubmit(pin), onCancel, submitting, error); state chọn nhân viên hiện hành (membershipId) như 03-04 nếu cần + PIN. AdjustBillDialog.tsx: preview totals bằng computeBillTotals; "Lưu": nếu có thay đổi giảm giá/void → mở PinPrompt trước → applyDiscountAction; nếu chỉ đổi %phí/%VAT → setChargePctAction thẳng. BillPanel: nút "Giảm giá / Điều chỉnh" mở AdjustBillDialog; mỗi dòng có nút "Bỏ" (void) → PinPrompt → voidBillItemAction. Sau mỗi action refresh panel.</action>
  <verify>Preview tổng đổi khi gõ; áp giảm cần PIN; void dòng cần PIN; đổi %phí/VAT không cần PIN; panel cập nhật</verify>
  <acceptance_criteria>
    - PinPrompt bàn phím số, thông báo lỗi chung khi sai
    - AdjustBillDialog preview tổng realtime; ẩn giảm giá khi allow_discount=false
    - Void dòng + giảm giá gọi PIN; đổi phí/VAT không cần PIN
  </acceptance_criteria>
  <done>Thu ngân/quản lý điều chỉnh bill (BILL-03)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Điều chỉnh bill + PIN gate. Dev localhost:3000, POS.</what-built>
  <how-to-verify>
    1. Bàn có bill open. Panel → "Giảm giá / Điều chỉnh" → chọn "%" 10% → preview tổng giảm ngay → Lưu → PinPrompt.
    2. Nhập PIN nhân viên vai trò waiter (sai quyền) → "PIN hoặc quyền không hợp lệ", tổng KHÔNG đổi.
    3. Nhập PIN manager/cashier đúng → giảm giá áp, tổng đúng công thức (VAT tính trên đã giảm).
    4. Đổi %VAT 8→10 (không giảm giá) → Lưu → cập nhật KHÔNG cần PIN.
    5. Dòng "Trà đá" → "Bỏ" → PinPrompt manager → dòng biến mất, tổng giảm.
    6. /admin/settings tắt "Cho phép giảm giá" → panel không còn ô giảm giá.
    7. (DB) bills.discount_type/value/amount + vat_amount đúng; void → bill_items dòng đó mất.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (PIN không chặn, tổng sai, allow_discount không ẩn).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Giảm giá amount/% tính đúng; VAT trên (subtotal-giảm+phí)
- [ ] Giảm giá + void cần PIN manager/cashier; sai → không áp + lỗi chung
- [ ] Sửa %phí/%VAT trên bill (default settings) cập nhật tổng
- [ ] allow_discount=false ẩn giảm giá
- [ ] Void dòng bỏ khỏi bill, tính lại tổng
</verification>

<success_criteria>
- Checkpoint "approved"
- BILL-03 chứng minh (điều chỉnh + công thức + PIN gate)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P4/04-03-SUMMARY.md (file đã đổi, bằng chứng PIN gate + công thức sau điều chỉnh).
</output>
