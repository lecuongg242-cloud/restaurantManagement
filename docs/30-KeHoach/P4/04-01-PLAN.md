---
phase: 04-dong-tien
plan: 01
type: execute
wave: 1
depends_on: ["03-02", "03-05"]
files_modified:
  - supabase/migrations/0012_bills_core.sql
  - lib/billing/types.ts
  - lib/billing/compute.ts
  - lib/billing/compute.test.ts
  - lib/billing/bill.ts
  - lib/orders/pos.ts
  - app/r/[slug]/pos/actions.ts
  - components/pos/BillPanel.tsx
  - components/pos/OrderPanel.tsx
  - components/pos/PosBoard.tsx
autonomous: false
requirements: [BILL-01, BILL-03]
user_setup:
  - "Chạy luồng P3: 1 bàn có ≥2 order đã 'Đã phục vụ' (để có order_items chốt bill)."
must_haves:
  truths:
    - "POS panel bàn có nút 'Tính tiền' → tạo/mở 1 bill gom mọi order_item (≠cancelled) của phiên bàn"
    - "Màn bill hiện danh sách món (tên/SL/đơn giá/thành tiền) + subtotal + phí phục vụ + VAT + tổng theo settings tenant"
    - "Công thức tính đúng từng đồng: subtotal→(giảm giá)→phí→VAT→tổng; số nguyên VND, khớp compute.test.ts"
    - "Gọi 'Tính tiền' lần 2 sau khi bàn gọi thêm món → bill gom thêm phần mới (idempotent, không nhân đôi)"
  artifacts:
    - "0012_bills_core.sql: bills + bill_items + payments + RLS auth_tenant_ids() + index + publication bills"
    - "lib/billing/compute.ts: computeBillTotals(input) thuần (không I/O) + compute.test.ts phủ các ca (0%, chỉ VAT, giảm amount>subtotal, làm tròn)"
    - "lib/billing/bill.ts: openBillForSession(sessionId) idempotent gom order_items chưa phân bổ"
  key_links:
    - "amount dòng bill = unit_price_snapshot (từ order_items) * qty_allocated — KHÔNG đọc lại giá menu (giá đã snapshot từ P3)"
    - "bill_no = max+1 bill trong ngày VN (giống nextKitchenNo 0011)"
    - "Bất biến: Σ qty_allocated per order_item = qty (order_item chưa hủy) — bill mặc định phân bổ trọn qty"
    - "Phí/VAT default lấy từ settings (service_charge_pct/vat_pct); discount_type mặc định 'none' (giảm giá ở 04-03)"
---

<objective>
Chốt bill gộp cả bàn: từ POS bấm "Tính tiền" → gom mọi món đã gọi của phiên bàn thành 1 bill, tính subtotal + phí phục vụ + VAT + tổng theo công thức chuẩn. Đặt schema bill/payment + module tính toán thuần cho toàn P4.

Purpose: Bằng chứng BILL-01 (bill gom mọi order của phiên) + BILL-03 phần công thức tổng; nền cho tách/gộp (04-02), điều chỉnh (04-03), thu tiền (04-04).
Output: Migration bills core, module compute thuần + test, hàm mở bill idempotent, màn BillPanel trên POS.
</objective>

<context>
@docs/10-BanThietKe/01-KyThuatChiTiet.md  (§3.5 bill/payment + công thức)
@docs/30-KeHoach/P4/00-TongQuan.md
@docs/15-QuyetDinh/QD-007-DongTienP4.md
@supabase/migrations/0008_orders_core.sql  (mẫu RLS + snapshot giá)
@supabase/migrations/0011_orders_kitchen_no.sql  (mẫu số/ngày VN)
@lib/orders/pos.ts  (getPosSnapshot — nguồn phiên bàn + order_items)
@lib/tenant/settings.ts  (service_charge_pct, vat_pct)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration bills core</name>
  <files>supabase/migrations/0012_bills_core.sql</files>
  <read_first>docs/10-BanThietKe/01-KyThuatChiTiet.md (§3.5), supabase/migrations/0008_orders_core.sql</read_first>
  <action>Tạo theo §3.5 + 00-TongQuan (Mô hình dữ liệu): bills(id uuid pk, tenant_id fk tenants on delete cascade, bill_no int, table_session_id uuid null fk table_sessions on delete set null, status text check in ('open','paid','void') default 'open', subtotal int not null default 0, discount_type text check in ('none','amount','percent') default 'none', discount_value int not null default 0, discount_amount int not null default 0, service_charge_pct int not null default 0, service_charge_amount int not null default 0, vat_pct int not null default 0, vat_amount int not null default 0, total int not null default 0, note text, created_by uuid null, closed_by uuid null, paid_at timestamptz null, created_at timestamptz default now(), updated_at timestamptz default now()). bill_items(id, tenant_id fk cascade, bill_id fk bills on delete cascade, order_item_id fk order_items on delete restrict, qty_allocated int not null check >=1, unit_price_snapshot int not null, amount int not null). payments(id, tenant_id fk cascade, bill_id fk bills on delete cascade, method text check in ('cash','transfer'), amount int not null, received_by uuid null, received_at timestamptz default now(), note text). RLS bật + policy auth_tenant_ids() cả 3 bảng (mẫu 0008). Index: bills(tenant_id,status), bills(tenant_id,table_session_id), bills(tenant_id,paid_at), bill_items(tenant_id,bill_id), bill_items(tenant_id,order_item_id), payments(tenant_id,bill_id). Cuối file: alter publication supabase_realtime add table public.bills.</action>
  <verify>supabase db push áp không lỗi; RLS bật 3 bảng; publication có bills; FK order_item_id restrict (không xóa order_item đã lên bill)</verify>
  <acceptance_criteria>
    - 3 bảng đủ cột công thức (subtotal, discount_*, service_charge_*, vat_*, total)
    - bill_items.order_item_id FK restrict; qty_allocated check >=1
    - payments.method check in (cash, transfer); publication có bills
  </acceptance_criteria>
  <done>Schema bill/payment + RLS + realtime sẵn</done>
</task>

<task type="auto">
  <name>Task 2: Module tính toán thuần + test</name>
  <files>lib/billing/types.ts, lib/billing/compute.ts, lib/billing/compute.test.ts</files>
  <read_first>lib/tenant/settings.ts, docs/30-KeHoach/P4/00-TongQuan.md (Công thức tổng)</read_first>
  <action>types.ts: Bill, BillItem, Payment, BillStatus, DiscountType, PaymentMethod + BillLineView (name, qty, unitPrice, amount) + BillTotals (subtotal, discountAmount, serviceChargeAmount, vatAmount, total). compute.ts: computeBillTotals({ lines: {amount:int}[], discountType, discountValue, serviceChargePct, vatPct }) → BillTotals thuần, KHÔNG I/O. Công thức: subtotal=Σ amount; discountAmount = amount? min(discountValue, subtotal) : percent? roundHalfUp(subtotal*discountValue/100) : 0; serviceChargeAmount = roundHalfUp((subtotal-discountAmount)*serviceChargePct/100); vatAmount = roundHalfUp((subtotal-discountAmount+serviceChargeAmount)*vatPct/100); total = subtotal-discountAmount+serviceChargeAmount+vatAmount (kẹp ≥0). Tất cả integer VND; helper roundHalfUp(n)=Math.round(n). compute.test.ts (node:test hoặc vitest theo cấu hình repo — dùng runner sẵn của P1): ca (a) 0% phí 0% VAT không giảm → total=subtotal; (b) chỉ VAT 8% → vat=round(subtotal*8/100); (c) phí 5% + VAT 8% thứ tự đúng (VAT tính trên subtotal-discount+phí); (d) giảm amount > subtotal → discount=subtotal, total=phí+VAT trên 0 =0; (e) giảm percent 10% + làm tròn lẻ; (f) subtotal=0 → mọi số =0.</action>
  <verify>npm test (hoặc runner repo) xanh; các ca (a)–(f) pass; không phụ thuộc DB</verify>
  <acceptance_criteria>
    - computeBillTotals thuần, deterministic, integer VND
    - Thứ tự áp: giảm giá → phí (trên đã giảm) → VAT (trên đã giảm + phí)
    - Test phủ giảm amount>subtotal, làm tròn, subtotal=0
  </acceptance_criteria>
  <done>Lõi tính tiền có test — chống lệch doanh thu</done>
</task>

<task type="auto">
  <name>Task 3: Mở bill idempotent từ phiên bàn</name>
  <files>lib/billing/bill.ts, lib/orders/pos.ts</files>
  <read_first>lib/orders/pos.ts (getPosSnapshot), lib/orders/create-order.ts (mẫu nextKitchenNo), lib/tenant/settings.ts</read_first>
  <action>bill.ts (server, phiên station RLS — KHÔNG service role, thao tác nội bộ tenant): nextBillNo(tenantId) = max(bill_no)+1 của bill created hôm nay theo giờ VN (mẫu nextKitchenNo 0011). openBillForSession(tenantId, sessionId, actorMembershipId): (1) lấy order_items (status≠'cancelled') của mọi orders thuộc session; (2) trừ đi order_item_id đã có trong bill_items của bill status in ('open','paid') của tenant → còn "phần chưa phân bổ"; (3) tìm bill 'open' hiện có của session: có → chỉ INSERT bill_items cho phần chưa phân bổ (qty_allocated=qty, unit_price_snapshot, amount=unit*qty); không có → tạo bills(status open, bill_no, table_session_id=sessionId, service_charge_pct/vat_pct từ settings, created_by=actor) rồi INSERT bill_items; (4) tính lại totals bằng computeBillTotals → UPDATE bills. Trả bill_id. getBillView(tenantId, billId): trả Bill + lines (join bill_items→order_items name_snapshot/qty_allocated/unit) + totals + payments. Thêm vào pos.ts: getPosSnapshot đính kèm openBillId cho mỗi phiên có bill 'open' (để panel hiện "Xem bill" vs "Tính tiền").</action>
  <verify>Gọi openBillForSession 2 lần (giữa 2 lần thêm 1 order mới) → bill_items = tổng phần, không nhân đôi; totals = compute; bill_no tăng theo ngày</verify>
  <acceptance_criteria>
    - Idempotent: order_item chỉ vào 1 bill_items (không trùng)
    - Bill mặc định phân bổ trọn qty mỗi order_item; totals khớp compute.ts
    - service_charge_pct/vat_pct lấy từ settings tại lúc tạo bill
  </acceptance_criteria>
  <done>Mở/đồng bộ bill từ phiên bàn an toàn</done>
</task>

<task type="auto">
  <name>Task 4: Server action + BillPanel trên POS</name>
  <files>app/r/[slug]/pos/actions.ts, components/pos/BillPanel.tsx, components/pos/OrderPanel.tsx, components/pos/PosBoard.tsx</files>
  <read_first>app/r/[slug]/pos/actions.ts (mẫu action + auth), components/pos/OrderPanel.tsx, components/pos/PosBoard.tsx</read_first>
  <design>BÁM design system QD-006 (Mistral) — token Tailwind có sẵn (bg-primary #fa520f, bg-surface/canvas, text-ink/steel, border-hairline, rounded-lg, shadow-card, space-*, tabular-nums cho số tiền). POS giữ nhẹ (KHÔNG vaul/motion — đó chỉ cho khách). BillPanel là panel/dialog desktop-tablet: danh sách dòng (tên · SL × đơn giá · thành tiền), khối tổng (Tạm tính / Phí phục vụ x% / VAT x% / **TỔNG** đậm), nút "Thu tiền" (mờ — bật ở 04-04). formatVnd dùng lại từ lib/orders/cart.</design>
  <action>actions.ts: openBillAction(slug, sessionId) — auth phiên station canAccess('pos'), resolve tenant, gọi openBillForSession(tenant, session, membershipId) → trả billId (revalidate POS). getBillAction(slug, billId) → getBillView (hoặc load trong server component). OrderPanel.tsx: khi phiên có món (đã phục vụ/đang phục vụ), thêm nút "Tính tiền" (nếu chưa có openBillId) / "Xem bill #{bill_no}" (nếu có) → mở BillPanel. BillPanel.tsx (client): nhận billView; render dòng món + khối tổng (computeBillTotals hoặc totals server trả); nút "Thu tiền" disabled kèm chú thích "(04-04)"; nút "Đóng". PosBoard.tsx: state billOpen + billView, nối action mở/đóng panel; realtime bills đã trong publication → khi bill đổi (2 thu ngân) refresh (dùng cơ chế postgres_changes + setAuth như order ở P3).</action>
  <verify>Bấm "Tính tiền" ở bàn có 2 order phục vụ → panel hiện đủ món + tổng đúng; thêm order mới rồi bấm lại → panel gom thêm; số tiền tabular-nums không lệch</verify>
  <acceptance_criteria>
    - "Tính tiền" tạo/mở bill; panel hiện đủ món + subtotal/phí/VAT/tổng
    - Panel bám token QD-006, POS không kéo thêm vaul/motion
    - Nút "Thu tiền" chừa chỗ 04-04 (disabled + nhãn)
  </acceptance_criteria>
  <done>Thu ngân chốt bill gộp cả bàn (BILL-01)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Chốt bill gộp cả bàn + công thức tổng. Dev localhost:3000, đăng nhập POS.</what-built>
  <how-to-verify>
    1. Chạy luồng P3: gọi 2 order ở bàn B1, POS duyệt → "Đã phục vụ" cả 2.
    2. /admin/settings đặt Phí phục vụ 5%, VAT 8%, lưu.
    3. POS → panel bàn B1 → "Tính tiền" → BillPanel hiện đủ món 2 order, Tạm tính = Σ (đơn giá×SL); Phí 5% + VAT 8% + TỔNG tính đúng (đối chiếu tay).
    4. Gọi thêm 1 order ở B1 → phục vụ → mở bill lại: món mới xuất hiện, tổng cập nhật, KHÔNG nhân đôi món cũ.
    5. (DB) 1 bill 'open' cho phiên; mỗi order_item có đúng 1 bill_items; bill_no là số ngày.
    6. Chạy `npm test` → compute.test.ts xanh.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (tổng lệch, nhân đôi món, panel vỡ).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0012 áp không lỗi; RLS 3 bảng; publication có bills
- [ ] computeBillTotals thuần + compute.test.ts xanh (giảm/phí/VAT/làm tròn)
- [ ] "Tính tiền" gom mọi order_item phiên bàn thành 1 bill (BILL-01)
- [ ] Công thức subtotal→phí→VAT→tổng đúng từng đồng (BILL-03 phần công thức)
- [ ] Mở bill lần 2 idempotent (không nhân đôi món)
</verification>

<success_criteria>
- Checkpoint "approved"
- BILL-01 + công thức tổng chứng minh được; schema bill sẵn cho 04-02..05
</success_criteria>

<output>
Tạo docs/30-KeHoach/P4/04-01-SUMMARY.md (file đã đổi, trạng thái cam kết, bằng chứng + kết quả compute.test.ts).
</output>
