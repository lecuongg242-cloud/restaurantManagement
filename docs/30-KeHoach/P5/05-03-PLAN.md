---
phase: 05-kenh-online
plan: 03
type: execute
wave: 3
depends_on: ["05-02", "04-04", "04-05"]
files_modified:
  - lib/billing/bill.ts
  - lib/billing/reports.ts
  - app/r/[slug]/admin/(protected)/online/actions.ts
  - components/admin/online/OnlineQueue.tsx
  - components/admin/online/OnlineBillPanel.tsx
  - lib/orders/broadcast.ts
autonomous: false
requirements: [ONLINE-01]
user_setup:
  - "Chạy 05-02: có ≥1 đơn online đã 'Nhận đơn' và bếp đưa tới 'ready'. Settings đặt %phí/%VAT (P2)."
must_haves:
  truths:
    - "Đơn online 'ready' → nhân viên 'Thu tiền' → openBillForOrder gom order_items thành 1 bill (session null, online_order_id) → PaymentDialog (mặt/CK) → bill paid + đơn 'completed' + món 'served'"
    - "In hóa đơn 80mm cho đơn online (tên NH, kênh, contact/địa chỉ, món, phí/VAT, tổng, phương thức)"
    - "Doanh thu dashboard tính CẢ đơn online (bill paid có online_order_id), không đếm trùng"
    - "Khách theo dõi thấy 'Hoàn tất' sau khi thu tiền; đơn rời hàng đợi /admin/online"
  artifacts:
    - "lib/billing/bill.ts: openBillForOrder(orderId) (1 đơn=1 bill, không tách) + payBill đặt đơn online completed"
    - "reports.ts: doanh thu gồm bill online; kiểm không loại nhầm (session null nhưng online_order_id not null)"
    - "OnlineBillPanel tái dùng PaymentDialog + printReceipt (04-04)"
  key_links:
    - "openBillForOrder: bill.table_session_id=null, online_order_id=orderId; computeBillTotals với %phí/%VAT settings; KHÔNG tách/gộp/chia đều"
    - "payBill (dùng lại 04-04) mở rộng: bill có online_order_id paid → set order status='completed' + order_items 'served'"
    - "reports.ts loại vỏ chia đều bằng split_count IS NULL — bill online split_count null nên TỰ vào; chỉ cần chắc không lọc theo table_session_id"
    - "ReceiptDoc (04-04) hiện nhãn kênh + contact thay 'Bàn' cho đơn online"
---

<objective>
Hoàn tất vòng đời đơn online: từ 'ready' → thu tiền (tái dùng bill P4, 1 đơn = 1 bill) → in hóa đơn → đơn 'completed' → vào doanh thu dashboard. Đóng ONLINE-01.

Purpose: Bằng chứng ONLINE-01 phần cuối (vòng đời tới hoàn tất + tiền + doanh thu khớp cả kênh online).
Output: openBillForOrder, payBill mở rộng completed, doanh thu gồm online, panel thu tiền + in hóa đơn ở /admin/online.
</objective>

<context>
@docs/30-KeHoach/P5/00-TongQuan.md
@docs/15-QuyetDinh/QD-008-KenhOnlineP5.md
@lib/billing/bill.ts  (openBillForSession + payBill + closeSessionIfSettled — mẫu để thêm openBillForOrder)
@lib/billing/compute.ts  (computeBillTotals — dùng lại)
@lib/billing/reports.ts  (doanh thu 04-05 — mở rộng gồm online)
@lib/billing/receipt-view.ts  (buildReceiptView — hóa đơn 04-04)
@components/pos/PaymentDialog.tsx  (thu mặt/CK — dùng lại)
@components/print/ReceiptDoc.tsx  (hóa đơn 80mm — dùng lại)
@app/r/[slug]/admin/(protected)/online/actions.ts  (từ 05-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: openBillForOrder + payBill hoàn tất đơn</name>
  <files>lib/billing/bill.ts</files>
  <read_first>lib/billing/bill.ts (openBillForSession, payBill, closeSessionIfSettled), lib/billing/compute.ts, lib/tenant/settings.ts</read_first>
  <action>openBillForOrder(tenantId, orderId, actorMembershipId): lấy order (phải channel≠dine_in, tenant khớp); nếu đã có bill (open|paid) với online_order_id=orderId → trả bill đó (idempotent); else gom order_items (≠cancelled) của order → tạo bills(status open, bill_no=nextBillNo, table_session_id=null, online_order_id=orderId, service_charge_pct/vat_pct từ settings, created_by=actor) + bill_items trọn qty (unit_price_snapshot, amount) → computeBillTotals → update bills. KHÔNG tách/gộp. payBill (mở rộng): sau khi set bill paid + ghi payments, NẾU bill.online_order_id không null → update orders set status='completed' (+ order_items của order sang 'served'); broadcast order:{id} 'completed'. (Đơn dine-in giữ nhánh cũ closeSessionIfSettled.)</action>
  <verify>openBillForOrder tạo 1 bill/đơn, gọi lại không nhân đôi; payBill đặt đơn completed + món served; totals = compute (phí/VAT settings)</verify>
  <acceptance_criteria>
    - openBillForOrder idempotent, session null, online_order_id set, không tách
    - payBill đơn online → order completed + món served + broadcast
  </acceptance_criteria>
  <done>Thu tiền đơn online qua luồng bill P4</done>
</task>

<task type="auto">
  <name>Task 2: Panel thu tiền + in hóa đơn ở /admin/online</name>
  <files>app/r/[slug]/admin/(protected)/online/actions.ts, components/admin/online/OnlineQueue.tsx, components/admin/online/OnlineBillPanel.tsx</files>
  <read_first>components/pos/PaymentDialog.tsx, components/pos/BillPanel.tsx (mẫu render bill + PaymentDialog), lib/billing/receipt-view.ts, lib/print/adapter.ts (printReceipt)</read_first>
  <design>BÁM QD-006 khu admin (desktop). OnlineBillPanel giống BillPanel POS (danh sách món + Tạm tính/Phí/VAT/TỔNG, tabular-nums) nhưng KHÔNG nút tách/điều chỉnh (đơn online 1 bill). Đầu panel: nhãn kênh + contact (tên/SĐT/địa chỉ nếu giao).</design>
  <action>actions.ts: openOnlineBillAction(slug, orderId) → auth admin, openBillForOrder → billView; payOnlineBillAction(slug, billId, method, cashReceived?) → payBill (dùng lại payBillAction logic 04-04) → trả tiền thối. OnlineQueue.tsx: đơn 'ready' bật nút "Thu tiền & hoàn tất" → mở OnlineBillPanel. OnlineBillPanel.tsx (client): render billView + PaymentDialog (dùng lại) → thu mặt/CK → sau paid: nút "In hóa đơn" (getPrintAdapter().printReceipt) + "Xong"; đơn chuyển 'completed' rời cột. ReceiptDoc/buildReceiptView: khi bill có online_order_id → hiện nhãn kênh (Mang về/Giao) + contact thay dòng "Bàn".</action>
  <verify>Đơn ready → "Thu tiền & hoàn tất" → panel tổng đúng (phí/VAT) → thu mặt (thối đúng) → paid → in hóa đơn 80mm đủ mục (kênh+contact) → đơn completed rời hàng đợi</verify>
  <acceptance_criteria>
    - Thu tiền đơn online (mặt/CK) → completed; hóa đơn 80mm có kênh + contact
    - Panel không có tách/điều chỉnh (1 đơn = 1 bill)
  </acceptance_criteria>
  <done>ONLINE-01: hoàn tất + hóa đơn</done>
</task>

<task type="auto">
  <name>Task 3: Doanh thu gồm đơn online</name>
  <files>lib/billing/reports.ts</files>
  <read_first>lib/billing/reports.ts (query doanh thu 04-05 + lọc split_count)</read_first>
  <action>Rà soát reports.ts: doanh thu = Σ bills paid trong kỳ (ngày VN) với split_count IS NULL. Đảm bảo KHÔNG lọc theo table_session_id (đơn online session null) — nếu có điều kiện loại session null thì bỏ. Món bán chạy: join bill_items → order_items gồm cả đơn online (đếm 1 lần). Theo phương thức TT: payments gồm cả đơn online. Không đổi công thức, chỉ mở rộng phạm vi. Nếu cần tách "tại chỗ vs online" thì thêm cột phụ (tùy chọn, không bắt buộc V1).</action>
  <verify>Tạo 1 bill dine-in + 1 bill online cùng ngày → doanh thu = tổng cả 2; món online vào "bán chạy"; payments online vào "theo phương thức"</verify>
  <acceptance_criteria>
    - Doanh thu + món bán chạy + phương thức TT gồm cả đơn online, không đếm trùng
    - Không loại nhầm bill session null (online)
  </acceptance_criteria>
  <done>BILL-05 mở rộng: doanh thu khớp cả kênh online</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hoàn tất đơn online: thu tiền (bill P4) + hóa đơn + vào doanh thu. Đóng ONLINE-01. Dev localhost:3000.</what-built>
  <how-to-verify>
    1. Chạy 05-02: 1 đơn online (mang về) đã Nhận đơn, bếp đưa tới "ready".
    2. (Owner) /admin/online → đơn ready → "Thu tiền & hoàn tất" → panel hiện món + Tạm tính/Phí/VAT/TỔNG đúng.
    3. Thu Tiền mặt (khách đưa dư) → tiền thối đúng → bill paid; đơn chuyển "Hoàn tất", rời hàng đợi.
    4. Trang theo dõi khách → "Hoàn tất" (realtime).
    5. "In hóa đơn" → PDF 80mm: tên NH, "Mang về", tên/SĐT khách, món+giá, Phí/VAT, TỔNG, phương thức. Không tràn.
    6. (Delivery) đơn giao → hóa đơn hiện địa chỉ.
    7. /admin/reports → doanh thu hôm nay GỒM đơn online vừa thu; đối chiếu tay khớp; món online vào "bán chạy"; payments vào "theo phương thức".
    8. (Owner Bún Bò) không thấy bill/đơn Phở Việt.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (đơn không completed, doanh thu thiếu online, hóa đơn tràn/thiếu contact).</resume-signal>
</task>

</tasks>

<verification>
- [ ] openBillForOrder idempotent (1 đơn=1 bill, session null, online_order_id)
- [ ] payBill đơn online → completed + món served + broadcast
- [ ] Hóa đơn 80mm đơn online đủ mục (kênh + contact); doanh thu gồm online không trùng (ONLINE-01)
- [ ] RLS cách ly tenant
</verification>

<success_criteria>
- Checkpoint "approved"
- ONLINE-01 đóng trọn: vòng đời tới hoàn tất + thu tiền + doanh thu khớp cả online
</success_criteria>

<output>
Tạo docs/30-KeHoach/P5/05-03-SUMMARY.md (file đã đổi, trạng thái cam kết, bằng chứng).
Cập nhật docs/30-KeHoach/P5/00-TongQuan.md (tick nghiệm thu) + Roadmap P5 (đánh dấu hoàn tất khi 3 plan approved).
</output>
