---
phase: 05-kenh-online
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - supabase/migrations/0014_reservations.sql
  - lib/reservations/types.ts
  - lib/reservations/reservations.ts
  - app/r/[slug]/(customer)/reserve/page.tsx
  - app/r/[slug]/(customer)/reserve/actions.ts
  - components/reserve/ReservationForm.tsx
  - app/r/[slug]/admin/(protected)/reservations/page.tsx
  - app/r/[slug]/admin/(protected)/reservations/actions.ts
  - components/admin/reservations/ReservationList.tsx
  - lib/auth/rbac.ts
  - components/admin/AdminNav.tsx
autonomous: false
requirements: [RESV-01, RESV-02]
user_setup:
  - "Không cần dữ liệu trước; chỉ cần 1 tenant (pho-viet) đã có sẵn (P1)."
must_haves:
  truths:
    - "Khách mở /r/[slug]/reserve (không đăng nhập) gửi đặt bàn (tên, SĐT, số người, ngày giờ) → tạo reservation status 'pending'"
    - "Quản lý mở /admin/reservations thấy danh sách theo NGÀY (mặc định hôm nay, giờ VN) + điều hướng ◀▶ ngày"
    - "Quản lý xác nhận → 'confirmed'; từ chối (bắt buộc lý do) → 'rejected'; badge đếm 'pending'"
    - "Cách ly tenant: owner tenant khác không thấy reservation của tenant này (RLS)"
  artifacts:
    - "0014_reservations.sql: reservations + RLS auth_tenant_ids() + index (tenant_id,reserved_at) & (tenant_id,status) + publication"
    - "lib/reservations/reservations.ts: createReservation (service role, scope slug) + listReservationsByDay + decideReservation"
    - "Form khách validate SĐT + ngày giờ tương lai; admin list nhóm theo trạng thái, giờ VN"
  key_links:
    - "Khách anon ghi qua SERVICE ROLE scope theo slug (D15) — KHÔNG policy anon trên reservations"
    - "reserved_at lưu UTC, hiển thị + gom nhóm theo NGÀY VIỆT NAM (UTC+7) như dashboard 04-05"
    - "area_id tùy chọn (gợi ý khu vực), KHÔNG giữ bàn/mở phiên (QD-008 D-P5-2)"
---

<objective>
Kênh đặt bàn online: khách gửi yêu cầu đặt bàn (không đăng nhập) → quản lý duyệt/từ chối + xem danh sách theo ngày. Bảng `reservations` độc lập, không đụng order/bill.

Purpose: Bằng chứng RESV-01 (khách gửi đặt bàn → pending) + RESV-02 (duyệt + danh sách theo ngày).
Output: Migration reservations, lib service role tạo/duyệt/list, form khách, màn admin duyệt, bật nav "Đặt bàn".
</objective>

<context>
@docs/30-KeHoach/P5/00-TongQuan.md
@docs/15-QuyetDinh/QD-008-KenhOnlineP5.md
@supabase/migrations/0008_orders_core.sql   (mẫu RLS auth_tenant_ids + realtime)
@supabase/migrations/0007_areas_tables.sql   (bảng areas cho area_id)
@lib/orders/create-order.ts   (mẫu ghi service role scope slug — D15)
@lib/orders/customer-menu.ts  (getPublicTenant — resolve slug công khai)
@app/r/[slug]/admin/(protected)/settings/page.tsx  (mẫu trang admin protected + guard)
@components/admin/AdminNav.tsx  (2 mục "chờ" reservations/online)
@lib/billing/reports.ts  (mẫu gom nhóm theo ngày VN UTC+7)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration reservations</name>
  <files>supabase/migrations/0014_reservations.sql</files>
  <read_first>supabase/migrations/0008_orders_core.sql (RLS + publication), 0007_areas_tables.sql</read_first>
  <action>Tạo reservations(id uuid pk default gen_random_uuid(), tenant_id uuid not null references tenants on delete cascade, customer_name text not null, customer_phone text not null, party_size int not null check (party_size >= 1), reserved_at timestamptz not null, note text, status text not null default 'pending' check (status in ('pending','confirmed','rejected','cancelled')), area_id uuid references areas on delete set null, decided_by uuid, decided_at timestamptz, reject_reason text, created_at timestamptz default now(), updated_at timestamptz default now()). RLS bật + policy reservations_tenant_all for all using/with check (tenant_id in (select auth_tenant_ids())) — mẫu 0008. Index: idx_reservations_tenant_reserved on (tenant_id, reserved_at); idx_reservations_tenant_status on (tenant_id, status). Cuối file: alter publication supabase_realtime add table public.reservations.</action>
  <verify>supabase db push áp không lỗi; RLS bật; publication có reservations; check status + party_size hợp lệ</verify>
  <acceptance_criteria>
    - reservations đủ cột (contact, party_size, reserved_at, status, area_id, decided_*)
    - RLS auth_tenant_ids() cả đọc/ghi; 2 index; publication có bảng
  </acceptance_criteria>
  <done>Schema đặt bàn + RLS + realtime sẵn</done>
</task>

<task type="auto">
  <name>Task 2: Lib reservations (service role tạo + admin duyệt/list)</name>
  <files>lib/reservations/types.ts, lib/reservations/reservations.ts</files>
  <read_first>lib/orders/create-order.ts (createQrOrder — mẫu service role scope slug), lib/orders/customer-menu.ts (getPublicTenant), lib/billing/reports.ts (ngày VN)</read_first>
  <action>types.ts: Reservation, ReservationStatus. reservations.ts (server-only):
    (a) createReservation({slug, customerName, customerPhone, partySize, reservedAt, note?, areaId?}) — SERVICE ROLE (createAdminClient), resolve tenant qua slug (getPublicTenant); validate: tên + SĐT không rỗng, partySize 1..50, reservedAt parse được + ở TƯƠNG LAI (không cho đặt quá khứ); nếu areaId có thì phải thuộc tenant. Insert status 'pending'. Trả {reservationId} | {error}.
    (b) listReservationsByDay(tenantId, dayVN: 'YYYY-MM-DD') — phiên admin RLS; tính [startUtc, endUtc) của ngày VN (UTC+7, mẫu reports.ts) → select reservations reserved_at trong khoảng, order reserved_at asc; trả kèm tên khu vực (join areas) + đếm theo status.
    (c) decideReservation({tenantId, reservationId, decision: 'confirmed'|'rejected', decidedBy, rejectReason?}) — phiên admin RLS; update status + decided_by + decided_at (+reject_reason nếu rejected; bắt buộc reason khi rejected). Chỉ đổi được từ 'pending'.</action>
  <verify>createReservation từ chối ngày quá khứ + partySize sai; listReservationsByDay gom đúng ngày VN (test 1 mốc sát nửa đêm); decide chỉ từ pending</verify>
  <acceptance_criteria>
    - createReservation service role scope slug, validate tương lai + partySize
    - listReservationsByDay theo ngày VN (UTC+7), kèm đếm status
    - decideReservation: pending → confirmed|rejected (rejected bắt buộc lý do)
  </acceptance_criteria>
  <done>Lõi đặt bàn: tạo (khách) + duyệt/list (admin)</done>
</task>

<task type="auto">
  <name>Task 3: Form khách /reserve + submit</name>
  <files>app/r/[slug]/(customer)/reserve/page.tsx, app/r/[slug]/(customer)/reserve/actions.ts, components/reserve/ReservationForm.tsx</files>
  <read_first>app/r/[slug]/(customer)/page.tsx (header khách + tên/logo tenant), components/ui/input.tsx, components/ui/submit-button.tsx</read_first>
  <design>BÁM design system QD-006 bề mặt KHÁCH (editorial): font-display cho tiêu đề, thẻ kem, nút bg-primary, mobile-first ≤360px không vỡ (như menu P3). Field: Tên*, SĐT*, Số người* (stepper hoặc number), Ngày giờ* (input datetime-local, min = hiện tại), Ghi chú, (tùy chọn) Khu vực. Sau gửi: màn cảm ơn "Đã gửi yêu cầu đặt bàn — nhà hàng sẽ liên hệ xác nhận".</design>
  <action>page.tsx (server): header tên+logo tenant (dùng lại mẫu (customer)/page.tsx); render ReservationForm; searchParams ok/error. actions.ts: submitReservation(formData) — server action, gọi createReservation(slug,...); redirect ?ok / ?error. ReservationForm.tsx (client): các field trên, validate client nhẹ (SĐT số, ngày ≥ nay), disable nút khi đang gửi (SubmitButton). Khu vực: nếu truyền danh sách areas thì hiện select (không bắt buộc).</action>
  <verify>Gửi đơn hợp lệ → reservation 'pending' trong DB + màn cảm ơn; ngày quá khứ / thiếu SĐT bị chặn; 360px không vỡ</verify>
  <acceptance_criteria>
    - Khách (không đăng nhập) gửi đặt bàn → pending; màn cảm ơn
    - Validate: SĐT, ngày giờ tương lai, số người ≥1; mobile 360px
    - Bám token khách QD-006 (editorial), không lộ dữ liệu tenant khác
  </acceptance_criteria>
  <done>RESV-01: khách gửi đặt bàn → pending</done>
</task>

<task type="auto">
  <name>Task 4: Màn admin /reservations (duyệt + list theo ngày) + nav + rbac</name>
  <files>app/r/[slug]/admin/(protected)/reservations/page.tsx, app/r/[slug]/admin/(protected)/reservations/actions.ts, components/admin/reservations/ReservationList.tsx, lib/auth/rbac.ts, components/admin/AdminNav.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/settings/page.tsx (guard + layout admin), components/admin/AdminNav.tsx, lib/auth/rbac.ts, lib/billing/reports.ts (ngày VN + ◀▶ kỳ)</read_first>
  <action>rbac.ts: thêm 'reservations' vào type ManageSection (canManage vẫn owner/manager). AdminNav.tsx: đổi mục 'reservations' từ span "chờ" thành Link href `${base}/reservations` (bỏ nhãn chờ). page.tsx (server, guard getSessionMembership + canManage('reservations') như settings): đọc dayVN từ searchParams (mặc định hôm nay VN); listReservationsByDay; render ReservationList + KPI đếm (pending/confirmed/rejected). actions.ts: confirmReservationAction(slug, id) + rejectReservationAction(slug, id, reason) — auth admin, gọi decideReservation, revalidate. ReservationList.tsx (client): bảng/thẻ theo giờ (giờ VN), badge trạng thái màu (pending cam / confirmed xanh / rejected xám), nút "Xác nhận" + "Từ chối" (mở ô nhập lý do) cho đơn pending; ◀ ngày ▶ (đổi query dayVN); nghe postgres_changes bảng reservations + setAuth để đơn mới pop (mẫu POS P3). Badge "pending" ở đầu trang.</action>
  <verify>Mở /admin/reservations thấy đơn hôm nay; ◀▶ đổi ngày đúng; xác nhận/từ chối đổi trạng thái + lý do; owner tenant khác không thấy (RLS)</verify>
  <acceptance_criteria>
    - Danh sách theo ngày VN + điều hướng ◀▶; đếm theo trạng thái
    - Xác nhận → confirmed; từ chối (bắt buộc lý do) → rejected
    - Nav "Đặt bàn" hoạt động; guard owner/manager; RLS cách ly tenant
  </acceptance_criteria>
  <done>RESV-02: duyệt + danh sách theo ngày</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Kênh đặt bàn: khách gửi → quản lý duyệt + danh sách theo ngày. Dev localhost:3000.</what-built>
  <how-to-verify>
    1. (Khách, cửa sổ ẩn danh) /r/pho-viet/reserve → điền Tên/SĐT/4 người/ngày mai 19:00 → Gửi → màn cảm ơn.
    2. Thử ngày quá khứ + bỏ trống SĐT → bị chặn.
    3. (Owner) /r/pho-viet/admin/reservations → thấy đơn vừa tạo ở đúng NGÀY (badge pending = 1).
    4. "Xác nhận" → chuyển confirmed; tạo đơn khác → "Từ chối" (nhập lý do) → rejected.
    5. ◀ ▶ đổi ngày → danh sách gom đúng theo ngày VN.
    6. Mở cửa sổ thứ 2 /admin/reservations → gửi 1 đơn khách mới → đơn pop realtime (không reload).
    7. (Owner Bún Bò) /r/bun-bo/admin/reservations → KHÔNG thấy đơn Phở Việt.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (đơn sai ngày, không duyệt được, rò tenant).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0014 áp không lỗi; RLS + publication reservations
- [ ] Khách gửi đặt bàn (không đăng nhập) → pending (RESV-01)
- [ ] Admin danh sách theo ngày VN + ◀▶; xác nhận/từ chối (RESV-02)
- [ ] Realtime đơn mới pop; RLS cách ly tenant
</verification>

<success_criteria>
- Checkpoint "approved"
- RESV-01 + RESV-02 chứng minh được; bảng reservations sẵn (không ảnh hưởng order/bill)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P5/05-01-SUMMARY.md (file đã đổi, trạng thái cam kết, bằng chứng).
</output>
