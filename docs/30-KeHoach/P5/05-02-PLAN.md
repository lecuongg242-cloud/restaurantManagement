---
phase: 05-kenh-online
plan: 02
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - supabase/migrations/0015_online_orders.sql
  - lib/orders/create-order.ts
  - lib/orders/online.ts
  - app/r/[slug]/(customer)/online/page.tsx
  - app/r/[slug]/(customer)/online/actions.ts
  - components/online/OnlineOrderScreen.tsx
  - components/online/ContactForm.tsx
  - app/r/[slug]/(customer)/order/[id]/page.tsx
  - lib/orders/kds.ts
  - components/kds/KdsBoard.tsx
  - app/r/[slug]/admin/(protected)/online/page.tsx
  - app/r/[slug]/admin/(protected)/online/actions.ts
  - components/admin/online/OnlineQueue.tsx
  - lib/auth/rbac.ts
  - components/admin/AdminNav.tsx
autonomous: false
requirements: [ONLINE-01]
user_setup:
  - "Menu pho-viet có ≥3 món active (P2). Đăng nhập owner để nhận đơn; station/kitchen để xem KDS."
must_haves:
  truths:
    - "Khách mở /r/[slug]/online (không đăng nhập) chọn kênh (mang về/giao), gọi món + tùy chọn, nhập tên/SĐT (+địa chỉ nếu giao) → tạo order channel≠dine_in, source='online', status pending_confirm, table_session_id NULL"
    - "Nhân viên /admin/online thấy đơn pending → 'Nhận đơn' → confirmed + kitchen_no; 'Từ chối' → cancelled (lý do)"
    - "Đơn confirmed hiện trên KDS với nhãn kênh (Mang về/Giao) + #kitchen_no (không số bàn); bếp đổi trạng thái món như dine-in"
    - "Khách theo dõi /r/[slug]/order/[id] thấy trạng thái realtime (Broadcast): Chờ xác nhận → Đang chuẩn bị → Sẵn sàng"
  artifacts:
    - "0015_online_orders.sql: orders.source thêm 'online'; index orders(tenant_id,channel,status); bills.online_order_id + index"
    - "create-order.ts: insertOrderGraph tham số hóa channel (bỏ hardcode dine_in); createOnlineOrder"
    - "KDS query gồm order confirmed session NULL (online) + nhãn kênh"
  key_links:
    - "Khách online ẩn danh qua SERVICE ROLE scope slug (D15); source='online'; luôn pending_confirm (bỏ qua qr_order_auto_send — D-P5-5)"
    - "address bắt buộc khi channel='delivery'; name+phone bắt buộc cả 2 kênh"
    - "Theo dõi khách = Broadcast order:{id} (tái dùng P3), KHÔNG postgres_changes (RLS chặn anon)"
    - "openBillForOrder + thu tiền + completed để plan 05-03 (đây chỉ tới 'ready')"
---

<objective>
Kênh đặt món online: khách đặt mang về/giao (không đăng nhập) → nhân viên nhận đơn → xuống KDS realtime → khách theo dõi trạng thái. Tái dùng menu/cart/broadcast P3; đơn không gắn bàn.

Purpose: Bằng chứng ONLINE-01 phần đầu (tạo đơn kênh online + duyệt + bếp + theo dõi). Vòng đời tới `completed` + thu tiền ở 05-03.
Output: Migration online, createOnlineOrder, bề mặt khách /online + theo dõi, KDS nhận đơn online, hàng đợi /admin/online.
</objective>

<context>
@docs/30-KeHoach/P5/00-TongQuan.md
@docs/15-QuyetDinh/QD-008-KenhOnlineP5.md
@lib/orders/create-order.ts   (insertOrderGraph hardcode dine_in — cần tham số hóa; createQrOrder mẫu)
@lib/orders/customer-menu.ts  (getCustomerMenu — menu khách)
@app/r/[slug]/(customer)/menu/page.tsx  (bề mặt gọi món QR P3 — tái dùng cart/components)
@app/r/[slug]/(customer)/order/[id]/page.tsx  (trang theo dõi đơn P3 — mở rộng cho online)
@lib/orders/kds.ts  (query KDS — thêm đơn online session null)
@lib/orders/broadcast.ts  (Broadcast order:{id})
@lib/orders/pos.ts  (mẫu duyệt order 03-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration online orders</name>
  <files>supabase/migrations/0015_online_orders.sql</files>
  <read_first>supabase/migrations/0008_orders_core.sql (orders.source check), 0012_bills_core.sql (bills)</read_first>
  <action>alter orders drop constraint check source → thêm check (source in ('qr','staff','online')) (drop + add lại constraint đúng tên hiện có). create index if not exists idx_orders_tenant_channel_status on orders (tenant_id, channel, status). alter table bills add column if not exists online_order_id uuid references orders(id) on delete set null. create index if not exists idx_bills_tenant_online_order on bills (tenant_id, online_order_id). (Không đụng RLS — orders/bills đã có policy tenant.)</action>
  <verify>db push không lỗi; insert order source='online' OK; bills.online_order_id tồn tại</verify>
  <acceptance_criteria>
    - orders.source chấp nhận 'online'; index channel/status
    - bills.online_order_id + index (dùng ở 05-03)
  </acceptance_criteria>
  <done>Schema sẵn cho đơn online + bill mỗi đơn</done>
</task>

<task type="auto">
  <name>Task 2: createOnlineOrder (tham số hóa channel)</name>
  <files>lib/orders/create-order.ts, lib/orders/online.ts</files>
  <read_first>lib/orders/create-order.ts (insertOrderGraph, createQrOrder, validateAndBuildLines)</read_first>
  <action>create-order.ts: sửa insertOrderGraph nhận thêm `channel: 'dine_in'|'takeaway'|'delivery'` (mặc định 'dine_in' để không đổi hành vi cũ) + cho `sessionId: string | null` (đơn online null) + `source` mở rộng 'online'. createOnlineOrder({slug, channel, lines, customerName, customerPhone, address?, note?}) trong online.ts (hoặc create-order.ts): SERVICE ROLE; resolve tenant qua slug; validate channel ∈ {takeaway,delivery}; name+phone bắt buộc; nếu delivery thì address bắt buộc; validateAndBuildLines (dùng lại); insertOrderGraph với sessionId=null, source='online', status='pending_confirm' (KHÔNG áp qr_order_auto_send — D-P5-5), customerContact={name,phone,address?}. Trả {orderId}|{error}. Không mở table_session.</action>
  <verify>Tạo đơn takeaway (name+phone) OK; delivery thiếu address bị chặn; order có channel đúng, session null, source online, status pending_confirm; dine_in cũ KHÔNG đổi hành vi</verify>
  <acceptance_criteria>
    - insertOrderGraph tham số hóa channel/session/source, mặc định giữ dine_in cũ
    - createOnlineOrder validate kênh + contact (+address khi delivery), pending_confirm, session null
  </acceptance_criteria>
  <done>Lõi tạo đơn online (không đụng luồng dine-in)</done>
</task>

<task type="auto">
  <name>Task 3: Bề mặt khách /online + theo dõi đơn</name>
  <files>app/r/[slug]/(customer)/online/page.tsx, app/r/[slug]/(customer)/online/actions.ts, components/online/OnlineOrderScreen.tsx, components/online/ContactForm.tsx, app/r/[slug]/(customer)/order/[id]/page.tsx</files>
  <read_first>app/r/[slug]/(customer)/menu/page.tsx (menu + cart P3), components giỏ hàng P3, app/r/[slug]/(customer)/order/[id]/page.tsx (theo dõi P3), lib/orders/broadcast.ts</read_first>
  <design>BÁM QD-006 bề mặt KHÁCH (editorial, mobile-first ≤360px, ≤6 chạm như ORDER-01). Toggle kênh trên đầu: "Mang về" / "Giao tận nơi". Tái dùng danh sách menu + giỏ + dialog tùy chọn của /menu (P3). Bước cuối: ContactForm (Tên*, SĐT*, Địa chỉ* chỉ khi Giao, Ghi chú) → "Đặt đơn". Sau đặt: điều hướng trang theo dõi.</design>
  <action>page.tsx (server): getCustomerMenu(slug) (dùng lại); header tên+logo; render OnlineOrderScreen (menu + channel). actions.ts: submitOnlineOrder(formData) → createOnlineOrder → redirect /r/[slug]/order/{orderId}. OnlineOrderScreen.tsx (client): channel toggle + tái dùng cart P3 + ContactForm; hiển thị địa chỉ chỉ khi delivery. order/[id]/page.tsx: mở rộng trang theo dõi P3 để nhận cả đơn online (channel≠dine_in) — hiển thị nhãn kênh + trạng thái map: pending_confirm="Chờ xác nhận", confirmed/preparing="Đang chuẩn bị", ready="Sẵn sàng (lấy/giao)", completed="Hoàn tất", cancelled="Đã hủy (+lý do)"; subscribe Broadcast order:{id} (dùng lại), fallback polling.</action>
  <verify>Đặt đơn mang về 6 chạm ở 360px; delivery hiện ô địa chỉ bắt buộc; sau đặt tới trang theo dõi hiện "Chờ xác nhận"</verify>
  <acceptance_criteria>
    - Khách đặt món online (mang về/giao) không đăng nhập; ≤6 chạm, 360px không vỡ
    - Địa chỉ bắt buộc khi giao; trang theo dõi hiện trạng thái realtime
  </acceptance_criteria>
  <done>ONLINE-01: khách đặt + theo dõi</done>
</task>

<task type="auto">
  <name>Task 4: KDS nhận đơn online</name>
  <files>lib/orders/kds.ts, components/kds/KdsBoard.tsx</files>
  <read_first>lib/orders/kds.ts (query KDS hiện tại), components/kds/KdsBoard.tsx</read_first>
  <action>kds.ts: mở rộng query lấy order `confirmed/preparing/ready` gồm cả `table_session_id IS NULL` (đơn online) — hiện tại giả định có bàn; thêm nhãn nguồn: nếu channel='takeaway'→"Mang về", 'delivery'→"Giao", else tên bàn. Giữ nguyên realtime postgres_changes (RLS phiên station). KdsBoard.tsx: thẻ đơn online hiện nhãn kênh (màu) + "#{kitchen_no}" thay số bàn; món ẩn khi served/cancelled (như dine-in). Không đổi cơ chế đo ≤3s.</action>
  <verify>Nhận 1 đơn online (từ Task 5) → thẻ hiện trên KDS với nhãn "Mang về #N"; đổi trạng thái món chạy; đơn dine-in vẫn hiện số bàn</verify>
  <acceptance_criteria>
    - KDS hiện đơn online (session null) kèm nhãn kênh + #kitchen_no
    - Không ảnh hưởng hiển thị/đo đơn dine-in
  </acceptance_criteria>
  <done>Bếp thấy đơn online realtime</done>
</task>

<task type="auto">
  <name>Task 5: Hàng đợi /admin/online (nhận đơn/từ chối) + nav + rbac</name>
  <files>app/r/[slug]/admin/(protected)/online/page.tsx, app/r/[slug]/admin/(protected)/online/actions.ts, components/admin/online/OnlineQueue.tsx, lib/auth/rbac.ts, components/admin/AdminNav.tsx</files>
  <read_first>app/r/[slug]/admin/(protected)/settings/page.tsx (guard), lib/orders/pos.ts (duyệt order 03-02 — confirmOrder gán kitchen_no), lib/orders/broadcast.ts, components/admin/AdminNav.tsx, lib/auth/rbac.ts</read_first>
  <action>rbac.ts: thêm 'online' vào ManageSection. AdminNav.tsx: mục 'online' thành Link `${base}/online`. page.tsx (server, guard canManage('online')): liệt kê đơn online (channel≠dine_in) theo trạng thái: chờ (pending_confirm) / đang làm (confirmed,preparing,ready) — dùng RLS phiên admin; render OnlineQueue. actions.ts: acceptOnlineOrderAction(slug, orderId) — auth admin, set order confirmed + confirmed_at/by + kitchen_no (nextKitchenNo, dùng lại) + broadcast order:{id}; rejectOnlineOrderAction(slug, orderId, reason) — set cancelled + cancel_reason + broadcast. OnlineQueue.tsx (client): cột "Chờ xác nhận" (nút Nhận đơn / Từ chối+lý do) + "Đang xử lý" (hiện trạng thái + contact/địa chỉ); nghe postgres_changes bảng orders (channel filter client) + setAuth để đơn mới pop. (Nút "Sẵn sàng → thu tiền/hoàn tất" để 05-03 — hiện disabled + nhãn "(05-03)".)</action>
  <verify>Đơn khách vừa đặt hiện ở "Chờ xác nhận"; "Nhận đơn" → confirmed + kitchen_no + xuống KDS + khách thấy "Đang chuẩn bị"; "Từ chối" → cancelled + khách thấy "Đã hủy"</verify>
  <acceptance_criteria>
    - /admin/online liệt kê đơn theo trạng thái; nhận đơn (→confirmed+kitchen_no) / từ chối (lý do)
    - Nav "Đơn online" hoạt động; guard owner/manager; broadcast cập nhật khách
    - Nút hoàn tất/thu tiền chừa chỗ 05-03 (disabled + nhãn)
  </acceptance_criteria>
  <done>ONLINE-01: nhận đơn + đẩy bếp</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Đặt món online: khách đặt → nhận đơn → KDS → theo dõi. Chưa thu tiền (05-03). Dev localhost:3000.</what-built>
  <how-to-verify>
    1. (Khách ẩn danh) /r/pho-viet/online → "Mang về" → chọn 2 món (có tùy chọn) → nhập Tên/SĐT → "Đặt đơn" → tới trang theo dõi "Chờ xác nhận".
    2. Thử "Giao tận nơi" bỏ trống địa chỉ → bị chặn.
    3. (Owner, cửa sổ 2) /r/pho-viet/admin/online → đơn hiện ở "Chờ xác nhận" (realtime).
    4. "Nhận đơn" → đơn sang "Đang xử lý" + có #số; trang theo dõi khách đổi "Đang chuẩn bị" (không reload).
    5. (Station/kitchen) /r/pho-viet/kds → thẻ "Mang về #N" hiện; đổi món preparing→ready → khách thấy "Sẵn sàng".
    6. Đặt đơn khác → /admin/online "Từ chối" (lý do) → khách thấy "Đã hủy" + lý do.
    7. (Owner Bún Bò) /admin/online → KHÔNG thấy đơn Phở Việt.
  </how-to-verify>
  <resume-signal>Gõ "approved" hoặc mô tả lỗi (đơn không lên KDS, theo dõi không cập nhật, địa chỉ không bắt buộc).</resume-signal>
</task>

</tasks>

<verification>
- [ ] Migration 0015 áp không lỗi; source 'online'; bills.online_order_id
- [ ] createOnlineOrder: channel≠dine_in, session null, pending_confirm; dine-in cũ nguyên vẹn
- [ ] Khách đặt + theo dõi realtime; địa chỉ bắt buộc khi giao (ONLINE-01)
- [ ] Nhận đơn → KDS (nhãn kênh + #số); từ chối → hủy + lý do
- [ ] RLS cách ly tenant
</verification>

<success_criteria>
- Checkpoint "approved"
- ONLINE-01 phần tạo/duyệt/bếp/theo dõi chứng minh được; sẵn cho 05-03 (hoàn tất + tiền)
</success_criteria>

<output>
Tạo docs/30-KeHoach/P5/05-02-SUMMARY.md (file đã đổi, trạng thái cam kết, bằng chứng).
</output>
