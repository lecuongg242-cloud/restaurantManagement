# BÁO CÁO TIẾN ĐỘ — P2: Dữ liệu nhà hàng

**Ngày:** 21/07/2026 · **Người thực hiện:** Thợ Xây (Claude Code) · **Vòng:** 1
**Đối chiếu:** `docs/20-DanhSachYeuCau/P2-DuLieuNhaHang.md` (ĐÃ DUYỆT v1.0, 12 tiêu chí)

## 1. Kế hoạch đã làm (3 plan)

| Plan | Nội dung | Commit |
|---|---|---|
| 02-01 | Migration 0002 (6 bảng + CHECK giá + composite FK + RLS + RPC `resolve_table_by_qr` + view `tenant_public_info` + bucket `menu-images` cấu hình 2MB/MIME); admin CRUD menu + toggle hết món; menu khách mobile-first + realtime | `87932e8` |
| 02-02 | CRUD khu vực/bàn, tạo bàn hàng loạt, QR sinh cục bộ (`qrcode`), tải PNG từng bàn, trang in tổng A4 | `330d6d4` |
| 02-03 | Wizard onboarding 4 bước (Nhà hàng → Menu → Bàn → In QR) + checklist, tự mở khi tenant trống, trang quản trị nối link | `d22917c` |

## 2. File đã tạo/sửa (chính)

- `supabase/migrations/0002_menu_tables.sql` — toàn bộ schema P2 + RLS + RPC + bucket
- `app/(admin)/r/[slug]/admin/menu/{page.tsx, actions.ts, [itemId]/page.tsx}`
- `app/(admin)/r/[slug]/admin/tables/{page.tsx, actions.ts, print/page.tsx}`
- `app/(admin)/r/[slug]/admin/onboarding/{page.tsx, actions.ts}`
- `app/(customer)/r/[slug]/page.tsx` + `app/(customer)/r/[slug]/t/[qrToken]/page.tsx`
- `components/{menu/menu-view.tsx, confirm-button.tsx, print-button.tsx}`
- `lib/{menu.ts, format.ts, qr.ts}` · `next.config.ts` (ảnh Supabase) · `app/(admin)/r/[slug]/admin/page.tsx`
- `scripts/test-rls.mjs` — thêm 4 mục P2 (~30 assertions mới), tự phát hiện thiếu migration

## 3. Bằng chứng đã có

- **Build**: `next build` PASS — 18 routes (6 route P2 mới). **Lint**: 0 lỗi. **Typecheck**: PASS.
- **Test RLS**: `npm run test:rls` trên DB dev → **P1 43/43 PASS, 0 FAIL** (không hỏng gì — vế sau tiêu chí #9 ĐẠT). Các mục P2 (5–8 trong script) tự báo "BỎ QUA: schema 0002 chưa áp" — xem mục 5.
- Đổi tên bàn giữ nguyên `qr_token` (không đụng cột này trong action `renameTable`) — QR đã in không chết.
- Anon không có bất kỳ policy nào trên `tables`/`areas`; RPC `security definer` + `set search_path`, trả tối đa 1 dòng.

## 4. Trạng thái theo 12 tiêu chí cam kết

| # | Tiêu chí | Trạng thái |
|---|---|---|
| 1 | CRUD danh mục/món đầy đủ, 2 cờ riêng | 🔎 CODE XONG — demo trình duyệt sau khi áp migration |
| 2 | Chặn ảnh tại cấu hình bucket | ⏳ CHỜ MIGRATION — cấu hình trong 0002; test tự động (3MB/gif qua API) đã viết |
| 3 | Hết món realtime ≤ 3s | 🔎 CODE XONG — publication + subscription sẵn; cần quay video 2 màn hình |
| 4 | Menu khách 4G ≤ 3s, 360px, 44px | 🔎 CODE XONG (server-render, ảnh lazy, touch 44px) — số đo chạy trên môi trường dev |
| 5 | CRUD bàn, bàn hàng loạt, QR unique, đổi tên giữ QR | 🔎 CODE XONG — `qr_token` unique ở DB; demo sau migration |
| 6 | PNG từng bàn + trang in tổng A4 | 🔎 CODE XONG — cần test in thật |
| 7 | Quét QR đúng nhà hàng/bàn; bàn xóa → trang thân thiện | 🔎 CODE XONG + test RPC tự động — demo điện thoại thật sau deploy |
| 8 | Anon không SELECT tables/areas; RPC 1/0/0 | ⏳ CHỜ MIGRATION — test tự động đã viết đủ 3 nhánh |
| 9 | RLS 6 bảng mới + P1 không hỏng | ✅ **P1 43/43 PASS** · phần 6 bảng mới ⏳ CHỜ MIGRATION |
| 10 | Composite FK 4 quan hệ + CHECK giá | ⏳ CHỜ MIGRATION — test service-role lách RLS đã viết đủ 4 FK + 2 CHECK |
| 11 | Wizard 4 bước + checklist + resume | 🔎 CODE XONG — thoát/vào lại giữ bước qua `?step` + đếm từ DB |
| 12 | TENANT-03 ≤ 15 phút (người ngoài) | ⏳ Đo thật sau khi hệ thống chạy trên dev |

**KHÔNG ĐƯỢC LÀM — tự kiểm:** ✅ không xây tính năng P3+ (menu khách chỉ xem, ghi chú "gọi món mở ở giai đoạn sau"); ✅ không service-role trong route (mọi action dùng session user + RLS; storage qua policy); ✅ không policy `USING (true)`, anon không có policy trên tables/areas; ✅ không sửa DB thủ công (migration 0002 chờ pipeline/CLI có kiểm soát); ✅ QR sinh cục bộ bằng thư viện `qrcode`; ✅ không hardcode tenant/slug/domain (origin lấy từ request headers).

## 5. Rủi ro / Chờ gì

**Chặn duy nhất: chưa áp được migration 0002 lên Supabase dev.** Máy này không có
`SUPABASE_ACCESS_TOKEN` (P1 áp migration trong phiên có đăng nhập CLI). Cần Chủ dự án một trong hai:
1. Chạy `supabase login` (hoặc cấp `SUPABASE_ACCESS_TOKEN`) → tôi chạy
   `npx supabase link --project-ref vuppmvgnmektgvicgsof && npx supabase db push`; **hoặc**
2. Tự chạy 2 lệnh trên (push sẽ áp `0002_menu_tables.sql`).

Sau khi áp: tôi chạy lại `npm run test:rls` (kỳ vọng ~73 assertions PASS toàn bộ), demo trình duyệt các tiêu chí 1/3/5/6/7/11, đo 4G, và phối hợp đo TENANT-03 với người ngoài — cập nhật báo cáo vòng 2.

## 6. Kết luận

Toàn bộ code P2 (3 plan) hoàn thành theo thiết kế v1.0 đã duyệt; build/lint/typecheck xanh; bộ test P1 không hỏng (43/43). 1/12 tiêu chí ĐẠT một nửa có bằng chứng, 6/12 code xong chờ demo, 5/12 chờ áp migration (việc cần quyền của Chủ dự án). Chưa yêu cầu "XÁC NHẬN XONG" ở vòng này.
