# BÁO CÁO TIẾN ĐỘ — P1: Nền tảng

**Ngày:** 20/07/2026 · **Người thực hiện:** Thợ Xây (Claude Code) · **Vòng:** 1
**Đối chiếu:** `docs/20-DanhSachYeuCau/P1-NenTang.md` (ĐÃ DUYỆT)

## 1. Kế hoạch đã làm (4 plan)

| Plan | Nội dung | Commit |
|---|---|---|
| 01-01 | Scaffold Next.js 16 + Tailwind 4, route groups đúng thiết kế, Supabase clients, .env.example, README | `d0516bc` |
| 01-02 | Migration `0001_core.sql` (5 bảng + RLS đầy đủ + RPC accept_invitation), bộ test RLS 40+ assertions, CI workflow | `814a39f` |
| 01-03 | Route guard 6 vai trò trong proxy, đăng nhập, /choose, luồng mời `/invite/[token]`, trang super-admin, trang nhân viên (mời/thu hồi/khóa) | `a2efc56` |
| 01-04 | Token 3 lớp (QD-002), font tiếng Việt (Be Vietnam Pro/Inter/JetBrains Mono), style-guide đầy đủ, 3 surface (khách/staff/KDS dark) | `1d40124` |

## 2. File đã tạo/sửa (chính)

`app/` (12 routes), `lib/supabase/*`, `lib/env.ts`, `proxy.ts`, `components/{copy-link,setup-notice}.tsx`, `supabase/migrations/0001_core.sql`, `scripts/test-rls.mjs`, `.github/workflows/ci.yml`, `design-system/restaurant-system/MASTER.md`, `docs/15-QuyetDinh/QD-002-DesignSystem.md`.

## 3. Bằng chứng đã có

- **Build**: `next build` pass, 12 routes (log trong phiên làm việc).
- **Lint**: ESLint 0 lỗi.
- **Smoke test server thật**: `/`, `/style-guide`, `/login`, `/r/demo/*` → 200; `/khong-ton-tai` → 404; `/choose`, `/super` khi CHƯA có env → 200 với thông báo "Chưa kết nối Supabase" (đã sửa bug 500 phát hiện qua smoke test).
- **Scan secrets**: 0 key trong source + toàn bộ git history (lệnh grep lưu trong báo cáo phiên).
- **Tiếng Việt**: style-guide render đầy đủ dấu (ăâđêôơư + 5 thanh) với font subset vietnamese.

## 4. Trạng thái theo danh sách cam kết

| # | Tiêu chí | Trạng thái |
|---|---|---|
| 1 | Chạy 3 môi trường | ⏳ CHỜ HẠ TẦNG — local ĐẠT; dev/prod cần Vercel + Supabase (mục 5) |
| 2 | Super-admin tạo tenant ≤ 2 phút | ⏳ CHỜ HẠ TẦNG — code xong, cần DB thật để quay video |
| 3 | Đăng nhập + giữ phiên | ⏳ CHỜ HẠ TẦNG — code xong (cookie SSR chuẩn @supabase/ssr) |
| 4 | Ma trận 6 tài khoản × 4 khu vực | ⏳ CHỜ HẠ TẦNG — guard đã viết đúng ma trận; test chạy khi có DB |
| 5 | RLS cách ly tuyệt đối | ⏳ CHỜ HẠ TẦNG — policies + bộ test tự động sẵn sàng (`npm run test:rls`) |
| 6 | Mời qua tenant_invitations (3 nhánh) | ⏳ CHỜ HẠ TẦNG — RPC + UI + test đủ 3 nhánh hợp lệ/hết hạn/thu hồi |
| 7 | Khóa 2 tầng ngay request kế tiếp | ⏳ CHỜ HẠ TẦNG — middleware check status + RLS `status='active'`; test có sẵn |
| 8 | Không secrets trong code/log | ✅ **ĐẠT** — scan source + git history: 0 match; .env.example không có giá trị thật |
| 9 | Style-guide đủ design system | ✅ **ĐẠT** (code + smoke test; ảnh chụp bổ sung khi bạn mở `npm run dev`) |
| 10 | Migration theo pipeline | ⏳ CHỜ HẠ TẦNG — CI workflow sẵn sàng, cần GitHub repo + secrets |

**KHÔNG ĐƯỢC LÀM — tự kiểm:** ✅ không service-role phía client (mọi mutation dùng session user, RLS enforced); ✅ không disable RLS/policy `USING(true)`; ✅ không sửa DB thủ công (chưa có DB); ✅ membership chỉ sinh qua accept_invitation; ✅ không xây UI P2+; ✅ không hardcode tenant.

## 5. Rủi ro / Chờ gì

- **Chặn chính:** chưa có hạ tầng — cần Chủ dự án tạo: (1) 2 Supabase projects (dev, prod); (2) tài khoản Vercel + import repo; (3) GitHub repo (cho CI). Máy này không có Docker nên không chạy Supabase local — local sẽ trỏ vào project dev.
- Sau khi có key dev: tôi chạy `npm run test:rls` + quay bằng chứng các tiêu chí 2–7, cập nhật báo cáo vòng 2.
- Next.js 16 đổi convention middleware→proxy (đã theo); npm audit còn 2 cảnh báo moderate ở dependencies dev — theo dõi, không chặn.

## 6. Kết luận

Toàn bộ code P1 hoàn thành theo đúng thiết kế đã duyệt; 2/10 tiêu chí ĐẠT có bằng chứng, 8/10 sẵn sàng nghiệm thu ngay khi có hạ tầng (việc chỉ Chủ dự án làm được). Chưa yêu cầu "XÁC NHẬN XONG" ở vòng này.
