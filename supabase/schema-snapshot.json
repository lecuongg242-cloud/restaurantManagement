{
 "bang": {
  "areas": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "name text not null",
   "sort_order integer not null 0",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "bill_items": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "bill_id uuid not null",
   "order_item_id uuid not null",
   "qty_allocated integer not null",
   "unit_price_snapshot integer not null",
   "amount integer not null"
  ],
  "bills": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "bill_no integer null",
   "table_session_id uuid null",
   "status text not null 'open'::text",
   "subtotal integer not null 0",
   "discount_type text not null 'none'::text",
   "discount_value integer not null 0",
   "discount_amount integer not null 0",
   "service_charge_pct integer not null 0",
   "service_charge_amount integer not null 0",
   "vat_pct integer not null 0",
   "vat_amount integer not null 0",
   "total integer not null 0",
   "note text null",
   "created_by uuid null",
   "closed_by uuid null",
   "paid_at timestamp with time zone null",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()",
   "split_parent_id uuid null",
   "split_count integer null",
   "online_order_id uuid null",
   "discount_by uuid null",
   "discount_at timestamp with time zone null"
  ],
  "bills_revenue": [
   "id uuid null",
   "tenant_id uuid null",
   "bill_no integer null",
   "table_session_id uuid null",
   "status text null",
   "subtotal integer null",
   "discount_type text null",
   "discount_value integer null",
   "discount_amount integer null",
   "service_charge_pct integer null",
   "service_charge_amount integer null",
   "vat_pct integer null",
   "vat_amount integer null",
   "total integer null",
   "note text null",
   "created_by uuid null",
   "closed_by uuid null",
   "paid_at timestamp with time zone null",
   "created_at timestamp with time zone null",
   "updated_at timestamp with time zone null",
   "split_parent_id uuid null",
   "split_count integer null",
   "online_order_id uuid null",
   "business_at timestamp with time zone null"
  ],
  "ingredients": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "name text not null",
   "kind text not null 'purchased'::text",
   "base_unit text not null",
   "purchase_unit text null",
   "purchase_factor numeric not null 1",
   "yield_pct integer not null 100",
   "must_count boolean not null false",
   "batch_output_qty numeric null",
   "last_unit_cost numeric null",
   "last_cost_at timestamp with time zone null",
   "active boolean not null true",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "leads": [
   "id uuid not null gen_random_uuid()",
   "name text not null",
   "phone text not null",
   "note text null",
   "source text not null 'landing'::text",
   "status text not null 'new'::text",
   "created_at timestamp with time zone not null now()",
   "contacted_at timestamp with time zone null"
  ],
  "memberships": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "user_id uuid null",
   "role text not null",
   "display_name text null",
   "pin_hash text null",
   "active boolean not null true",
   "created_at timestamp with time zone not null now()",
   "email USER-DEFINED null"
  ],
  "menu_categories": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "name text not null",
   "sort_order integer not null 0",
   "active boolean not null true",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "menu_item_modifier_groups": [
   "item_id uuid not null",
   "group_id uuid not null",
   "tenant_id uuid not null",
   "sort_order integer not null 0"
  ],
  "menu_items": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "category_id uuid not null",
   "name text not null",
   "description text null",
   "base_price integer not null 0",
   "image_url text null",
   "is_available boolean not null true",
   "sort_order integer not null 0",
   "active boolean not null true",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "modifier_groups": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "name text not null",
   "min_select integer not null 0",
   "max_select integer not null 1",
   "required boolean not null false",
   "sort_order integer not null 0",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "modifier_options": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "group_id uuid not null",
   "name text not null",
   "price_delta integer not null 0",
   "is_available boolean not null true",
   "sort_order integer not null 0",
   "created_at timestamp with time zone not null now()"
  ],
  "order_item_modifiers": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "order_item_id uuid not null",
   "option_id uuid null",
   "name_snapshot text not null",
   "price_delta_snapshot integer not null 0"
  ],
  "order_items": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "order_id uuid not null",
   "menu_item_id uuid null",
   "name_snapshot text not null",
   "unit_price_snapshot integer not null",
   "qty integer not null",
   "note text null",
   "status text not null 'queued'::text",
   "cancel_reason text null",
   "cancelled_by uuid null",
   "prepared_at timestamp with time zone null",
   "created_at timestamp with time zone not null now()",
   "cancelled_at timestamp with time zone null"
  ],
  "orders": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "table_session_id uuid null",
   "channel text not null 'dine_in'::text",
   "source text not null",
   "status text not null 'pending_confirm'::text",
   "customer_contact jsonb null",
   "note text null",
   "created_by uuid null",
   "confirmed_by uuid null",
   "confirmed_at timestamp with time zone null",
   "cancel_reason text null",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()",
   "kitchen_no integer null",
   "parent_order_id uuid null",
   "cancelled_at timestamp with time zone null",
   "idempotency_key uuid null"
  ],
  "payments": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "bill_id uuid not null",
   "method text not null",
   "amount integer not null",
   "received_by uuid null",
   "received_at timestamp with time zone not null now()",
   "note text null",
   "idempotency_key uuid null"
  ],
  "print_jobs": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "type text not null",
   "target_station text null",
   "payload jsonb not null",
   "status text not null 'pending'::text",
   "created_at timestamp with time zone not null now()",
   "printed_at timestamp with time zone null"
  ],
  "printer_heartbeats": [
   "tenant_id uuid not null",
   "seen_at timestamp with time zone not null now()",
   "printer_ok boolean null",
   "printer_host text null",
   "printer_checked_at timestamp with time zone null"
  ],
  "profiles": [
   "id uuid not null",
   "full_name text null",
   "phone text null",
   "created_at timestamp with time zone not null now()"
  ],
  "recipe_lines": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "ingredient_id uuid not null",
   "qty numeric not null",
   "menu_item_id uuid null",
   "modifier_option_id uuid null",
   "parent_ingredient_id uuid null",
   "created_at timestamp with time zone not null now()"
  ],
  "reservations": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "customer_name text not null",
   "customer_phone text not null",
   "party_size integer not null",
   "reserved_at timestamp with time zone not null",
   "note text null",
   "status text not null 'pending'::text",
   "area_id uuid null",
   "decided_by uuid null",
   "decided_at timestamp with time zone null",
   "reject_reason text null",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()",
   "table_id uuid null"
  ],
  "staff_calls": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "table_id uuid not null",
   "table_name text not null",
   "status text not null 'pending'::text",
   "note text null",
   "created_at timestamp with time zone not null now()",
   "resolved_at timestamp with time zone null",
   "resolved_by uuid null"
  ],
  "staff_login_throttle": [
   "email USER-DEFINED not null",
   "fails integer not null 0",
   "locked_until timestamp with time zone null",
   "updated_at timestamp with time zone not null now()"
  ],
  "super_admins": [
   "user_id uuid not null",
   "created_at timestamp with time zone not null now()"
  ],
  "table_sessions": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "table_id uuid not null",
   "status text not null 'open'::text",
   "opened_at timestamp with time zone not null now()",
   "closed_at timestamp with time zone null",
   "opened_by uuid null"
  ],
  "tables": [
   "id uuid not null gen_random_uuid()",
   "tenant_id uuid not null",
   "area_id uuid null",
   "name text not null",
   "seats integer not null 2",
   "qr_token text not null encode(gen_random_bytes(9), 'hex'::text)",
   "status text not null 'available'::text",
   "sort_order integer not null 0",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()"
  ],
  "tenants": [
   "id uuid not null gen_random_uuid()",
   "slug text not null",
   "name text not null",
   "subdomain text null",
   "logo_url text null",
   "settings jsonb not null '{}'::jsonb",
   "status text not null 'active'::text",
   "created_at timestamp with time zone not null now()",
   "updated_at timestamp with time zone not null now()",
   "cover_url text null"
  ]
 },
 "view": {
  "bills_revenue": "SELECT id, tenant_id, bill_no, table_session_id, status, subtotal, discount_type, discount_value, discount_amount, service_charge_pct, service_charge_amount, vat_pct, vat_amount, total, note, created_by, closed_by, paid_at, created_at, updated_at, split_parent_id, split_count, online_order_id, COALESCE(( SELECT min(o.created_at) AS min FROM ((bill_items bi JOIN order_items oi ON ((oi.id = bi.order_item_id))) JOIN orders o ON ((o.id = oi.order_id))) WHERE (bi.bill_id = b.id)), ( SELECT min(o.created_at) AS min FROM orders o WHERE ((o.table_session_id = b.table_session_id) AND (b.table_session_id IS NOT NULL))), created_at) AS business_at FROM bills b;"
 },
 "ham": {
  "auth_tenant_ids()": "CREATE OR REPLACE FUNCTION public.auth_tenant_ids() RETURNS SETOF uuid LANGUAGE sql STABLE SECURITY DEFINER SET search_path TO 'public' AS $function$ select m.tenant_id from public.memberships m join public.tenants t on t.id = m.tenant_id where m.user_id = auth.uid() and m.active and t.status = 'active' $function$",
  "handle_new_user()": "CREATE OR REPLACE FUNCTION public.handle_new_user() RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $function$ begin insert into profiles (id, full_name) values (new.id, coalesce(new.raw_user_meta_data ->> 'full_name', '')) on conflict (id) do nothing; return new; end; $function$",
  "is_super_admin()": "CREATE OR REPLACE FUNCTION public.is_super_admin() RETURNS boolean LANGUAGE sql STABLE SECURITY DEFINER SET search_path TO 'public' AS $function$ select exists ( select 1 from public.super_admins where user_id = auth.uid() ) $function$",
  "pay_bill(p_tenant uuid, p_bill uuid, p_method text, p_paid_at timestamp with time zone, p_note text, p_actor uuid, p_idem uuid)": "CREATE OR REPLACE FUNCTION public.pay_bill(p_tenant uuid, p_bill uuid, p_method text, p_paid_at timestamp with time zone, p_note text DEFAULT NULL::text, p_actor uuid DEFAULT NULL::uuid, p_idem uuid DEFAULT NULL::uuid) RETURNS jsonb LANGUAGE plpgsql SET search_path TO 'public' AS $function$ declare v_status text; v_total int; v_split_count int; v_has_payment boolean; v_key_match boolean; v_key_taken boolean; v_idem uuid; begin -- Khóa TRƯỚC khi đọc: mọi quyết định bên dưới phải dựa trên trạng thái không ai đổi được nữa. select b.status, b.total, b.split_count into v_status, v_total, v_split_count from public.bills b where b.id = p_bill and b.tenant_id = p_tenant for update; if not found then return jsonb_build_object('ok', false, 'code', 'not_found'); end if; select exists ( select 1 from public.payments p where p.tenant_id = p_tenant and p.bill_id = p_bill ), (p_idem is not null and exists ( select 1 from public.payments p where p.tenant_id = p_tenant and p.bill_id = p_bill and p.idempotency_key = p_idem )) into v_has_payment, v_key_match; -- GỬI LẠI một lần bấm ĐÃ XONG HẲN: hóa đơn 'paid' + đúng khóa của lần bấm đó, trên CHÍNH hóa đơn -- này. Trả kết quả cũ như một lần thành công — nơi gọi vẫn chạy phần đuôi (có thể lượt trước chết -- ngay sau khi đóng bill). if v_status = 'paid' and v_key_match then return jsonb_build_object('ok', true, 'replayed', true, 'total', v_total); end if; -- Không khớp khóa ⇒ đây là một lượt thu MỚI nhắm vào hóa đơn đã chốt. Giữ nguyên chốt fail-closed -- của bản cũ: từ chối, để thu ngân nhìn lại chứ không lặng lẽ báo \"đã thu\". if v_status <> 'open' then return jsonb_build_object('ok', false, 'code', 'not_open'); end if; if v_split_count is not null then return jsonb_build_object('ok', false, 'code', 'split_shell'); end if; if v_total <= 0 then return jsonb_build_object('ok', false, 'code', 'empty_total'); end if; -- Khóa client gửi lên ĐANG THUỘC VỀ HÓA ĐƠN KHÁC ⇒ nó không phải danh tính hợp lệ của lượt thu -- này (client lỗi, hoặc ai đó gọi thẳng action). Ghi kèm nó sẽ đâm `uniq_payments_idempotency_key` -- (0034) và ném 23505 ra khỏi function — tức MỘT KHÓA SAI CHẶN ĐỨNG việc thu tiền của một hóa đơn -- hợp lệ. Với nhà hàng đang bán thì \"không thu được tiền\" là hỏng nặng hơn \"mất một lớp phòng thủ -- phụ\", nên: BỎ khóa đi (ghi null) và thu bình thường. -- -- An toàn không suy giảm: lớp chống trùng THẬT là `v_has_payment` — neo vào hóa đơn, không cần -- khóa. Lượt gửi lại sau đó vẫn thấy hóa đơn đã có tiền và vẫn không ghi dòng thứ hai. select exists ( select 1 from public.payments p where p.tenant_id = p_tenant and p.idempotency_key = p_idem and p.bill_id <> p_bill ) into v_key_taken; v_idem := case when p_idem is null or v_key_taken then null else p_idem end; -- Hóa đơn còn 'open' mà đã có payment = lượt thu trước chết đúng giữa hai bước (dưới bản cũ). KHÔNG -- ghi thêm dòng nào — chỉ chạy nốt phần còn dở là đóng hóa đơn. Đây là đường tự chữa lành, và là -- lý do điều kiện gửi lại phải neo vào hóa đơn: nó đúng kể cả khi client không gửi khóa nào. if not v_has_payment then insert into public.payments ( tenant_id, bill_id, method, amount, received_at, received_by, note, idempotency_key ) values ( p_tenant, p_bill, p_method, v_total, p_paid_at, p_actor, p_note, v_idem ); end if; -- `amount` lấy `v_total` đọc DƯỚI KHÓA, không lấy số tầng app đọc trước đó: giữa hai thời điểm ấy -- hóa đơn có thể vừa được giảm giá / thêm món. update public.bills set status = 'paid', paid_at = p_paid_at, closed_by = p_actor, updated_at = now() where id = p_bill and tenant_id = p_tenant and status = 'open'; -- `key_reused` không đổi hành vi phía app; để đây làm dấu vết đọc được khi cần truy vì sao một -- dòng payments không mang khóa nào. return jsonb_build_object( 'ok', true, 'replayed', v_has_payment, 'total', v_total, 'key_reused', v_key_taken ); end; $function$",
  "printer_heartbeat(p_printer_ok boolean, p_printer_host text)": "CREATE OR REPLACE FUNCTION public.printer_heartbeat(p_printer_ok boolean DEFAULT NULL::boolean, p_printer_host text DEFAULT NULL::text) RETURNS timestamp with time zone LANGUAGE plpgsql SECURITY DEFINER SET search_path TO 'public' AS $function$ declare v_tenant uuid; v_now timestamptz := now(); begin select m.tenant_id into v_tenant from public.memberships m where m.user_id = auth.uid() and m.role = 'printer' and m.active and m.tenant_id in (select public.auth_tenant_ids()) limit 1; if v_tenant is null then raise exception 'chi tai khoan cau in moi bao nhip tim duoc' using errcode = '42501'; end if; insert into public.printer_heartbeats (tenant_id, seen_at, printer_ok, printer_host, printer_checked_at) values ( v_tenant, v_now, p_printer_ok, left(p_printer_host, 100), case when p_printer_ok is null then null else v_now end ) on conflict (tenant_id) do update set seen_at = excluded.seen_at, printer_ok = coalesce(excluded.printer_ok, public.printer_heartbeats.printer_ok), printer_host = coalesce(excluded.printer_host, public.printer_heartbeats.printer_host), printer_checked_at = coalesce(excluded.printer_checked_at, public.printer_heartbeats.printer_checked_at); return v_now; end; $function$",
  "report_by_area(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_by_area(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(area_name text, table_name text, revenue bigint, bill_count bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce(a.name, case when t.id is null then 'Không gắn bàn' else 'Chưa xếp khu' end), coalesce(t.name, '—'), coalesce(sum(b.total), 0)::bigint, count(*)::bigint from public.bills_revenue b left join public.table_sessions ts on ts.id = b.table_session_id left join public.tables t on t.id = ts.table_id left join public.areas a on a.id = t.area_id where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.business_at >= p_from and b.business_at < p_to group by 1, 2 order by 3 desc; $function$",
  "report_by_category(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_by_category(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(name text, qty bigint, revenue bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce(mc.name, 'Khác'), sum(bi.qty_allocated)::bigint, sum(bi.amount)::bigint from public.bill_items bi join public.bills_revenue b on b.id = bi.bill_id join public.order_items oi on oi.id = bi.order_item_id left join public.menu_items mi on mi.id = oi.menu_item_id left join public.menu_categories mc on mc.id = mi.category_id where bi.tenant_id = p_tenant and b.status = 'paid' and b.business_at >= p_from and b.business_at < p_to group by 1 order by 3 desc; $function$",
  "report_by_channel(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_by_channel(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(channel text, source text, has_table boolean, revenue bigint, item_count bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select o.channel, o.source, (o.table_session_id is not null), sum(bi.amount)::bigint, sum(bi.qty_allocated)::bigint from public.bill_items bi join public.bills_revenue b on b.id = bi.bill_id join public.order_items oi on oi.id = bi.order_item_id join public.orders o on o.id = oi.order_id where bi.tenant_id = p_tenant and b.status = 'paid' and b.business_at >= p_from and b.business_at < p_to group by 1, 2, 3 order by 4 desc; $function$",
  "report_cancel_after_print_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_cancel_after_print_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(after_qty bigint, after_amount bigint, comparable_qty bigint, comparable_amount bigint, approx_qty bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ with c as ( select oi.qty as qty, (oi.unit_price_snapshot::bigint * oi.qty) as amount, (oi.cancelled_at is distinct from oi.created_at) as trusted, -- Đơn chưa in lần nào → first_printed_at IS NULL → phép so dưới ra NULL → không vào tử số. (oi.cancelled_at > pj.first_printed_at) as after_print from public.order_items oi left join lateral ( select min(j.created_at) as first_printed_at from public.print_jobs j where j.tenant_id = p_tenant and j.type = 'kitchen_ticket' and j.payload ->> 'orderId' = oi.order_id::text ) pj on true where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to ) select coalesce(sum(c.qty) filter (where c.trusted and c.after_print), 0)::bigint, coalesce(sum(c.amount) filter (where c.trusted and c.after_print), 0)::bigint, coalesce(sum(c.qty) filter (where c.trusted), 0)::bigint, coalesce(sum(c.amount) filter (where c.trusted), 0)::bigint, coalesce(sum(c.qty) filter (where not c.trusted), 0)::bigint from c; $function$",
  "report_cancel_by_actor(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_cancel_by_actor(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(membership_id uuid, display_name text, role text, cnt bigint, qty bigint, amount bigint, after_qty bigint, after_amount bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select oi.cancelled_by, coalesce(m.display_name, '—'), coalesce(m.role, ''), count(*)::bigint, sum(oi.qty)::bigint, sum(oi.unit_price_snapshot::bigint * oi.qty)::bigint, coalesce(sum(oi.qty) filter ( where oi.cancelled_at is distinct from oi.created_at and oi.cancelled_at > pj.first_printed_at ), 0)::bigint, coalesce(sum(oi.unit_price_snapshot::bigint * oi.qty) filter ( where oi.cancelled_at is distinct from oi.created_at and oi.cancelled_at > pj.first_printed_at ), 0)::bigint from public.order_items oi left join public.memberships m on m.id = oi.cancelled_by and m.tenant_id = p_tenant left join lateral ( select min(j.created_at) as first_printed_at from public.print_jobs j where j.tenant_id = p_tenant and j.type = 'kitchen_ticket' and j.payload ->> 'orderId' = oi.order_id::text ) pj on true where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to group by oi.cancelled_by, m.display_name, m.role order by sum(oi.unit_price_snapshot::bigint * oi.qty) desc; $function$",
  "report_cancel_full_signature(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer)": "CREATE OR REPLACE FUNCTION public.report_cancel_full_signature(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer DEFAULT 20) RETURNS TABLE(order_id uuid, cancelled_at timestamp with time zone, printed_at timestamp with time zone, place text, qty bigint, amount bigint, reason text, actor_name text, actor_role text, time_approx boolean) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ with touched as ( -- Chặn quét cả bảng: chỉ xét đơn có ít nhất một món bị hủy TRONG KỲ (index -- idx_order_items_cancelled của 0028). `max(cancelled_at)` nằm trong kỳ luôn kéo theo điều -- kiện này, nên lọc trước không đổi kết quả. select distinct oi.order_id from public.order_items oi where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to ), all_cancelled as ( -- Điều kiện 2: đơn KHÔNG còn món nào ngoài 'cancelled'. Đọc từ chính các dòng món chứ không -- từ `orders.status` — roll-up trạng thái nằm ở tầng app và có thể trượt. select oi.order_id, max(oi.cancelled_at) as cancelled_at, sum(oi.qty)::bigint as qty, sum(oi.unit_price_snapshot::bigint * oi.qty)::bigint as amount, bool_or(oi.cancelled_at is not distinct from oi.created_at) as time_approx, (array_agg(oi.cancelled_by order by oi.cancelled_at desc nulls last))[1] as last_actor, (array_agg(oi.cancel_reason order by oi.cancelled_at desc nulls last))[1] as last_reason from public.order_items oi join touched tc on tc.order_id = oi.order_id where oi.tenant_id = p_tenant group by oi.order_id having count(*) filter (where oi.status <> 'cancelled') = 0 ) select o.id, a.cancelled_at, pj.first_printed_at, coalesce( case when t.name is not null then 'Bàn ' || t.name end, case when o.kitchen_no is not null then 'Đơn #' || o.kitchen_no end, '—' ), a.qty, a.amount, coalesce(nullif(o.cancel_reason, ''), nullif(a.last_reason, ''), '—'), coalesce(m.display_name, '—'), coalesce(m.role, ''), a.time_approx from all_cancelled a join public.orders o on o.id = a.order_id and o.tenant_id = p_tenant join lateral ( select min(j.created_at) as first_printed_at from public.print_jobs j where j.tenant_id = p_tenant and j.type = 'kitchen_ticket' and j.payload ->> 'orderId' = o.id::text ) pj on pj.first_printed_at is not null -- điều kiện 1: đã in phiếu bếp left join public.table_sessions ts on ts.id = o.table_session_id left join public.tables t on t.id = ts.table_id left join public.memberships m on m.id = a.last_actor and m.tenant_id = p_tenant where a.cancelled_at >= p_from and a.cancelled_at < p_to -- điều kiện 3: không một đồng nào được thu cho bất kỳ món nào của đơn này. and not exists ( select 1 from public.order_items oi2 join public.bill_items bi on bi.order_item_id = oi2.id and bi.tenant_id = p_tenant join public.payments p on p.bill_id = bi.bill_id and p.tenant_id = p_tenant where oi2.order_id = o.id and oi2.tenant_id = p_tenant ) order by a.cancelled_at desc limit greatest(p_limit, 1); $function$",
  "report_cancel_list(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer, p_offset integer)": "CREATE OR REPLACE FUNCTION public.report_cancel_list(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer DEFAULT 20, p_offset integer DEFAULT 0) RETURNS TABLE(cancelled_at timestamp with time zone, place text, item_name text, qty integer, amount bigint, reason text, actor_name text, printed_at timestamp with time zone, after_print boolean, time_approx boolean) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select oi.cancelled_at, coalesce( case when t.name is not null then 'Bàn ' || t.name end, case when o.kitchen_no is not null then 'Đơn #' || o.kitchen_no end, '—' ), oi.name_snapshot, oi.qty, (oi.unit_price_snapshot::bigint * oi.qty)::bigint, -- Hủy cả đơn chỉ ghi lý do ở `orders`; hủy lẻ ghi ở `order_items`. Lấy cái nào có. coalesce(nullif(oi.cancel_reason, ''), nullif(o.cancel_reason, ''), '—'), coalesce(m.display_name, '—'), pj.first_printed_at, case when oi.cancelled_at is not distinct from oi.created_at then null -- mốc xấp xỉ: không kết luận else oi.cancelled_at > pj.first_printed_at end, (oi.cancelled_at is not distinct from oi.created_at) from public.order_items oi join public.orders o on o.id = oi.order_id left join public.table_sessions ts on ts.id = o.table_session_id left join public.tables t on t.id = ts.table_id left join public.memberships m on m.id = oi.cancelled_by and m.tenant_id = p_tenant left join lateral ( select min(j.created_at) as first_printed_at from public.print_jobs j where j.tenant_id = p_tenant and j.type = 'kitchen_ticket' and j.payload ->> 'orderId' = oi.order_id::text ) pj on true where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to order by oi.cancelled_at desc, oi.id desc limit greatest(p_limit, 1) offset greatest(p_offset, 0); $function$",
  "report_cancel_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_cancel_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(cancelled_qty bigint, cancelled_amount bigint, ordered_qty bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce((select sum(oi.qty) from public.order_items oi where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to), 0)::bigint, coalesce((select sum(oi.unit_price_snapshot::bigint * oi.qty) from public.order_items oi where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to), 0)::bigint, -- Mẫu số của tỷ lệ hủy = số món ĐÃ GỌI trong kỳ (gồm cả món sau đó bị hủy) ⇒ lọc theo -- created_at, không phải cancelled_at. coalesce((select sum(oi.qty) from public.order_items oi where oi.tenant_id = p_tenant and oi.created_at >= p_from and oi.created_at < p_to), 0)::bigint; $function$",
  "report_cancel_top_items(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer)": "CREATE OR REPLACE FUNCTION public.report_cancel_top_items(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer DEFAULT 10) RETURNS TABLE(name text, qty bigint, amount bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select oi.name_snapshot, sum(oi.qty)::bigint, sum(oi.unit_price_snapshot::bigint * oi.qty)::bigint from public.order_items oi where oi.tenant_id = p_tenant and oi.status = 'cancelled' and oi.cancelled_at >= p_from and oi.cancelled_at < p_to group by oi.name_snapshot order by sum(oi.qty) desc, sum(oi.unit_price_snapshot::bigint * oi.qty) desc limit greatest(p_limit, 1); $function$",
  "report_discount_by_actor(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_discount_by_actor(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(membership_id uuid, display_name text, role text, cnt bigint, amount bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select b.discount_by, coalesce(m.display_name, '—'), coalesce(m.role, ''), count(*)::bigint, coalesce(sum(b.discount_amount), 0)::bigint from public.bills b left join public.memberships m on m.id = b.discount_by and m.tenant_id = p_tenant where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.discount_amount > 0 and b.paid_at >= p_from and b.paid_at < p_to group by b.discount_by, m.display_name, m.role order by sum(b.discount_amount) desc; $function$",
  "report_discount_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_discount_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(bill_cnt bigint, discount_amount bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select count(*)::bigint, coalesce(sum(b.discount_amount), 0)::bigint from public.bills b where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.discount_amount > 0 and b.paid_at >= p_from and b.paid_at < p_to; $function$",
  "report_hour_dow(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_hour_dow(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(dow integer, hour integer, revenue bigint, bill_count bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select extract(dow from b.business_at at time zone 'Asia/Ho_Chi_Minh')::int, extract(hour from b.business_at at time zone 'Asia/Ho_Chi_Minh')::int, coalesce(sum(b.total), 0)::bigint, count(*)::bigint from public.bills_revenue b where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.business_at >= p_from and b.business_at < p_to group by 1, 2 order by 1, 2; $function$",
  "report_payments(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_payments(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(method text, amount bigint, count bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select p.method, coalesce(sum(p.amount), 0)::bigint, count(*)::bigint from public.payments p join public.bills_revenue b on b.id = p.bill_id where p.tenant_id = p_tenant and b.business_at >= p_from and b.business_at < p_to group by 1 order by 2 desc; $function$",
  "report_series(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_grain text)": "CREATE OR REPLACE FUNCTION public.report_series(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_grain text DEFAULT 'day'::text) RETURNS TABLE(bucket_start timestamp with time zone, revenue bigint, bill_count bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select (date_trunc( case when p_grain in ('hour','day','week','month') then p_grain else 'day' end, b.business_at at time zone 'Asia/Ho_Chi_Minh' ) at time zone 'Asia/Ho_Chi_Minh'), coalesce(sum(b.total), 0)::bigint, count(*)::bigint from public.bills_revenue b where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.business_at >= p_from and b.business_at < p_to group by 1 order by 1; $function$",
  "report_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone)": "CREATE OR REPLACE FUNCTION public.report_summary(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone) RETURNS TABLE(total_revenue bigint, bill_count bigint, avg_per_bill bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce(sum(b.total), 0)::bigint, count(*)::bigint, coalesce(round(avg(b.total)), 0)::bigint from public.bills_revenue b where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and b.business_at >= p_from and b.business_at < p_to; $function$",
  "report_top_items(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer)": "CREATE OR REPLACE FUNCTION public.report_top_items(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_limit integer DEFAULT 10) RETURNS TABLE(name text, qty bigint, revenue bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce(oi.name_snapshot, '—'), sum(bi.qty_allocated)::bigint, sum(bi.amount)::bigint from public.bill_items bi join public.bills_revenue b on b.id = bi.bill_id join public.order_items oi on oi.id = bi.order_item_id where bi.tenant_id = p_tenant and b.status = 'paid' and b.business_at >= p_from and b.business_at < p_to group by 1 order by 2 desc, 3 desc limit greatest(p_limit, 1); $function$",
  "takeaway_paid_total(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_root_ids uuid[])": "CREATE OR REPLACE FUNCTION public.takeaway_paid_total(p_tenant uuid, p_from timestamp with time zone, p_to timestamp with time zone, p_root_ids uuid[] DEFAULT NULL::uuid[]) RETURNS TABLE(paid_total bigint, paid_bills bigint) LANGUAGE sql STABLE SET search_path TO 'public' AS $function$ select coalesce(sum(b.total), 0)::bigint, count(*)::bigint from public.bills_revenue b join public.orders o on o.id = b.online_order_id where b.tenant_id = p_tenant and b.status = 'paid' and b.split_count is null and o.channel = 'takeaway' and o.parent_order_id is null and ( case when p_root_ids is null then b.business_at >= p_from and b.business_at < p_to else o.id = any(p_root_ids) end ); $function$",
  "unsplit_bill_evenly(p_tenant uuid, p_bill uuid, p_actor uuid)": "CREATE OR REPLACE FUNCTION public.unsplit_bill_evenly(p_tenant uuid, p_bill uuid, p_actor uuid DEFAULT NULL::uuid) RETURNS jsonb LANGUAGE plpgsql SET search_path TO 'public' AS $function$ declare v_status text; v_split_count int; v_blocked int; v_voided int; begin -- Vỏ: khóa trước khi đọc trạng thái, để hai thu ngân cùng bấm \"Gỡ chia\" thì người sau đọc lại -- được kết quả của người trước (split_count đã null → 'not_split') chứ không gỡ chồng lên nhau. select b.status, b.split_count into v_status, v_split_count from public.bills b where b.id = p_bill and b.tenant_id = p_tenant for update; if not found then return jsonb_build_object('ok', false, 'code', 'not_found'); end if; if v_status <> 'open' or v_split_count is null then return jsonb_build_object('ok', false, 'code', 'not_split'); end if; -- Khóa các con của LƯỢT CHIA HIỆN TẠI (order by id: thứ tự khóa cố định, tránh deadlock khi hai -- lượt gỡ giao nhau). Con 'void' là tàn dư của lượt chia trước, không ai còn thu vào chúng được -- nữa nên không cần khóa. `perform` vì `for update` không dùng chung được với hàm tổng hợp. perform 1 from public.bills c where c.tenant_id = p_tenant and c.split_parent_id = p_bill and c.status <> 'void' order by c.id for update; -- Chốt bảo vệ tiền: con đã rời 'open' (đang/đã thu) HOẶC đã có dòng payments nào (thu chưa đủ -- nên bill còn 'open') thì từ chối CẢ LƯỢT. Hoàn tiền là việc của người thật, không tự động hóa. -- Bỏ con 'void': lượt gỡ trước đã kết luận chúng KHÔNG có tiền (nếu có thì lượt đó đã bị chặn), -- đếm lại chúng là tự chặn vĩnh viễn mọi lượt gỡ sau. select count(*) into v_blocked from public.bills c where c.tenant_id = p_tenant and c.split_parent_id = p_bill and c.status <> 'void' and ( c.status <> 'open' or exists ( select 1 from public.payments p where p.tenant_id = p_tenant and p.bill_id = c.id ) ); if v_blocked > 0 then return jsonb_build_object('ok', false, 'code', 'has_payment'); end if; -- `closed_by` = người ĐÓNG hóa đơn (0012). Void chính là đóng nó, nên ghi người bấm gỡ chia vào -- đây là đúng nghĩa cột — và không cột nào của báo cáo đọc `closed_by`, nên không làm lệch số. -- Ghi thêm dấu vào `note` để người mở bảng `bills` biết vì sao dòng này void. update public.bills set status = 'void', closed_by = coalesce(p_actor, closed_by), note = coalesce(note || ' · ', '') || 'Đã gỡ chia', updated_at = now() where tenant_id = p_tenant and split_parent_id = p_bill and status = 'open'; get diagnostics v_voided = row_count; -- Vỏ trở lại hóa đơn thường. Vỏ giữ nguyên `bill_items` từ lúc chia nên tầng app chỉ cần tính -- lại tổng là về đúng con số trước khi chia. update public.bills set split_count = null, updated_at = now() where id = p_bill and tenant_id = p_tenant; -- Vỏ MỒ CÔI (còn cờ chia đều, 0 con — di chứng của một lượt gỡ hỏng giữa chừng thời bản cũ) -- rơi đúng vào đây với v_voided = 0: không có gì để void, chỉ bỏ cờ. Đó là kết quả HỢP LỆ, -- không phải lỗi — nếu từ chối thì bill đó không thu được bằng đường nào. return jsonb_build_object('ok', true, 'voided', v_voided); end; $function$"
 },
 "policy": {
  "public.areas.areas_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.bill_items.bill_items_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.bills.bills_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.ingredients.ingredients_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.memberships.memberships_tenant_read": "r | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) |",
  "public.memberships.memberships_tenant_write": "* | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin())",
  "public.menu_categories.menu_categories_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.menu_item_modifier_groups.mimg_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.menu_items.menu_items_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.modifier_groups.modifier_groups_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.modifier_options.modifier_options_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.order_item_modifiers.order_item_modifiers_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.order_items.order_items_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.orders.orders_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.payments.payments_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.print_jobs.print_jobs_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.printer_heartbeats.printer_heartbeats_select": "r | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) |",
  "public.profiles.profiles_self_read": "r | ((id = auth.uid()) OR is_super_admin()) |",
  "public.profiles.profiles_self_write": "* | (id = auth.uid()) | (id = auth.uid())",
  "public.recipe_lines.recipe_lines_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.reservations.reservations_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.staff_calls.staff_calls_tenant_read": "r | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) |",
  "public.staff_calls.staff_calls_tenant_write": "* | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) | ((tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin())",
  "public.super_admins.super_admins_self_read": "r | (user_id = auth.uid()) |",
  "public.table_sessions.table_sessions_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.tables.tables_tenant_all": "* | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) | (tenant_id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids))",
  "public.tenants.tenants_member_read": "r | ((id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) |",
  "public.tenants.tenants_member_update": "w | ((id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin()) | ((id IN ( SELECT auth_tenant_ids() AS auth_tenant_ids)) OR is_super_admin())",
  "public.tenants.tenants_super_write": "* | is_super_admin() | is_super_admin()",
  "storage.objects.menu_images_public_read": "r | (bucket_id = 'menu-images'::text) |",
  "storage.objects.menu_images_service_write": "* | (bucket_id = 'menu-images'::text) | (bucket_id = 'menu-images'::text)"
 },
 "index": {
  "areas_pkey": "CREATE UNIQUE INDEX areas_pkey ON public.areas USING btree (id)",
  "bill_items_pkey": "CREATE UNIQUE INDEX bill_items_pkey ON public.bill_items USING btree (id)",
  "bills_pkey": "CREATE UNIQUE INDEX bills_pkey ON public.bills USING btree (id)",
  "idx_areas_tenant_sort": "CREATE INDEX idx_areas_tenant_sort ON public.areas USING btree (tenant_id, sort_order)",
  "idx_bill_items_order_item_bill": "CREATE INDEX idx_bill_items_order_item_bill ON public.bill_items USING btree (order_item_id, bill_id)",
  "idx_bill_items_tenant_bill": "CREATE INDEX idx_bill_items_tenant_bill ON public.bill_items USING btree (tenant_id, bill_id)",
  "idx_bill_items_tenant_order_item": "CREATE INDEX idx_bill_items_tenant_order_item ON public.bill_items USING btree (tenant_id, order_item_id)",
  "idx_bills_split_parent": "CREATE INDEX idx_bills_split_parent ON public.bills USING btree (tenant_id, split_parent_id)",
  "idx_bills_tenant_online_order": "CREATE INDEX idx_bills_tenant_online_order ON public.bills USING btree (tenant_id, online_order_id)",
  "idx_bills_tenant_paid_at": "CREATE INDEX idx_bills_tenant_paid_at ON public.bills USING btree (tenant_id, paid_at)",
  "idx_bills_tenant_session": "CREATE INDEX idx_bills_tenant_session ON public.bills USING btree (tenant_id, table_session_id)",
  "idx_bills_tenant_status": "CREATE INDEX idx_bills_tenant_status ON public.bills USING btree (tenant_id, status)",
  "idx_ingredients_tenant": "CREATE INDEX idx_ingredients_tenant ON public.ingredients USING btree (tenant_id)",
  "idx_leads_created_at": "CREATE INDEX idx_leads_created_at ON public.leads USING btree (created_at DESC)",
  "idx_leads_phone_created": "CREATE INDEX idx_leads_phone_created ON public.leads USING btree (phone, created_at DESC)",
  "idx_memberships_pin_lookup": "CREATE INDEX idx_memberships_pin_lookup ON public.memberships USING btree (tenant_id, active) WHERE (user_id IS NULL)",
  "idx_memberships_tenant": "CREATE INDEX idx_memberships_tenant ON public.memberships USING btree (tenant_id)",
  "idx_memberships_tenant_role": "CREATE INDEX idx_memberships_tenant_role ON public.memberships USING btree (tenant_id, role)",
  "idx_memberships_user": "CREATE INDEX idx_memberships_user ON public.memberships USING btree (user_id)",
  "idx_menu_categories_tenant_sort": "CREATE INDEX idx_menu_categories_tenant_sort ON public.menu_categories USING btree (tenant_id, sort_order)",
  "idx_menu_items_tenant_cat_sort": "CREATE INDEX idx_menu_items_tenant_cat_sort ON public.menu_items USING btree (tenant_id, category_id, sort_order)",
  "idx_mimg_group": "CREATE INDEX idx_mimg_group ON public.menu_item_modifier_groups USING btree (group_id)",
  "idx_modifier_groups_tenant_sort": "CREATE INDEX idx_modifier_groups_tenant_sort ON public.modifier_groups USING btree (tenant_id, sort_order)",
  "idx_modifier_options_group_sort": "CREATE INDEX idx_modifier_options_group_sort ON public.modifier_options USING btree (group_id, sort_order)",
  "idx_order_item_modifiers_item": "CREATE INDEX idx_order_item_modifiers_item ON public.order_item_modifiers USING btree (order_item_id)",
  "idx_order_items_cancelled": "CREATE INDEX idx_order_items_cancelled ON public.order_items USING btree (tenant_id, cancelled_at) WHERE (status = 'cancelled'::text)",
  "idx_order_items_tenant_created": "CREATE INDEX idx_order_items_tenant_created ON public.order_items USING btree (tenant_id, created_at)",
  "idx_order_items_tenant_order": "CREATE INDEX idx_order_items_tenant_order ON public.order_items USING btree (tenant_id, order_id)",
  "idx_order_items_tenant_status": "CREATE INDEX idx_order_items_tenant_status ON public.order_items USING btree (tenant_id, status)",
  "idx_orders_parent": "CREATE INDEX idx_orders_parent ON public.orders USING btree (tenant_id, parent_order_id) WHERE (parent_order_id IS NOT NULL)",
  "idx_orders_tenant_channel_status": "CREATE INDEX idx_orders_tenant_channel_status ON public.orders USING btree (tenant_id, channel, status)",
  "idx_orders_tenant_session": "CREATE INDEX idx_orders_tenant_session ON public.orders USING btree (tenant_id, table_session_id)",
  "idx_orders_tenant_status": "CREATE INDEX idx_orders_tenant_status ON public.orders USING btree (tenant_id, status)",
  "idx_payments_tenant_bill": "CREATE INDEX idx_payments_tenant_bill ON public.payments USING btree (tenant_id, bill_id)",
  "idx_payments_tenant_received_at": "CREATE INDEX idx_payments_tenant_received_at ON public.payments USING btree (tenant_id, received_at)",
  "idx_print_jobs_kitchen_order": "CREATE INDEX idx_print_jobs_kitchen_order ON public.print_jobs USING btree (tenant_id, ((payload ->> 'orderId'::text)), created_at) WHERE (type = 'kitchen_ticket'::text)",
  "idx_print_jobs_tenant_status_created": "CREATE INDEX idx_print_jobs_tenant_status_created ON public.print_jobs USING btree (tenant_id, status, created_at)",
  "idx_print_jobs_tenant_type_created": "CREATE INDEX idx_print_jobs_tenant_type_created ON public.print_jobs USING btree (tenant_id, type, created_at DESC)",
  "idx_recipe_lines_ingredient": "CREATE INDEX idx_recipe_lines_ingredient ON public.recipe_lines USING btree (ingredient_id)",
  "idx_recipe_lines_tenant": "CREATE INDEX idx_recipe_lines_tenant ON public.recipe_lines USING btree (tenant_id)",
  "idx_reservations_tenant_reserved": "CREATE INDEX idx_reservations_tenant_reserved ON public.reservations USING btree (tenant_id, reserved_at)",
  "idx_reservations_tenant_status": "CREATE INDEX idx_reservations_tenant_status ON public.reservations USING btree (tenant_id, status)",
  "idx_reservations_tenant_table": "CREATE INDEX idx_reservations_tenant_table ON public.reservations USING btree (tenant_id, table_id)",
  "idx_staff_calls_table_pending": "CREATE INDEX idx_staff_calls_table_pending ON public.staff_calls USING btree (table_id, status, created_at)",
  "idx_staff_calls_tenant_status": "CREATE INDEX idx_staff_calls_tenant_status ON public.staff_calls USING btree (tenant_id, status, created_at)",
  "idx_table_sessions_tenant_table": "CREATE INDEX idx_table_sessions_tenant_table ON public.table_sessions USING btree (tenant_id, table_id)",
  "idx_tables_tenant_area_sort": "CREATE INDEX idx_tables_tenant_area_sort ON public.tables USING btree (tenant_id, area_id, sort_order)",
  "idx_tenants_slug": "CREATE INDEX idx_tenants_slug ON public.tenants USING btree (slug)",
  "ingredients_pkey": "CREATE UNIQUE INDEX ingredients_pkey ON public.ingredients USING btree (id)",
  "leads_pkey": "CREATE UNIQUE INDEX leads_pkey ON public.leads USING btree (id)",
  "memberships_pkey": "CREATE UNIQUE INDEX memberships_pkey ON public.memberships USING btree (id)",
  "menu_categories_pkey": "CREATE UNIQUE INDEX menu_categories_pkey ON public.menu_categories USING btree (id)",
  "menu_item_modifier_groups_pkey": "CREATE UNIQUE INDEX menu_item_modifier_groups_pkey ON public.menu_item_modifier_groups USING btree (item_id, group_id)",
  "menu_items_pkey": "CREATE UNIQUE INDEX menu_items_pkey ON public.menu_items USING btree (id)",
  "modifier_groups_pkey": "CREATE UNIQUE INDEX modifier_groups_pkey ON public.modifier_groups USING btree (id)",
  "modifier_options_pkey": "CREATE UNIQUE INDEX modifier_options_pkey ON public.modifier_options USING btree (id)",
  "order_item_modifiers_pkey": "CREATE UNIQUE INDEX order_item_modifiers_pkey ON public.order_item_modifiers USING btree (id)",
  "order_items_pkey": "CREATE UNIQUE INDEX order_items_pkey ON public.order_items USING btree (id)",
  "orders_pkey": "CREATE UNIQUE INDEX orders_pkey ON public.orders USING btree (id)",
  "payments_pkey": "CREATE UNIQUE INDEX payments_pkey ON public.payments USING btree (id)",
  "print_jobs_pkey": "CREATE UNIQUE INDEX print_jobs_pkey ON public.print_jobs USING btree (id)",
  "printer_heartbeats_pkey": "CREATE UNIQUE INDEX printer_heartbeats_pkey ON public.printer_heartbeats USING btree (tenant_id)",
  "profiles_pkey": "CREATE UNIQUE INDEX profiles_pkey ON public.profiles USING btree (id)",
  "recipe_lines_pkey": "CREATE UNIQUE INDEX recipe_lines_pkey ON public.recipe_lines USING btree (id)",
  "reservations_pkey": "CREATE UNIQUE INDEX reservations_pkey ON public.reservations USING btree (id)",
  "staff_calls_pkey": "CREATE UNIQUE INDEX staff_calls_pkey ON public.staff_calls USING btree (id)",
  "staff_login_throttle_pkey": "CREATE UNIQUE INDEX staff_login_throttle_pkey ON public.staff_login_throttle USING btree (email)",
  "super_admins_pkey": "CREATE UNIQUE INDEX super_admins_pkey ON public.super_admins USING btree (user_id)",
  "table_sessions_pkey": "CREATE UNIQUE INDEX table_sessions_pkey ON public.table_sessions USING btree (id)",
  "tables_pkey": "CREATE UNIQUE INDEX tables_pkey ON public.tables USING btree (id)",
  "tables_qr_token_key": "CREATE UNIQUE INDEX tables_qr_token_key ON public.tables USING btree (qr_token)",
  "tenants_pkey": "CREATE UNIQUE INDEX tenants_pkey ON public.tenants USING btree (id)",
  "tenants_slug_key": "CREATE UNIQUE INDEX tenants_slug_key ON public.tenants USING btree (slug)",
  "tenants_subdomain_key": "CREATE UNIQUE INDEX tenants_subdomain_key ON public.tenants USING btree (subdomain)",
  "uniq_bill_items_bill_order_item": "CREATE UNIQUE INDEX uniq_bill_items_bill_order_item ON public.bill_items USING btree (bill_id, order_item_id)",
  "uniq_membership_tenant_user": "CREATE UNIQUE INDEX uniq_membership_tenant_user ON public.memberships USING btree (tenant_id, user_id) WHERE (user_id IS NOT NULL)",
  "uniq_memberships_email": "CREATE UNIQUE INDEX uniq_memberships_email ON public.memberships USING btree (email) WHERE (email IS NOT NULL)",
  "uniq_orders_idempotency_key": "CREATE UNIQUE INDEX uniq_orders_idempotency_key ON public.orders USING btree (tenant_id, idempotency_key) WHERE (idempotency_key IS NOT NULL)",
  "uniq_payments_idempotency_key": "CREATE UNIQUE INDEX uniq_payments_idempotency_key ON public.payments USING btree (tenant_id, idempotency_key) WHERE (idempotency_key IS NOT NULL)",
  "uniq_table_session_open": "CREATE UNIQUE INDEX uniq_table_session_open ON public.table_sessions USING btree (table_id) WHERE (status = 'open'::text)",
  "uq_ingredients_tenant_name": "CREATE UNIQUE INDEX uq_ingredients_tenant_name ON public.ingredients USING btree (tenant_id, lower(btrim(name))) WHERE active",
  "uq_recipe_lines_item": "CREATE UNIQUE INDEX uq_recipe_lines_item ON public.recipe_lines USING btree (menu_item_id, ingredient_id) WHERE (menu_item_id IS NOT NULL)",
  "uq_recipe_lines_option": "CREATE UNIQUE INDEX uq_recipe_lines_option ON public.recipe_lines USING btree (modifier_option_id, ingredient_id) WHERE (modifier_option_id IS NOT NULL)",
  "uq_recipe_lines_parent": "CREATE UNIQUE INDEX uq_recipe_lines_parent ON public.recipe_lines USING btree (parent_ingredient_id, ingredient_id) WHERE (parent_ingredient_id IS NOT NULL)"
 },
 "trigger": {
  "on_auth_user_created on auth.users": "CREATE TRIGGER on_auth_user_created AFTER INSERT ON auth.users FOR EACH ROW EXECUTE FUNCTION handle_new_user()"
 },
 "rangBuoc": {
  "areas.areas_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "bill_items.bill_items_amount_check": "CHECK ((amount >= 0))",
  "bill_items.bill_items_bill_id_fkey": "FOREIGN KEY (bill_id) REFERENCES bills(id) ON DELETE CASCADE",
  "bill_items.bill_items_order_item_id_fkey": "FOREIGN KEY (order_item_id) REFERENCES order_items(id) ON DELETE RESTRICT",
  "bill_items.bill_items_qty_allocated_check": "CHECK ((qty_allocated >= 1))",
  "bill_items.bill_items_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "bill_items.bill_items_unit_price_snapshot_check": "CHECK ((unit_price_snapshot >= 0))",
  "bills.bills_discount_amount_check": "CHECK ((discount_amount >= 0))",
  "bills.bills_discount_type_check": "CHECK ((discount_type = ANY (ARRAY['none'::text, 'amount'::text, 'percent'::text])))",
  "bills.bills_discount_value_check": "CHECK ((discount_value >= 0))",
  "bills.bills_online_order_id_fkey": "FOREIGN KEY (online_order_id) REFERENCES orders(id) ON DELETE SET NULL",
  "bills.bills_service_charge_amount_check": "CHECK ((service_charge_amount >= 0))",
  "bills.bills_service_charge_pct_check": "CHECK (((service_charge_pct >= 0) AND (service_charge_pct <= 100)))",
  "bills.bills_split_parent_id_fkey": "FOREIGN KEY (split_parent_id) REFERENCES bills(id) ON DELETE CASCADE",
  "bills.bills_status_check": "CHECK ((status = ANY (ARRAY['open'::text, 'paid'::text, 'void'::text])))",
  "bills.bills_subtotal_check": "CHECK ((subtotal >= 0))",
  "bills.bills_table_session_id_fkey": "FOREIGN KEY (table_session_id) REFERENCES table_sessions(id) ON DELETE SET NULL",
  "bills.bills_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "bills.bills_total_check": "CHECK ((total >= 0))",
  "bills.bills_vat_amount_check": "CHECK ((vat_amount >= 0))",
  "bills.bills_vat_pct_check": "CHECK (((vat_pct >= 0) AND (vat_pct <= 100)))",
  "ingredients.ingredients_base_unit_check": "CHECK ((base_unit = ANY (ARRAY['g'::text, 'ml'::text, 'cai'::text])))",
  "ingredients.ingredients_batch_output_qty_check": "CHECK (((batch_output_qty IS NULL) OR (batch_output_qty > (0)::numeric)))",
  "ingredients.ingredients_kind_check": "CHECK ((kind = ANY (ARRAY['purchased'::text, 'prepared'::text])))",
  "ingredients.ingredients_last_unit_cost_check": "CHECK (((last_unit_cost IS NULL) OR (last_unit_cost >= (0)::numeric)))",
  "ingredients.ingredients_name_check": "CHECK ((length(btrim(name)) > 0))",
  "ingredients.ingredients_purchase_factor_check": "CHECK ((purchase_factor > (0)::numeric))",
  "ingredients.ingredients_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "ingredients.ingredients_yield_pct_check": "CHECK (((yield_pct >= 1) AND (yield_pct <= 100)))",
  "leads.leads_name_check": "CHECK ((length(btrim(name)) >= 2))",
  "leads.leads_phone_check": "CHECK ((phone ~ '^0\\d{9,10}$'::text))",
  "leads.leads_status_check": "CHECK ((status = ANY (ARRAY['new'::text, 'contacted'::text, 'closed'::text])))",
  "memberships.memberships_role_check": "CHECK ((role = ANY (ARRAY['owner'::text, 'manager'::text, 'cashier'::text, 'waiter'::text, 'kitchen'::text, 'station'::text, 'printer'::text])))",
  "memberships.memberships_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "memberships.memberships_user_id_fkey": "FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE",
  "menu_categories.menu_categories_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "menu_item_modifier_groups.menu_item_modifier_groups_group_id_fkey": "FOREIGN KEY (group_id) REFERENCES modifier_groups(id) ON DELETE CASCADE",
  "menu_item_modifier_groups.menu_item_modifier_groups_item_id_fkey": "FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE",
  "menu_item_modifier_groups.menu_item_modifier_groups_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "menu_items.menu_items_base_price_check": "CHECK ((base_price >= 0))",
  "menu_items.menu_items_category_id_fkey": "FOREIGN KEY (category_id) REFERENCES menu_categories(id) ON DELETE CASCADE",
  "menu_items.menu_items_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "modifier_groups.modifier_groups_max_ge_min": "CHECK ((max_select >= min_select))",
  "modifier_groups.modifier_groups_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "modifier_options.modifier_options_group_id_fkey": "FOREIGN KEY (group_id) REFERENCES modifier_groups(id) ON DELETE CASCADE",
  "modifier_options.modifier_options_price_delta_check": "CHECK ((price_delta >= 0))",
  "modifier_options.modifier_options_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "order_item_modifiers.order_item_modifiers_order_item_id_fkey": "FOREIGN KEY (order_item_id) REFERENCES order_items(id) ON DELETE CASCADE",
  "order_item_modifiers.order_item_modifiers_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "order_items.order_items_menu_item_id_fkey": "FOREIGN KEY (menu_item_id) REFERENCES menu_items(id) ON DELETE SET NULL",
  "order_items.order_items_order_id_fkey": "FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE",
  "order_items.order_items_qty_check": "CHECK ((qty >= 1))",
  "order_items.order_items_status_check": "CHECK ((status = ANY (ARRAY['queued'::text, 'preparing'::text, 'ready'::text, 'served'::text, 'cancelled'::text])))",
  "order_items.order_items_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "order_items.order_items_unit_price_snapshot_check": "CHECK ((unit_price_snapshot >= 0))",
  "orders.orders_channel_check": "CHECK ((channel = ANY (ARRAY['dine_in'::text, 'takeaway'::text, 'delivery'::text])))",
  "orders.orders_parent_order_id_fkey": "FOREIGN KEY (parent_order_id) REFERENCES orders(id) ON DELETE SET NULL",
  "orders.orders_source_check": "CHECK ((source = ANY (ARRAY['qr'::text, 'staff'::text, 'online'::text])))",
  "orders.orders_status_check": "CHECK ((status = ANY (ARRAY['pending_confirm'::text, 'confirmed'::text, 'preparing'::text, 'ready'::text, 'served'::text, 'completed'::text, 'cancelled'::text])))",
  "orders.orders_table_session_id_fkey": "FOREIGN KEY (table_session_id) REFERENCES table_sessions(id) ON DELETE SET NULL",
  "orders.orders_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "payments.payments_amount_check": "CHECK ((amount >= 0))",
  "payments.payments_bill_id_fkey": "FOREIGN KEY (bill_id) REFERENCES bills(id) ON DELETE CASCADE",
  "payments.payments_method_check": "CHECK ((method = ANY (ARRAY['cash'::text, 'transfer'::text])))",
  "payments.payments_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "print_jobs.print_jobs_status_check": "CHECK ((status = ANY (ARRAY['pending'::text, 'printed'::text, 'failed'::text, 'superseded'::text])))",
  "print_jobs.print_jobs_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "print_jobs.print_jobs_type_check": "CHECK ((type = ANY (ARRAY['receipt'::text, 'kitchen_ticket'::text, 'customer_ticket'::text])))",
  "printer_heartbeats.printer_heartbeats_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "profiles.profiles_id_fkey": "FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE",
  "recipe_lines.recipe_lines_ingredient_id_fkey": "FOREIGN KEY (ingredient_id) REFERENCES ingredients(id) ON DELETE RESTRICT",
  "recipe_lines.recipe_lines_menu_item_id_fkey": "FOREIGN KEY (menu_item_id) REFERENCES menu_items(id) ON DELETE CASCADE",
  "recipe_lines.recipe_lines_modifier_option_id_fkey": "FOREIGN KEY (modifier_option_id) REFERENCES modifier_options(id) ON DELETE CASCADE",
  "recipe_lines.recipe_lines_not_self": "CHECK ((parent_ingredient_id IS DISTINCT FROM ingredient_id))",
  "recipe_lines.recipe_lines_one_owner": "CHECK ((num_nonnulls(menu_item_id, modifier_option_id, parent_ingredient_id) = 1))",
  "recipe_lines.recipe_lines_parent_ingredient_id_fkey": "FOREIGN KEY (parent_ingredient_id) REFERENCES ingredients(id) ON DELETE CASCADE",
  "recipe_lines.recipe_lines_qty_check": "CHECK ((qty > (0)::numeric))",
  "recipe_lines.recipe_lines_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "reservations.reservations_area_id_fkey": "FOREIGN KEY (area_id) REFERENCES areas(id) ON DELETE SET NULL",
  "reservations.reservations_party_size_check": "CHECK ((party_size >= 1))",
  "reservations.reservations_status_check": "CHECK ((status = ANY (ARRAY['pending'::text, 'confirmed'::text, 'rejected'::text, 'cancelled'::text])))",
  "reservations.reservations_table_id_fkey": "FOREIGN KEY (table_id) REFERENCES tables(id) ON DELETE SET NULL",
  "reservations.reservations_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "staff_calls.staff_calls_resolved_by_fkey": "FOREIGN KEY (resolved_by) REFERENCES memberships(id) ON DELETE SET NULL",
  "staff_calls.staff_calls_status_check": "CHECK ((status = ANY (ARRAY['pending'::text, 'resolved'::text])))",
  "staff_calls.staff_calls_table_id_fkey": "FOREIGN KEY (table_id) REFERENCES tables(id) ON DELETE CASCADE",
  "staff_calls.staff_calls_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "super_admins.super_admins_user_id_fkey": "FOREIGN KEY (user_id) REFERENCES auth.users(id) ON DELETE CASCADE",
  "table_sessions.table_sessions_status_check": "CHECK ((status = ANY (ARRAY['open'::text, 'closed'::text])))",
  "table_sessions.table_sessions_table_id_fkey": "FOREIGN KEY (table_id) REFERENCES tables(id) ON DELETE CASCADE",
  "table_sessions.table_sessions_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "tables.tables_area_id_fkey": "FOREIGN KEY (area_id) REFERENCES areas(id) ON DELETE SET NULL",
  "tables.tables_qr_token_key": "UNIQUE (qr_token)",
  "tables.tables_seats_check": "CHECK ((seats >= 1))",
  "tables.tables_status_check": "CHECK ((status = ANY (ARRAY['available'::text, 'occupied'::text, 'reserved'::text, 'cleaning'::text])))",
  "tables.tables_tenant_id_fkey": "FOREIGN KEY (tenant_id) REFERENCES tenants(id) ON DELETE CASCADE",
  "tenants.tenants_slug_key": "UNIQUE (slug)",
  "tenants.tenants_status_check": "CHECK ((status = ANY (ARRAY['active'::text, 'suspended'::text])))",
  "tenants.tenants_subdomain_key": "UNIQUE (subdomain)"
 }
}
